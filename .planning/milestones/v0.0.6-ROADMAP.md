# Milestone v0.0.6: 路径文件的统一

**Status:** ✅ SHIPPED 2026-06-12
**Phases:** 20-25
**Total Plans:** 13

## Overview

参照 Claude Code 路径机制，实现统一配置层级体系和模块自动发现。从 Settings 模型构建到全模块适配器集成，完成端到端链路验证。

## Phases

### Phase 20: Config Foundation — Settings Model + Merge Engine

**Goal**: 合并引擎和 Settings 模型就绪，可被后续所有阶段依赖
**Depends on**: Nothing (first phase of v0.0.6, pure new code)
**Requirements**: CFG-02, CFG-03, CFG-06
**Plans**: 1 plan

Plans:

- [x] 20-01: Settings model + merge engine

**Details:**
- _merge_settings() handles three strategies: scalar override takes highest-priority value, dict shallow-merges keys, array produces union with dedup and order preserved
- Settings Pydantic model instantiates with all-default values when no config files exist (fresh-install safe)
- APP_* environment variables override scalar Settings fields at validation time using env_nested_delimiter='__'
- Zero new dependencies — custom _merge_settings() replaces pydantic-settings deep_merge

---

### Phase 21: Discovery + Loader + AGENTS.md Chain

**Goal**: ConfigLoader 作为完整可用的统一入口，支持路径发现和指令链加载
**Depends on**: Phase 20
**Requirements**: CFG-01, CFG-04, CFG-05, INS-01, INS-02, INS-04, INS-05
**Plans**: 2 plans

Plans:

- [x] 21-01: discover_paths() + ConfigLoader core
- [x] 21-02: AGENTS.md chain loader + Profile loading

**Details:**
- ConfigLoader.load_settings() returns a merged Settings object by reading global → project → local → env in priority order
- discover_paths(module_name) returns ordered [global_path, project_path] for any of the 8 supported module types
- load_agents_md() concatenates the full instruction chain: global AGENTS.md → project AGENTS.md → local AGENTS.md → parent directory traversal (stopping at .git boundary) → user.md
- Profile loading reads profiles/<name>/ directory files from discovered paths

---

### Phase 22: Simple Module Adapters — Skills, Hooks, Commands

**Goal**: Skills、Hooks、Commands 模块可通过 from_loader() 工厂方法从 ConfigLoader 初始化
**Depends on**: Phase 21
**Requirements**: ADP-01, ADP-02, ADP-03, ADP-09
**Plans**: 1 plan

Plans:

- [x] 22-01: Three from_loader() factory methods (SkillRegistry, HookManager, CommandDispatcher) with TDD

**Details:**
- SkillRegistry.from_loader(loader) creates a registry populated from discover("skills") paths
- HookManager.from_loader(loader) loads and merges hooks from all discovered hooks.json files
- CommandRegistry.from_loader(loader) loads commands from all discovered command directories
- All existing constructor signatures remain unchanged — from_loader() is purely additive

---

### Phase 23: Complex Module Adapters — Agents, Profiles, MCP, Tasks, Permissions

**Goal**: 所有剩余模块适配器完成，支持多目录扫描和名称冲突处理
**Depends on**: Phase 22
**Requirements**: ADP-04, ADP-05, ADP-06, ADP-07, ADP-08
**Plans**: 2 plans

Plans:

- [x] 23-01: Agents and Profiles adapters
- [x] 23-02: MCP, Tasks, Permissions adapters

**Details:**
- AgentConfig.from_loader() loads agents from multi-directory scan with project overriding global and emitting warnings on name collisions
- AgentProfile.from_profile() discovers and loads a named profile across global and project scopes
- McpManager.from_loader() merges MCP server configs from all discovered paths
- TaskManager defaults tasks_dir to .agent-framework/tasks/
- PermissionPipeline receives allow/deny lists from Settings.permissions automatically

---

### Phase 24: Backend Integration + E2E Wiring + Path-Scoped Rules

**Goal**: 应用层完整集成，端到端链路验证通过，零回归
**Depends on**: Phase 23
**Requirements**: INS-03, INS-06, INT-01, INT-02, INT-03, INT-04, INT-05, INT-06
**Plans**: 3 plans

Plans:

- [x] 24-01: RuleLoader module + config leaf dependency test
- [x] 24-02: PromptAssembler integration + test signature updates
- [x] 24-03: Backend wiring + AgentFactory.from_configloader + E2E test

**Details:**
- backend/app/config/ derives default values from ConfigLoader.load_settings() without circular imports
- Backend AgentFactory initializes module registries via ConfigLoader in a single startup flow
- rules/*.md files support frontmatter paths conditions for scoped loading (path-scoped rules)
- PromptAssembler integrates the full instruction chain and Profile files into corresponding tags
- Full end-to-end test: ConfigLoader loads settings → discovers modules → adapters create registries

---

### Phase 25: Address Tech Debt — Integration Warnings + Stale Docs

**Goal**: 清理 v0.0.6 遗留的技术债务：集成测试中的 asyncio 警告和过时文档
**Depends on**: Phase 24
**Plans**: 4 plans

Plans:

- [x] 25-01: Asyncio warning verification + STATE.md update
- [x] 25-02: REQUIREMENTS.md completion markers + traceability
- [x] 25-03: README.md full refresh (class names, modules, roadmap)
- [x] 25-04: PROJECT.md + CONCERNS.md updates

**Details:**
- asyncio timing/cleanup warnings (WR-01/02/03) self-healed via strict deprecation checks
- All 26 REQUIREMENTS.md items marked complete with updated traceability table
- README.md refreshed: class names fixed, config/rules modules added, milestone-level roadmap
- PROJECT.md updated to reflect v0.0.6 shipped state, 4 CONCERNS.md issues resolved

---

## Milestone Summary

**Key Decisions:**

- Decision: Zero new dependencies — custom _merge_settings() replaces pydantic-settings deep_merge (Rationale: 框架层保持轻量)
- Decision: config/ module as leaf dependency (Rationale: imports nothing from other framework modules, prevents circular imports)
- Decision: Additive API only — from_loader() factory methods alongside untouched constructors (Rationale: backward compatibility)
- Decision: Natural-order iteration (last-write-wins) for project-override-global in from_loader() methods (Rationale: matches Claude Code convention)
- Decision: flat frontmatter for Agent configs (Rationale: no pyyaml dependency)
- Decision: shallow merge only for nested dicts (Rationale: design doc explicit, avoids over-complexity)

**Issues Resolved:**

- RESOLVED: asyncio timing/cleanup warnings self-healed
- RESOLVED: REQUIREMENTS.md stale checkboxes and traceability updated
- RESOLVED: README.md class name mismatches (CommandRouter → CommandDispatcher)
- RESOLVED: 4 CONCERNS.md issues marked resolved (backend scaffold, orchestrator engine, agent tool dispatch, web_search mock)

**Issues Deferred (7 WARNING integration items):**

- INT-W01: AgentFactory duplicate assembler/skill-registry construction (works but wasteful)
- INT-W02: context_path for scoped rules never forwarded to runtime (feature tested but not activated)
- INT-W03: CommandDispatcher loaded but not consumed downstream
- INT-W04: AgentFactory from_settings() path produces structurally inconsistent objects
- INT-W05: McpManager.from_loader() implemented but no production wiring
- INT-W06: PermissionPipeline.from_loader() implemented but no production wiring
- INT-W07: rules/__init__.py barrel export unused (direct import)

**Technical Debt Incurred:**

- Module adapters loaded but outputs not consumed in production paths (5 of 8 adapters)
- Naming mismatch: CommandRegistry vs CommandDispatcher in ROADMAP/REQUIREMENTS (documented, functional)
- Naming mismatch: AgentProfile.from_loader vs AgentConfig.from_loader in ROADMAP/REQUIREMENTS (documented, functional)

---

_For current project status, see .planning/ROADMAP.md_
