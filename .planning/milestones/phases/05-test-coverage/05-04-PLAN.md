---
phase: 05-test-coverage
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified: []
autonomous: true
requirements: [R5]

must_haves:
  truths:
    - "全量 675+ 测试通过，无回归"
    - "新增 12 个测试全部通过（5 loop + 3 safety + 4 permissions）"
  artifacts: []
  key_links: []
---

<objective>
运行全量测试套件，确认 Phase 5 新增测试无回归。

Purpose: D-12 要求全量测试确认无回归。这是最终验证步骤，依赖 01-03 三个 plan 全部完成。
Output: 无新文件，测试通过确认。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and verify no regressions</name>
  <files></files>
  <read_first>
    - framework/tests/test_teams_manager.py (verify TestTeamLoop class exists)
    - framework/tests/test_safety_integration.py (verify 3 tests exist)
    - framework/tests/test_permissions.py (verify TestEdgeCases class exists)
  </read_first>
  <action>
Per D-11 and D-12: Run the full test suite to confirm all existing and new tests pass with zero regressions.

Step 1: Verify the new test files/classes exist:
- `grep "class TestTeamLoop" framework/tests/test_teams_manager.py` — must find
- `grep "async def test_" framework/tests/test_safety_integration.py` — must find 3
- `grep "class TestEdgeCases" framework/tests/test_permissions.py` — must find

Step 2: Run the full test suite:
```bash
cd framework && python -m pytest tests/ -v --tb=short
```

Step 3: Verify the count. Expect 675+ total tests (662 existing + 5 loop + 3 safety + 4 permissions = ~674 minimum). The exact count may vary slightly if other tests were added in prior phases.

Step 4: If any test fails, diagnose and report the failure. Do NOT fix — report back for the specific plan to address.

Step 5: Report final count: `grep -r "def test_\|async def test_" framework/tests/ | wc -l`
  </action>
  <acceptance_criteria>
    - `cd framework && python -m pytest tests/ -q --tb=short` exits with code 0
    - Total test count is >= 674 (662 baseline + 12 new)
    - All 3 new test files/classes present (TestTeamLoop, test_safety_integration.py, TestEdgeCases)
    - Zero test failures
  </acceptance_criteria>
  <verify>
    <automated>cd framework && python -m pytest tests/ -q --tb=short</automated>
  </verify>
  <done>Full test suite passes with 675+ tests, zero failures. Phase 5 complete with no regressions.</done>
</task>

</tasks>

<verification>
```bash
cd framework && python -m pytest tests/ -q --tb=short
```
Exit code 0. Total test count >= 674.
</verification>

<success_criteria>
- Full test suite passes (exit code 0)
- Total test count >= 674
- Zero regressions (no previously passing test now fails)
- All Phase 5 new tests pass
</success_criteria>

<output>
Create `.planning/phases/05-test-coverage/05-04-SUMMARY.md` when done
</output>
