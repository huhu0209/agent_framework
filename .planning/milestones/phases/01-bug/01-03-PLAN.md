---
phase: 01-bug
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - framework/agent_framework/llm/transform/_normalize.py
  - framework/tests/test_normalize_messages.py
autonomous: true
requirements: [R2]
mode: gap_closure

must_haves:
  truths:
    - "normalize_messages 返回的所有消息对象与输入无共享引用（包括 SystemMessage 和 ToolMessage）"
  artifacts:
    - path: "framework/agent_framework/llm/transform/_normalize.py"
      provides: "SystemMessage/ToolMessage model_copy 修复"
      contains: "msg.model_copy()"
    - path: "framework/tests/test_normalize_messages.py"
      provides: "SystemMessage/ToolMessage 不可变性测试"
      min_lines: 200
  key_links:
    - from: "framework/agent_framework/llm/transform/_normalize.py"
      to: "msg.model_copy()"
      via: "bare append 替换"
      pattern: "model_copy"
---

<objective>
修复 VERIFICATION.md CR-01: _normalize.py 中 SystemMessage/ToolMessage 的 3 处 bare `result.append(msg)` 缺少 model_copy()，导致返回列表与输入共享对象引用。

Purpose: Gap closure — VERIFICATION 报告 normalize_messages 对 UserMessage/AssistantMessage 已正确使用 model_copy，但对 SystemMessage/ToolMessage 仍使用 bare append，违反不可变性约束。

Output: 3 行代码修复 + 2 个不可变性测试，全量测试通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-bug/01-VERIFICATION.md

<interfaces>
<!-- Gap source: VERIFICATION.md CR-01 -->
From framework/agent_framework/llm/transform/_normalize.py:
  Line 33: result.append(msg)           # SystemMessage/ToolMessage as first msg
  Line 40: result.append(msg)           # SystemMessage/ToolMessage non-merge path
  Line 52: result.append(msg)           # SystemMessage/ToolMessage in else branch

  Lines 31, 45, 50 already correctly use model_copy() for UserMessage/AssistantMessage.

<!-- Existing test coverage -->
From framework/tests/test_normalize_messages.py:
  TestImmutability class has 3 tests covering UserMessage/AssistantMessage.
  Missing: SystemMessage and ToolMessage reference isolation tests.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修复 SystemMessage/ToolMessage bare append + 补充不可变性测试</name>
  <files>framework/agent_framework/llm/transform/_normalize.py, framework/tests/test_normalize_messages.py</files>
  <read_first>
    - framework/agent_framework/llm/transform/_normalize.py
    - framework/tests/test_normalize_messages.py
  </read_first>
  <behavior>
    - Test 1: SystemMessage 输入后，result[0] 与原始 SystemMessage 不是同一对象（id 不同）
    - Test 2: ToolMessage 输入后，result 中的 ToolMessage 与原始不是同一对象（id 不同）
    - 修改输入消息的 content 不会影响 result 中对应消息的 content
  </behavior>
  <action>
    1. 在 framework/tests/test_normalize_messages.py 的 TestImmutability 类中新增两个测试（TDD RED 先写测试）：

    test_system_message_not_shared_reference:
      - 创建 SystemMessage(content="sys"), UserMessage(content=[TextBlock(text="hi")])
      - 调用 normalize_messages
      - 断言 result[0] is not sys_msg（对象身份隔离）
      - 验证修改 sys_msg.content 不影响 result[0].content

    test_tool_message_not_shared_reference:
      - 创建 UserMessage, AssistantMessage(content=[ToolUseBlock(id="t1", ...)]), ToolMessage(tool_call_id="t1", content="data")
      - 调用 normalize_messages
      - 找到 result 中 ToolMessage，断言 is not tool_msg（对象身份隔离）
      - 验证修改 tool_msg.content 不影响 result 中对应消息的 content

    2. 在 framework/agent_framework/llm/transform/_normalize.py 修改 3 处 bare append（TDD GREEN）：

    Line 33: result.append(msg) → result.append(msg.model_copy())
    Line 40: result.append(msg) → result.append(msg.model_copy())
    Line 52: result.append(msg) → result.append(msg.model_copy())

    这是安全的浅拷贝：SystemMessage.content 是 str（不可变），ToolMessage 无可变内容字段。
  </action>
  <acceptance_criteria>
    - framework/agent_framework/llm/transform/_normalize.py 第 33、40、52 行均使用 msg.model_copy() 而非 bare msg
    - framework/agent_framework/llm/transform/_normalize.py 不包含 `result.append(msg)` 的 bare append（grep 验证）
    - framework/tests/test_normalize_messages.py TestImmutability 包含 test_system_message_not_shared_reference
    - framework/tests/test_normalize_messages.py TestImmutability 包含 test_tool_message_not_shared_reference
    - `cd framework && pytest tests/test_normalize_messages.py -v` 全部通过
    - `cd framework && pytest tests/ -v --timeout=60` 全量测试无回归
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && pytest tests/test_normalize_messages.py::TestImmutability -v</automated>
  </verify>
  <done>_normalize.py 所有 5 处 append 均使用 model_copy，TestImmutability 覆盖全部 4 种消息类型。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal only | Gap closure 修复，无安全边界变更 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | N/A | Internal immutability fix | accept | 纯内部防御性修复 |
</threat_model>

<verification>
1. `cd framework && grep -n "result.append(msg)" framework/agent_framework/llm/transform/_normalize.py` — 应返回空（无 bare append）
2. `cd framework && grep -n "model_copy" framework/agent_framework/llm/transform/_normalize.py` — 应返回 5+ 行
3. `cd framework && pytest tests/test_normalize_messages.py -v` — 全部通过
4. `cd framework && pytest tests/ -v --timeout=60` — 全量测试无回归
5. Python 验证: `from agent_framework.llm.types import SystemMessage; from agent_framework.llm.transform import normalize_messages; s = SystemMessage(content="test"); r = normalize_messages([s]); assert r[0] is not s`
</verification>

<success_criteria>
- _normalize.py 无 bare `result.append(msg)`（全部通过 model_copy 创建新对象）
- TestImmutability 覆盖 SystemMessage 和 ToolMessage 引用隔离
- 全量测试通过，无回归
</success_criteria>

<output>
Create `.planning/phases/01-bug/01-03-SUMMARY.md` when done
</output>
