---
phase: 01-bug
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - framework/agent_framework/tasks/manager.py
  - framework/tests/test_task_manager.py
autonomous: true
requirements: [R2]

must_haves:
  truths:
    - "TaskManager._apply_changes 的 pending_writes 类型注解为 list[Task]，与实际使用一致"
    - "_clear_dependency 在 lock 内批量收集所有待写变更，最后一次性写入（per D-01）"
    - "_clear_dependency 写入失败时 log warning 而不回滚已完成的 task 状态变更（per D-02）"
    - "每个修复有对应新增测试验证"
  artifacts:
    - path: "framework/agent_framework/tasks/manager.py"
      provides: "类型修复 + 原子性修复"
      contains: "list[Task]"
    - path: "framework/agent_framework/tasks/manager.py"
      provides: "批量依赖清理"
      contains: "pending_clears"
    - path: "framework/tests/test_task_manager.py"
      provides: "原子性测试"
      min_lines: 160
  key_links:
    - from: "framework/agent_framework/tasks/manager.py"
      to: "_apply_changes"
      via: "pending_writes type annotation"
      pattern: "pending_writes.*list\\[Task\\]"
    - from: "framework/agent_framework/tasks/manager.py"
      to: "_clear_dependency"
      via: "batch collection + single write pass"
      pattern: "pending_clears"
---

<objective>
修复 TaskManager 中 2 个 Bug：pending_writes 类型注解错误（Bug #2）和 _clear_dependency 非原子性写入（Bug #5）。

Purpose: 这两个 Bug 都在同一个文件 manager.py 中，且 Bug #5 的修复范围涵盖 _apply_changes 和 _clear_dependency 两个方法。放在一起修复避免文件冲突和重复读取。

Output: manager.py 2 个 Bug 修复 + test_task_manager.py 新增测试，全量测试通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bug/01-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Bug #2: 类型注解错误 -->
From framework/agent_framework/tasks/manager.py line 199:
  pending_writes: list[tuple[Task]] = []
  # Should be list[Task] per D-03 -- code only appends Task objects, iterates as Task

<!-- Bug #5: 非原子性依赖清理 -->
From framework/agent_framework/tasks/manager.py lines 228-236:
  def _clear_dependency(self, completed_id: str):
      for task in self._load_all():
          if completed_id in task.blocked_by:
              new_blocked = [x for x in task.blocked_by if x != completed_id]
              self._write(dataclasses.replace(
                  task,
                  blocked_by=new_blocked,
                  updated_at=self._now(),
              ))
  # Problem: _write called inside loop, if one fails midway, partial writes leave inconsistent state
  # Fix per D-01: collect all pending clears first, then write in one pass inside lock
  # Fix per D-02: if batch write fails, log warning + retry later (don't rollback task status change)

<!-- Caller context -->
From framework/agent_framework/tasks/manager.py lines 90-96:
  async def update(self, task_id: str, **changes) -> Task:
      ...
      updated = self._apply_changes(task, changes)
      if updated.status == TaskStatus.COMPLETED:
          self._clear_dependency(updated.id)
      self._write(updated)
      return updated
  # _clear_dependency is called while self._lock is held

<!-- Task type -->
From framework/agent_framework/tasks/types.py:
  @dataclass(frozen=True)
  class Task:
      id: str
      subject: str
      description: str = ""
      status: TaskStatus = TaskStatus.PENDING
      owner: str = ""
      blocked_by: list[str] = field(default_factory=list)
      blocks: list[str] = field(default_factory=list)
      created_at: str = ""
      updated_at: str = ""

<!-- Existing test patterns -->
From framework/tests/test_task_manager.py:
  - @pytest.fixture def mgr(tmp_path) -- creates TaskManager with temp dir
  - test_add_blocked_by_maintains_bidirectional -- tests bidirectional dependency
  - test_complete_clears_downstream_dependency -- tests _clear_dependency indirectly
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修复 pending_writes 类型注解 + _clear_dependency 原子性 + 测试</name>
  <files>framework/agent_framework/tasks/manager.py, framework/tests/test_task_manager.py</files>
  <read_first>
    - framework/agent_framework/tasks/manager.py
    - framework/tests/test_task_manager.py
    - framework/agent_framework/tasks/types.py
  </read_first>
  <behavior>
    - Test 1: _apply_changes 中 pending_writes 的类型注解为 list[Task]（非 list[tuple[Task]]）
    - Test 2: 完成 task 时 _clear_dependency 批量写入——mock _write 方法，验证当多个 task 的 blocked_by 需要清理时，_write 的调用顺序是先收集再统一写入（可间接验证：完成后所有下游 task 的 blocked_by 已正确清理）
    - Test 3: _clear_dependency 部分写入失败时不影响已完成 task 的状态（per D-02）
  </behavior>
  <action>
    **Bug #2 修复（per D-03）：**
    在 framework/agent_framework/tasks/manager.py 第 199 行，将 `pending_writes: list[tuple[Task]] = []` 改为 `pending_writes: list[Task] = []`。

    **Bug #5 修复（per D-01）：**
    重构 `_clear_dependency` 方法（第 228-236 行），实现 batch writes in lock 模式：
    1. 先遍历所有 task，收集需要清理的依赖到 `pending_clears: list[Task]` 中（使用 dataclasses.replace 创建新 Task 对象，blocked_by 已过滤掉 completed_id）
    2. 遍历 pending_clears，对每个调用 self._write。用 try/except 包裹单个 _write 调用
    3. 写入失败时 log warning 并继续（per D-02：不回滚已完成的 task 状态变更，失败极罕见，依赖清理不完整不影响正确性）
    4. 使用已有的 `logger = logging.getLogger(__name__)`，warning 格式如 `logger.warning("无法清理任务 %s 的依赖关系: %s", dep_task.id, e)`

    最终 `_clear_dependency` 大致结构：
    ```
    def _clear_dependency(self, completed_id: str):
        pending_clears: list[Task] = []
        for task in self._load_all():
            if completed_id in task.blocked_by:
                new_blocked = [x for x in task.blocked_by if x != completed_id]
                pending_clears.append(dataclasses.replace(
                    task, blocked_by=new_blocked, updated_at=self._now(),
                ))
        for dep_task in pending_clears:
            try:
                self._write(dep_task)
            except Exception as e:
                logger.warning("无法清理任务 %s 的依赖关系: %s", dep_task.id, e)
    ```

    **测试（per D-04）：**
    在 framework/tests/test_task_manager.py 中新增：
    1. `test_apply_changes_pending_writes_type` -- 验证 add_blocked_by 和 add_blocks 时依赖 task 正确写入，间接验证 pending_writes 类型正确（Task 对象 append + for 循环遍历）
    2. `test_clear_dependency_batch_writes_all` -- 创建 3 个 task（t1, t2, t3），t2 和 t3 都 blocked_by t1，完成 t1 后验证 t2 和 t3 的 blocked_by 都已清理
    3. `test_clear_dependency_partial_failure_continues` -- 使用 monkeypatch 或 mock 让第二次 _write 抛出异常，验证第一次 _write 的结果仍然生效（t2 被清理），且不抛出异常到上层

    注意：test_clear_dependency_partial_failure_continues 需要 mock self._write。由于 _clear_dependency 是同步方法且在 _lock 内调用，可用 `unittest.mock.patch.object` 在 TaskManager 实例上 mock _write，让它在特定 task id 时抛出异常。
  </action>
  <acceptance_criteria>
    - framework/agent_framework/tasks/manager.py 第 199 行包含 `pending_writes: list[Task]`（不包含 `tuple`）
    - framework/agent_framework/tasks/manager.py `_clear_dependency` 方法包含 `pending_clears` 局部变量
    - framework/agent_framework/tasks/manager.py `_clear_dependency` 方法包含 `logger.warning` 调用
    - framework/tests/test_task_manager.py 包含 `test_clear_dependency_batch_writes_all` 测试函数
    - framework/tests/test_task_manager.py 包含 `test_clear_dependency_partial_failure_continues` 测试函数
    - `cd framework && pytest tests/test_task_manager.py -v` 全部通过
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && pytest tests/test_task_manager.py -v</automated>
  </verify>
  <done>pending_writes 类型注解修正为 list[Task]，_clear_dependency 使用 batch writes + 失败 log warning 策略，新增 3 个测试全部通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal only | 此 plan 仅修改框架内部 TaskManager，不涉及外部输入边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | N/A | Internal bug fixes | accept | 纯内部 Bug 修复，无安全边界变更 |
</threat_model>

<verification>
1. `cd framework && pytest tests/test_task_manager.py -v` -- 所有测试通过（包括新增 3 个）
2. `cd framework && pytest tests/ -v --timeout=60` -- 全量测试无回归
3. `grep -n "pending_writes.*list\[Task\]" framework/agent_framework/tasks/manager.py` -- 确认类型修复
4. `grep -n "pending_clears" framework/agent_framework/tasks/manager.py` -- 确认 batch 收集
5. `grep -n "logger.warning" framework/agent_framework/tasks/manager.py` -- 确认失败处理
</verification>

<success_criteria>
- 2 个 Bug 修复完成（类型注解 + 原子性），每个有对应新增测试
- _clear_dependency 先收集再批量写入，失败时 log warning 不中断
- 全量测试通过（630 tests, 0 failures）
</success_criteria>

<output>
Create `.planning/phases/01-bug/01-02-SUMMARY.md` when done
</output>
