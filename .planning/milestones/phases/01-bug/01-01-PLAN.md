---
phase: 01-bug
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - framework/agent_framework/agents/agent_loop.py
  - framework/agent_framework/safety/hitl.py
  - framework/agent_framework/llm/transform/_normalize.py
  - framework/tests/test_agent_loop.py
  - framework/tests/test_hitl.py
  - framework/tests/test_normalize_messages.py
autonomous: true
requirements: [R2]

must_haves:
  truths:
    - "AgentLoop 可用 list[Path] 类型参数 skill_dirs 实例化，不再触发 NameError"
    - "HITLManager.create_pending 使用 get_running_loop 而非已废弃的 get_event_loop"
    - "normalize_messages 不再原地变异输入消息的 content 字段"
    - "每个修复有对应新增测试验证"
  artifacts:
    - path: "framework/agent_framework/agents/agent_loop.py"
      provides: "Path import 修复"
      contains: "from pathlib import Path"
    - path: "framework/agent_framework/safety/hitl.py"
      provides: "deprecated API 修复"
      contains: "get_running_loop"
    - path: "framework/agent_framework/llm/transform/_normalize.py"
      provides: "不可变合并修复"
      contains: "model_copy"
    - path: "framework/tests/test_normalize_messages.py"
      provides: "变异测试"
      min_lines: 140
  key_links:
    - from: "framework/agent_framework/agents/agent_loop.py"
      to: "pathlib.Path"
      via: "import"
      pattern: "from pathlib import Path"
    - from: "framework/agent_framework/safety/hitl.py"
      to: "asyncio.get_running_loop"
      via: "API call"
      pattern: "get_running_loop"
    - from: "framework/agent_framework/llm/transform/_normalize.py"
      to: "result list"
      via: "model_copy replacement"
      pattern: "model_copy"
---

<objective>
修复 3 个独立的单文件 Bug：agent_loop.py 缺少 Path import、hitl.py 已废弃 API、normalize_messages 原地变异。

Purpose: 这 3 个 Bug 互不依赖，各自影响不同模块，可在一个 plan 中并行修复。每个修复新增针对性测试（per D-04）。

Output: 3 个源文件修复 + 3 个测试文件扩展，全量测试通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Bug #1: agent_loop.py 缺少 Path import -->
From framework/agent_framework/agents/agent_loop.py line 87:
  skill_dirs: list[Path] | None = None
  # Path is NOT imported -- NameError at runtime when skill_dirs is passed

From framework/agent_framework/agents/agent_loop.py lines 6-10:
  import asyncio
  from dataclasses import dataclass, field
  from typing import Any, AsyncGenerator, TYPE_CHECKING
  # Missing: from pathlib import Path

<!-- Bug #3: hitl.py deprecated API -->
From framework/agent_framework/safety/hitl.py line 47:
  loop = asyncio.get_event_loop()
  # Deprecated in Python 3.10+, should use get_running_loop()

From framework/agent_framework/safety/hitl.py lines 5-6:
  import asyncio
  from typing import Literal

<!-- Bug #4: normalize_messages 原地变异 -->
From framework/agent_framework/llm/transform/_normalize.py line 44-45:
  if type(msg) is type(last) and isinstance(msg, (UserMessage, AssistantMessage)):
      last.content = [*last.content, *msg.content]
      # Mutates Pydantic model in-place -- must use model_copy instead

<!-- Existing test patterns -->
From framework/tests/test_normalize_messages.py:
  class TestImmutability -- already has test_original_messages_not_mutated
  # Need additional test for the merged message object itself being new

From framework/tests/test_hitl.py:
  class TestHITLManager -- uses asyncio.run() for async tests
  # Need test verifying create_pending works inside running loop
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修复 agent_loop.py 缺少 Path import + 验证测试</name>
  <files>framework/agent_framework/agents/agent_loop.py, framework/tests/test_agent_loop.py</files>
  <read_first>
    - framework/agent_framework/agents/agent_loop.py
    - framework/tests/test_agent_loop.py
    - framework/tests/conftest.py
  </read_first>
  <behavior>
    - Test 1: 实例化 AgentLoop 并传入 skill_dirs=[Path("/tmp")] 不触发 NameError，且 AgentLoop 实例的 _skill_dirs 属性正确存储了传入值
  </behavior>
  <action>
    Bug #1 修复：在 framework/agent_framework/agents/agent_loop.py 顶部 import 区域（第 9 行 `from dataclasses import ...` 之前或之后），添加 `from pathlib import Path`。这是唯一的代码改动——`skill_dirs: list[Path] | None` 在第 87 行已正确引用 Path，只是 import 缺失。

    测试：在 framework/tests/test_agent_loop.py 中新增测试函数 `test_skill_dirs_accepted_without_name_error`。测试使用 MockAdapter（从 conftest.py import）和 ToolRouter 的最小构造，传入 `skill_dirs=[Path("/tmp")]` 参数，验证 AgentLoop 实例化成功且无 NameError。参考现有 test_agent_loop.py 中的 fixture 模式。

    注意：Claude's Discretion 允许自行选择测试组织方式。若现有 test_agent_loop.py 中有合适的 fixture 可复用则复用，否则构建最小必要 fixture。
  </action>
  <acceptance_criteria>
    - framework/agent_framework/agents/agent_loop.py 包含 `from pathlib import Path` import 语句
    - framework/tests/test_agent_loop.py 包含 `test_skill_dirs_accepted_without_name_error` 测试函数
    - `cd framework && pytest tests/test_agent_loop.py::test_skill_dirs_accepted_without_name_error -v` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && pytest tests/test_agent_loop.py -k "skill_dirs" -v</automated>
  </verify>
  <done>Path import 已添加，AgentLoop 传入 skill_dirs=[Path("/tmp")] 不再 NameError，新增测试通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 修复 HITLManager.create_pending 已废弃 API + 验证测试</name>
  <files>framework/agent_framework/safety/hitl.py, framework/tests/test_hitl.py</files>
  <read_first>
    - framework/agent_framework/safety/hitl.py
    - framework/tests/test_hitl.py
  </read_first>
  <behavior>
    - Test 1: create_pending 在 async 上下文中调用成功，返回的 Future 可被 resolve，结果正确
    - Test 2: create_pending 不再调用 get_event_loop（通过 mock 或属性检查验证使用 get_running_loop）
  </behavior>
  <action>
    Bug #3 修复：在 framework/agent_framework/safety/hitl.py 第 47 行，将 `loop = asyncio.get_event_loop()` 替换为 `loop = asyncio.get_running_loop()`。`get_running_loop` 在 Python 3.10+ 推荐使用，且仅在已运行的 event loop 上下文中有效——这正好符合 HITLManager 的使用场景（总是在 async 函数中调用）。

    测试：在 framework/tests/test_hitl.py 的 TestHITLManager 类中新增测试：
    1. `test_create_pending_uses_running_loop` -- 在 async 函数内调用 create_pending，验证返回的 Future 可被 resolve 且结果正确（复用现有 test_create_and_resolve 的模式，但明确在 async 上下文中调用）
    2. 可选：用 `monkeypatch` 或 `unittest.mock.patch` 验证 `asyncio.get_event_loop` 不被调用

    现有 test_create_and_resolve 使用 `asyncio.run()` 包裹，它在内部创建了 running loop，所以替换后现有测试应自然通过。确认现有 3 个测试（test_create_and_resolve, test_resolve_unknown_id_raises, test_cancel_all）仍全部通过。
  </action>
  <acceptance_criteria>
    - framework/agent_framework/safety/hitl.py 不包含 `get_event_loop`
    - framework/agent_framework/safety/hitl.py 包含 `get_running_loop`
    - `cd framework && pytest tests/test_hitl.py -v` 全部通过（现有 3 个 + 新增测试）
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && pytest tests/test_hitl.py -v</automated>
  </verify>
  <done>create_pending 使用 get_running_loop 替代已废弃 get_event_loop，现有测试通过，新增测试验证行为正确。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 修复 normalize_messages 原地变异 Pydantic 模型 + 验证测试</name>
  <files>framework/agent_framework/llm/transform/_normalize.py, framework/tests/test_normalize_messages.py</files>
  <read_first>
    - framework/agent_framework/llm/transform/_normalize.py
    - framework/tests/test_normalize_messages.py
    - .planning/codebase/CONVENTIONS.md (Pydantic v2 不可变模式)
  </read_first>
  <behavior>
    - Test 1: 连续 UserMessage 合并后，result 列表中被合并的消息对象是新的（id() 不同），原始输入消息未被修改
    - Test 2: 连续 AssistantMessage 合并后，原始输入消息的 content 未被修改
    - Test 3: 合并后消息的 content 包含所有被合并消息的 content blocks
  </behavior>
  <action>
    Bug #4 修复：在 framework/agent_framework/llm/transform/_normalize.py 第 44-45 行，将原地变异：
      last.content = [*last.content, *msg.content]
    替换为创建新对象：
      merged = last.model_copy(update={"content": [*last.content, *msg.content]})
      result[-1] = merged
    这遵循 CONVENTIONS.md 中的 Pydantic v2 不可变模式（`model_copy(update={...})` 创建新对象而非原地变异）。

    测试：在 framework/tests/test_normalize_messages.py 的 TestImmutability 类中新增：
    1. `test_merged_message_is_new_object` -- 创建两个连续 UserMessage，调用 normalize_messages，验证 result[0] 不是原始 msg1（id() 不同），且 msg1.content 未被修改
    2. `test_merged_assistant_not_mutated` -- 同理但使用 AssistantMessage

    注意：现有 test_original_messages_not_mutated 测试的是 msg1.content 的 id 是否保留，但第 45 行的变异恰好把 msg1.content 替换成了一个新 list（`[*last.content, *msg.content]`），所以 msg1.content 的值确实变了。新增测试应明确验证 msg1.content 的内容值未被改变。
  </action>
  <acceptance_criteria>
    - framework/agent_framework/llm/transform/_normalize.py 第 45 行附近不包含对 `last.content =` 的直接赋值（原地变异）
    - framework/agent_framework/llm/transform/_normalize.py 包含 `model_copy` 调用
    - framework/tests/test_normalize_messages.py TestImmutability 类包含至少 1 个新增测试
    - `cd framework && pytest tests/test_normalize_messages.py -v` 全部通过
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && pytest tests/test_normalize_messages.py -v</automated>
  </verify>
  <done>normalize_messages 使用 model_copy 创建新对象替代原地变异 content，原始消息对象不受影响，新增不可变性测试通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal only | 此 plan 仅修改框架内部代码，不涉及外部输入边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | N/A | Internal bug fixes | accept | 纯内部 Bug 修复，无安全边界变更 |
</threat_model>

<verification>
1. `cd framework && pytest tests/test_agent_loop.py tests/test_hitl.py tests/test_normalize_messages.py -v` -- 所有测试通过
2. `cd framework && pytest tests/ -v --timeout=60` -- 全量测试无回归
3. `grep -n "from pathlib import Path" framework/agent_framework/agents/agent_loop.py` -- 确认 import 存在
4. `grep -n "get_running_loop" framework/agent_framework/safety/hitl.py` -- 确认使用新 API
5. `grep -n "model_copy" framework/agent_framework/llm/transform/_normalize.py` -- 确认使用不可变模式
</verification>

<success_criteria>
- 3 个 Bug 修复完成，每个有对应新增测试
- 全量测试通过（630 tests, 0 failures）
- agent_loop.py 无 NameError
- hitl.py 无 deprecated API 调用
- normalize_messages 不原地变异输入消息
</success_criteria>

<output>
Create `.planning/phases/01-bug/01-01-SUMMARY.md` when done
</output>
