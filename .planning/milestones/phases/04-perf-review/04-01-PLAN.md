---
phase: 04-perf-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - framework/agent_framework/teams/bus.py
  - framework/tests/test_teams_bus.py
  - framework/agent_framework/tools/mcp/transport.py
  - framework/tests/test_mcp_transport.py
  - docs/reviews/PERF-REVIEW.md
autonomous: true
requirements: [R4]

must_haves:
  truths:
    - "read_inbox 原子清零 — 进程崩溃不会丢失已读消息"
    - "MCP header 读取使用 readline() 而非逐字节 read(1)"
    - "所有既有测试 + 新增测试全部通过"
    - "PERF-REVIEW.md 记录 2 个已修复项 + 3 个仅记录项"
  artifacts:
    - path: "framework/agent_framework/teams/bus.py"
      provides: "原子 inbox 读写"
      contains: "os.replace"
    - path: "framework/agent_framework/tools/mcp/transport.py"
      provides: "高效 MCP header 解析"
      contains: "readline"
    - path: "docs/reviews/PERF-REVIEW.md"
      provides: "性能审查报告"
      contains: "PERF-"
  key_links:
    - from: "framework/agent_framework/teams/bus.py"
      to: "tempfile + os.replace"
      via: "原子清零"
      pattern: "os\\.replace"
    - from: "framework/agent_framework/tools/mcp/transport.py"
      to: "StreamReader.readline"
      via: "MCP header 解析"
      pattern: "readline\\(\\)"
---

<objective>
修复 2 个影响数据安全的性能问题，新增对应测试，产出 PERF-REVIEW.md。

Purpose: 消除消息丢失风险（非原子读写）和性能瓶颈（逐字节读取），记录其他性能优化建议。
Output: bus.py 原子读写、transport.py 高效 header 解析、PERF-REVIEW.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-perf-review/04-CONTEXT.md

<interfaces>
<!-- MessageBus 当前接口 — 修改 read_inbox 方法 -->
From framework/agent_framework/teams/bus.py:
```python
class MessageBus:
    def __init__(self, team_dir: Path) -> None
    def send(self, sender: str, to: str, content: str, *, msg_type: str = "message") -> TeamMessage
    def read_inbox(self, name: str) -> list[TeamMessage]  # <-- 修改目标
    def broadcast(self, sender: str, teammates: list[str], content: str) -> None
```

<!-- 原子写入参考实现 — 从 index_manager.py 提取 -->
From framework/agent_framework/memory/index_manager.py:
```python
def _atomic_write(self, text: str) -> None:
    self._path.parent.mkdir(parents=True, exist_ok=True)
    fd, tmp_path = tempfile.mkstemp(
        dir=self._path.parent, suffix=".tmp", prefix=".memory_idx_"
    )
    try:
        with os.fdopen(fd, "w", encoding="utf-8") as f:
            f.write(text)
        os.replace(tmp_path, self._path)
    except BaseException:
        try:
            os.unlink(tmp_path)
        except OSError:
            pass
        raise
```

<!-- MCP transport 当前接口 — 修改 _read_until_header_end 方法 -->
From framework/agent_framework/tools/mcp/transport.py:
```python
class StdioTransport(McpTransport):
    async def _read_until_header_end(self) -> bytes  # <-- 修改目标
    def _parse_content_length(header: bytes) -> int
    async def _read_exact(self, n: int) -> bytes
```

<!-- TeamMessage 类型 -->
From framework/agent_framework/teams/types.py:
```python
class TeamMessage:
    type: str
    from_: str
    to: str
    content: str
    timestamp: float
```

<!-- SECURITY-REVIEW.md 格式 — PERF-REVIEW.md 参照 -->
每个问题包含: 描述 + 文件位置 + 严重性 + 修复状态
分为 "已修复" 和 "已记录" 两大区域，区域内按严重性排列
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修复 MessageBus inbox 非原子读写 (D-01, D-02, D-03)</name>
  <files>framework/agent_framework/teams/bus.py, framework/tests/test_teams_bus.py</files>
  <read_first>
    - framework/agent_framework/teams/bus.py (当前 read_inbox 实现)
    - framework/agent_framework/memory/index_manager.py (_atomic_write 参考实现，第 26-41 行)
    - framework/tests/test_teams_bus.py (现有测试，追加新测试)
  </read_first>
  <behavior>
    - test_read_inbox_atomic_no_message_loss_on_simulated_crash: 发送消息后 mock os.replace 在第一次调用时抛出 OSError，验证 read_inbox 仍返回已读取的消息（非空列表），下次 read_inbox 调用返回相同消息（可重复读取）
    - test_read_inbox_atomic_swap_uses_tempfile: 发送消息后调用 read_inbox，验证 JSONL 文件内容已清空（空字符串），消息正确返回
    - test_read_inbox_atomic_cleanup_on_failure: mock os.replace 抛异常后，验证临时文件被清理（os.unlink 被调用）
  </behavior>
  <action>
    在 bus.py 顶部添加 `import logging`, `import os`, `import tempfile` 导入。添加模块级 `logger = logging.getLogger(__name__)`。

    重写 `read_inbox` 方法（当前第 33-48 行）实现原子清零，方案如下（per D-01）：

    1. path 不存在或内容为空 → 返回 []
    2. 读取 raw = path.read_text().strip()，若为空返回 []
    3. 解析 JSONL 行为 TeamMessage 列表（保留现有 try/except 逻辑）
    4. 原子清零：创建临时文件写入空字符串，然后 os.replace(temp, path) 覆盖。参考 index_manager.py 的 _atomic_write 模式
    5. 原子清零失败处理（per D-02）：在 except 块中仅 logger.warning("原子清零收件箱失败: %s", path)，然后继续返回已解析的消息。临时文件需在 except 中尝试 os.unlink 清理
    6. 返回消息列表

    临时文件命名：使用 tempfile.mkstemp(dir=self._dir, suffix=".tmp", prefix=".inbox_")，与 index_manager.py 模式一致（per Claude's Discretion）。

    在 test_teams_bus.py 末尾追加 3 个新测试函数（per D-03）：
    - test_read_inbox_atomic_swap: 发送 2 条消息 → read_inbox → 验证返回 2 条消息且文件内容为空
    - test_read_inbox_atomic_no_message_loss_on_failure: 使用 unittest.mock.patch 对 os.replace 注入 OSError → 验证 read_inbox 仍返回消息 → 再次 read_inbox 验证消息仍在（重复读取，无丢失）
    - test_read_inbox_atomic_cleanup_tempfile: 使用 unittest.mock.patch 对 os.replace 注入 OSError → 验证 os.unlink 在 except 块中被调用（临时文件被清理）
  </action>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && uv run pytest tests/test_teams_bus.py -v</automated>
  </verify>
  <acceptance_criteria>
    - bus.py 包含 `import os`, `import tempfile`, `import logging`
    - bus.py 包含 `logger = logging.getLogger(__name__)`
    - bus.py 的 read_inbox 方法包含 `os.replace` 调用
    - bus.py 的 read_inbox 方法包含 `logger.warning` 调用
    - test_teams_bus.py 包含 3 个以 `test_read_inbox_atomic` 开头的测试函数
    - 所有 8 个 bus 测试通过（5 既有 + 3 新增）
  </acceptance_criteria>
  <done>
    read_inbox 使用 rename swap 原子清零。失败时仅 warning 不丢失消息。3 个新增测试全部通过。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 修复 MCP _read_until_header_end 逐字节读取 (D-04, D-05)</name>
  <files>framework/agent_framework/tools/mcp/transport.py, framework/tests/test_mcp_transport.py</files>
  <read_first>
    - framework/agent_framework/tools/mcp/transport.py (当前 _read_until_header_end 实现，第 122-129 行)
    - framework/tests/test_mcp_transport.py (现有测试，追加新测试)
  </read_first>
  <behavior>
    - test_read_until_header_end_uses_readline: 构造包含 `Content-Length: 10\r\n\r\n` header 的 StreamReader，调用 _read_until_header_end，验证返回 `b"Content-Length: 10\r\n\r\n"` 且 readline 被调用（而非 read(1)）
    - test_read_until_header_end_multiple_headers: 构造包含多行 header（`Content-Length: 10\r\nCustom: value\r\n\r\n`）的 StreamReader，验证完整返回
    - test_read_until_header_end_eof: 构造空 StreamReader，验证抛出 EOFError
  </behavior>
  <action>
    重写 `_read_until_header_end` 方法（当前第 122-129 行），替换逐字节 read(1) 为 readline() 循环（per D-04）：

    1. buf = b""
    2. while True 循环：调用 `await self._process.stdout.readline()`（StreamReader.readline 自动缓冲）
    3. 若 line 为空（b""）→ 抛出 EOFError("MCP server 关闭了连接")
    4. 将 line 追加到 buf
    5. 检测 header 结束：若 buf 以 b"\r\n\r\n" 结尾则 break 返回 buf
    6. 注意：readline() 返回的行已包含行尾 \r\n，所以空行（header 结束标记）返回 b"\r\n"，两连续空行会使 buf 包含 b"\r\n\r\n"

    在 test_mcp_transport.py 末尾追加新测试。由于 _read_until_header_end 需要一个真实的 asyncio.StreamReader，构造方式为：创建 asyncio.StreamReader 对象，通过 feed_data 注入数据，然后 feed_eof。或者使用测试用 helper 函数创建 mock 的 StdioTransport 实例，将其 _process.stdout 替换为构造的 StreamReader。

    具体 helper 方案：在测试文件中创建 `_make_transport_with_reader(data: bytes)` 函数，使用 asyncio.StreamReader：
    - reader = asyncio.StreamReader()
    - reader.feed_data(data)
    - reader.feed_eof()
    - 创建 StdioTransport 实例，手动设置 _process.stdout = reader
    - 返回 transport

    新增 3 个测试（per D-05）：
    - test_read_until_header_end_single_header: 馈入 `b"Content-Length: 10\r\n\r\n"`，验证返回完整 header bytes
    - test_read_until_header_end_multi_header: 馈入 `b"Content-Length: 10\r\nCustom: value\r\n\r\n"`，验证返回包含所有 header 行
    - test_read_until_header_end_eof: 馈入空数据（仅 feed_eof），验证抛出 EOFError
  </action>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && uv run pytest tests/test_mcp_transport.py -v</automated>
  </verify>
  <acceptance_criteria>
    - transport.py 的 _read_until_header_end 方法不再包含 `.read(1)`
    - transport.py 的 _read_until_header_end 方法包含 `readline()` 调用
    - test_mcp_transport.py 包含 3 个以 `test_read_until_header_end` 开头的测试函数
    - 所有 10 个 MCP transport 测试通过（7 既有 + 3 新增）
  </acceptance_criteria>
  <done>
    MCP header 读取使用 readline() 替代逐字节 read(1)。3 个新增测试全部通过。
  </done>
</task>

<task type="auto">
  <name>Task 3: 产出 PERF-REVIEW.md (D-06, D-07)</name>
  <files>docs/reviews/PERF-REVIEW.md</files>
  <read_first>
    - docs/reviews/SECURITY-REVIEW.md (报告格式参考 — 每个 issue 包含描述/文件位置/严重性/修复状态)
    - .planning/codebase/CONCERNS.md (Performance Concerns 部分 — 所有 5 个性能问题的详情)
    - framework/agent_framework/memory/log_manager.py (Record 3 的源文件，确认同步 I/O 位置)
    - framework/agent_framework/tasks/manager.py (Record 4 的源文件，确认全量扫描位置)
    - framework/agent_framework/tools/context/compactor.py (Record 5 的源文件，确认 LLM 调用位置)
  </read_first>
  <action>
    在 docs/reviews/PERF-REVIEW.md 创建性能审查报告，沿用 SECURITY-REVIEW.md 的精简格式（per D-06）。

    报告结构（per D-07）：

    1. 文件头：审计日期、范围（framework/agent_framework/）、审计者（Phase 04）

    2. 第一区域「已修复」— 按严重性排列（HIGH → MEDIUM）：
       - PERF-01 (HIGH): MessageBus inbox 非原子读写 — 描述问题（read_inbox 先读后清零、崩溃丢消息）、文件位置（bus.py:33-48）、修复方案（rename swap）、修复状态 FIXED
       - PERF-02 (MEDIUM): MCP header 逐字节读取 — 描述问题（read(1) 系统调用开销大）、文件位置（transport.py:122-129）、修复方案（readline 缓冲读取）、修复状态 FIXED

    3. 第二区域「已记录」— 按严重性排列（HIGH → MEDIUM → LOW）：
       - PERF-03 (HIGH): 同步文件 I/O 阻塞 async 事件循环 — 描述 memory 子系统和 bus 使用同步 read_text/write_text、文件位置（log_manager.py:44-45, bus.py:26-30, result_truncator.py:34）、改进路径（asyncio.to_thread 或 aiofiles）、修复状态 Documented only
       - PERF-04 (MEDIUM): TaskManager 全量扫描 — 描述每次查询都 glob + 读取所有 JSON 文件、文件位置（manager.py:139-146,158-168）、改进路径（内存索引 + 状态同步到磁盘）、修复状态 Documented only
       - PERF-05 (LOW): Context compaction 额外 LLM 调用 — 描述每次压缩触发完整 LLM 调用、文件位置（compactor.py:126-156）、改进路径（缓存摘要 + 增量摘要）、修复状态 Documented only

    4. Summary 表格：总数、已修复、已记录

    每个 issue 使用 SECURITY-REVIEW.md 同样的格式：Description + File Location + (Impact 或 Improvement Path) + Fix Status。措辞参考 CONCERNS.md 中的原文描述，保持技术准确性。
  </action>
  <verify>
    <automated>test -f /Users/huhu/project/agent_framework/docs/reviews/PERF-REVIEW.md && grep -c 'PERF-0' /Users/huhu/project/agent_framework/docs/reviews/PERF-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/reviews/PERF-REVIEW.md 文件存在
    - 包含 5 个 PERF-0X 编号的问题（grep -c 'PERF-0' 返回 >= 5）
    - 包含 "已修复" 和 "已记录" 两个分区
    - PERF-01 和 PERF-02 标记为 "FIXED"
    - PERF-03、PERF-04、PERF-05 标记为 "Documented only"
    - 包含 Summary 表格
  </acceptance_criteria>
  <done>
    PERF-REVIEW.md 包含 2 个已修复项 + 3 个已记录项，格式与 SECURITY-REVIEW.md 一致。
  </done>
</task>

</tasks>

<verification>
1. 全部测试通过：`cd /Users/huhu/project/agent_framework/framework && uv run pytest tests/ -v`
2. PERF-REVIEW.md 存在且格式正确：`grep 'PERF-0' docs/reviews/PERF-REVIEW.md`
3. 原子清零已实现：`grep 'os.replace' framework/agent_framework/teams/bus.py`
4. readline 已替换逐字节：`grep 'readline' framework/agent_framework/tools/mcp/transport.py`
5. 无逐字节残留：`grep -v '^#' framework/agent_framework/tools/mcp/transport.py | grep -c 'read(1)'` 返回 0
</verification>

<success_criteria>
- bus.py 使用 rename swap 原子清零 inbox（D-01），失败时仅 warning（D-02）
- transport.py 使用 readline() 替代逐字节 read(1)（D-04）
- 每个修复有对应新增测试（D-03, D-05）
- PERF-REVIEW.md 沿用 SECURITY-REVIEW.md 格式（D-06），分"已修复/已记录"两大区域（D-07）
- 全量测试通过，无回归
</success_criteria>

<output>
Create `.planning/phases/04-perf-review/04-01-SUMMARY.md` when done
</output>
