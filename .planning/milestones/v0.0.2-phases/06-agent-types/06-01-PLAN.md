---
phase: 06-agent-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - framework/agent_framework/agents/base.py
  - framework/agent_framework/agents/agent_loop.py
  - framework/agent_framework/agents/__init__.py
  - framework/agent_framework/agents/sub_agent.py
  - framework/agent_framework/tasks/runner.py
  - framework/agent_framework/teams/manager.py
  - framework/tests/test_agent_base.py
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-03
  - AGENT-04
  - AGENT-05

must_haves:
  truths:
    - "AgentEvent dataclass 存在且包含 type/step/data 三个字段"
    - "Agent ABC 存在且定义了 run() 抽象方法"
    - "LoopEvent 继承 AgentEvent，AgentLoop 继承 Agent"
    - "sub_agent.py/runner.py/manager.py 的类型标注指向 Agent 而非 AgentLoop"
    - "687 现有测试全部通过"
  artifacts:
    - path: "framework/agent_framework/agents/base.py"
      provides: "Agent ABC + AgentEvent dataclass"
      exports: ["Agent", "AgentEvent"]
    - path: "framework/agent_framework/agents/agent_loop.py"
      provides: "LoopEvent(AgentEvent), AgentLoop(Agent)"
      contains: "class AgentLoop(Agent)"
    - path: "framework/agent_framework/agents/__init__.py"
      provides: "Agent, AgentEvent, LoopEvent, AgentLoop 导出"
    - path: "framework/agent_framework/agents/sub_agent.py"
      provides: "类型标注更新"
      contains: "from agent_framework.agents.base import Agent"
    - path: "framework/tests/test_agent_base.py"
      provides: "AGENT-01 和 AGENT-02 测试"
      exports: ["test_agent_event_fields", "test_agent_abc_cannot_instantiate"]
  key_links:
    - from: "framework/agent_framework/agents/agent_loop.py"
      to: "framework/agent_framework/agents/base.py"
      via: "import Agent, AgentEvent"
      pattern: "from agent_framework.agents.base import"
    - from: "framework/agent_framework/agents/sub_agent.py"
      to: "framework/agent_framework/agents/base.py"
      via: "import Agent"
      pattern: "from agent_framework.agents.base import Agent"
---

<objective>
创建 Agent ABC 抽象基类和 AgentEvent 统一事件模型，让 AgentLoop 实现该接口，并更新 sub_agent.py/runner.py/manager.py 的类型标注从 AgentLoop 改为 Agent。这是 Phase 6 的基础设施，后续 PlanAndSolve 和 Reflection Agent 都依赖此接口。

Purpose: 为框架引入多 Agent 类型的扩展点，面向接口编程而非面向具体实现
Output: Agent ABC + AgentEvent + 类型标注更新 + 687 测试零回归
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agent-types/06-CONTEXT.md
@.planning/phases/06-agent-types/06-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md

<interfaces>
<!-- 现有 LoopEvent 定义 — 继承后将变为 AgentEvent 的子类 -->
From framework/agent_framework/agents/agent_loop.py:
```python
@dataclass
class LoopEvent:
    type: str  # "step" | "tool_result" | "done" | "max_steps" | "error"
    step: int
    data: dict[str, Any] = field(default_factory=dict)
    plan: PlanSnapshot | None = None

class AgentLoop:
    async def run(
        self,
        user_message: str,
        plan: list[PlanItem] | None = None,
        *,
        resume: bool = False,
    ) -> AsyncGenerator[LoopEvent, None]:
```

<!-- sub_agent.py 当前 import — 需改为 import Agent -->
From framework/agent_framework/agents/sub_agent.py:
```python
from agent_framework.agents.agent_loop import AgentLoop
```

<!-- runner.py 当前 import — 需增加 import Agent -->
From framework/agent_framework/tasks/runner.py:
```python
from agent_framework.agents.agent_loop import AgentLoop
```
runner.py 内部 `_execute` 方法创建 AgentLoop 实例，类型标注从 `AgentLoop` 改为 `Agent`。

<!-- manager.py 当前 import — 需增加 import Agent -->
From framework/agent_framework/teams/manager.py:
```python
from agent_framework.agents.agent_loop import AgentLoop
```
manager.py 内部 `_loop` 方法创建 AgentLoop 实例，类型标注从 `AgentLoop` 改为 `Agent`。

<!-- 现有 base.py — 空文件，将被替换 -->
From framework/agent_framework/agents/base.py:
当前内容为 scaffold 注释（15行），将被替换为 Agent ABC + AgentEvent 定义。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 AgentEvent + Agent ABC 并重构 LoopEvent/AgentLoop 继承关系</name>
  <files>
    framework/agent_framework/agents/base.py,
    framework/agent_framework/agents/agent_loop.py,
    framework/agent_framework/agents/__init__.py
  </files>
  <read_first>
    framework/agent_framework/agents/base.py
    framework/agent_framework/agents/agent_loop.py
    framework/agent_framework/agents/__init__.py
    .planning/phases/06-agent-types/06-CONTEXT.md (D-01, D-02, D-12)
    .planning/codebase/CONVENTIONS.md (dataclass 优先, import 组织)
  </read_first>
  <action>
    1. 替换 framework/agent_framework/agents/base.py 的全部内容为：
       - 模块 docstring（中文，符合 CONVENTIONS.md 规范）
       - `from __future__ import annotations`
       - 从 abc 导入 ABC, abstractmethod
       - 从 dataclasses 导入 dataclass, field
       - 从 typing 导入 Any, AsyncGenerator
       - 定义 `AgentEvent` dataclass（per D-01）：
         - `type: str`（无默认值）
         - `step: int`（无默认值）
         - `data: dict[str, Any] = field(default_factory=dict)`
       - 定义 `Agent` ABC（per D-12）：
         - 仅定义一个 `@abstractmethod async def run(self, user_message: str) -> AsyncGenerator[AgentEvent, None]:` 方法
         - 方法体为 `yield  # pragma: no cover`
         - 不约束 `__init__` 签名

    2. 修改 framework/agent_framework/agents/agent_loop.py：
       - 添加 import: `from agent_framework.agents.base import Agent, AgentEvent`
       - 修改 LoopEvent 从 `@dataclass class LoopEvent:` 变为 `@dataclass class LoopEvent(AgentEvent):`
       - 由于 AgentEvent 的 type/step/data 字段已定义，LoopEvent 只保留 `plan: PlanSnapshot | None = None` 字段
       - 修改 AgentLoop 从 `class AgentLoop:` 变为 `class AgentLoop(Agent):`
       - 确保 run() 方法签名保持不变（已有 `user_message: str` 作为第一个参数，额外参数 `plan` 和 `resume` 是 Python 允许的子类扩展）
       - LoopEvent(type=..., step=..., data=..., plan=...) 的所有构造调用保持兼容，因为 LoopEvent 继承了 AgentEvent 的全部字段

    3. 更新 framework/agent_framework/agents/__init__.py：
       - 导出 Agent 和 AgentEvent（当前为空文件）
       - 使用显式 import + `__all__` 列表（符合 CONVENTIONS.md 中 barrel file 模式，参考 llm/__init__.py 风格）
  </action>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && python -c "from agent_framework.agents.base import Agent, AgentEvent; from agent_framework.agents.agent_loop import AgentLoop, LoopEvent; assert issubclass(LoopEvent, AgentEvent); assert issubclass(AgentLoop, Agent)"</automated>
  </verify>
  <acceptance_criteria>
    - framework/agent_framework/agents/base.py 包含 `class AgentEvent:` 和 `class Agent(ABC):`
    - Agent ABC 的 run 方法有 `@abstractmethod` 装饰器
    - LoopEvent 继承 AgentEvent，LoopEvent 字段仅 `plan: PlanSnapshot | None = None`
    - AgentLoop 继承 Agent
    - `from agent_framework.agents.base import Agent, AgentEvent` 成功
    - `from agent_framework.agents import Agent, AgentEvent` 成功
    - issubclass(LoopEvent, AgentEvent) 返回 True
    - issubclass(AgentLoop, Agent) 返回 True
  </acceptance_criteria>
  <done>AgentEvent dataclass 和 Agent ABC 定义在 base.py 中，LoopEvent 继承 AgentEvent，AgentLoop 继承 Agent，__init__.py 导出正确</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 编写 Agent ABC + AgentEvent 测试并更新类型标注</name>
  <files>
    framework/tests/test_agent_base.py,
    framework/agent_framework/agents/sub_agent.py,
    framework/agent_framework/tasks/runner.py,
    framework/agent_framework/teams/manager.py
  </files>
  <read_first>
    framework/agent_framework/agents/sub_agent.py
    framework/agent_framework/tasks/runner.py
    framework/agent_framework/teams/manager.py
    framework/tests/conftest.py
    framework/tests/test_agent_loop.py (前 50 行，了解测试 helper 模式)
    .planning/phases/06-agent-types/06-CONTEXT.md (D-03)
    .planning/codebase/TESTING.md
  </read_first>
  <behavior>
    - Test 1: AgentEvent 默认值 — `AgentEvent(type="step", step=1)` 的 data 默认为空 dict
    - Test 2: AgentEvent 字段赋值 — `AgentEvent(type="done", step=2, data={"text": "ok"})` 字段正确
    - Test 3: Agent ABC 不可实例化 — `Agent()` 抛出 TypeError
    - Test 4: Agent 子类可以实现 — 创建一个简单的 Agent 子类并实例化成功
    - Test 5: AgentLoop 是 Agent 的子类 — `issubclass(AgentLoop, Agent)` 为 True
    - Test 6: LoopEvent 是 AgentEvent 的子类 — `issubclass(LoopEvent, AgentEvent)` 为 True
  </behavior>
  <action>
    1. 创建 framework/tests/test_agent_base.py（per TESTING.md 规范）：
       - 模块 docstring: `"""Agent ABC + AgentEvent 测试。"""`
       - 导入: Agent, AgentEvent from agent_framework.agents.base; AgentLoop, LoopEvent from agent_framework.agents.agent_loop
       - 测试 class TestAgentEvent:
         - test_default_data: 验证 AgentEvent(type="step", step=1).data == {}
         - test_custom_data: 验证自定义 data dict 正确传递
         - test_all_event_types: 验证 type 字段可以接受 "step", "tool_result", "done", "max_steps", "error"
       - 测试 class TestAgentABC:
         - test_cannot_instantiate: 验证 Agent() 抛出 TypeError（因为是 abstract）
         - test_subclass_can_instantiate: 创建一个简单的 Agent 子类实现 run()，验证可以实例化
         - test_agent_loop_is_agent: 验证 issubclass(AgentLoop, Agent)
         - test_loop_event_is_agent_event: 验证 issubclass(LoopEvent, AgentEvent)

    2. 更新 framework/agent_framework/agents/sub_agent.py（per D-03）：
       - 添加 import: `from agent_framework.agents.base import Agent`
       - D-03 说明：经代码审查确认，sub_agent.py 中 `loop` 变量无显式类型标注（`loop = AgentLoop(...)` 是纯赋值，第 38 行），因此无需修改变量标注。D-03 的核心目的是让 `Agent` 类型在文件中可被导入引用，为后续 Phase 7 的 agent_factory 模式做准备
       - 保留现有 `from agent_framework.agents.agent_loop import AgentLoop` import（用于创建实例），同时添加 `from agent_framework.agents.base import Agent`（用于类型引用）

    3. 更新 framework/agent_framework/tasks/runner.py（per D-03）：
       - 添加 import: `from agent_framework.agents.base import Agent`
       - D-03 说明：经代码审查确认，runner.py 中 `_execute` 方法的 `loop = AgentLoop(...)` 无显式类型标注（第 55 行）。D-03 通过添加 Agent import 使该类型可用于后续类型标注（Phase 7 agent_factory）
       - 保留现有 `from agent_framework.agents.agent_loop import AgentLoop` import（用于创建实例）

    4. 更新 framework/agent_framework/teams/manager.py（per D-03）：
       - 添加 import: `from agent_framework.agents.base import Agent`
       - D-03 说明：经代码审查确认，manager.py 中 `_loop` 方法的 `loop = AgentLoop(...)` 无显式类型标注（第 74 行）。D-03 通过添加 Agent import 使该类型可用于后续类型标注（Phase 7 agent_factory）
       - 保留现有 `from agent_framework.agents.agent_loop import AgentLoop` import（用于创建实例）

    5. 运行完整测试套件验证零回归：
       `cd framework && pytest tests/ -x -q`
  </action>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && pytest tests/test_agent_base.py tests/test_agent_loop.py tests/test_sub_agent.py tests/test_task_runner.py tests/test_teams_manager.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - framework/tests/test_agent_base.py 存在且包含 TestAgentEvent 和 TestAgentABC 两个测试类
    - test_agent_base.py 中所有测试通过
    - sub_agent.py 包含 `from agent_framework.agents.base import Agent` import
    - runner.py 包含 `from agent_framework.agents.base import Agent` import
    - manager.py 包含 `from agent_framework.agents.base import Agent` import
    - `cd framework && pytest tests/ -x -q` 返回 687+ passed, 0 failed
  </acceptance_criteria>
  <done>test_agent_base.py 覆盖 AGENT-01/02，三个文件类型标注更新完成，687 测试全部通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 内部框架层 | ABC 定义和类型标注更新，不涉及外部输入 |
| 测试边界 | 测试使用 MockAdapter，不调用真实 LLM API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Agent ABC 继承 | accept | 纯内部框架层，无外部输入 |
| T-06-02 | Spoofing | 类型标注更新 | accept | 仅编译时类型检查，运行时无安全影响 |
| T-06-SC | Tampering | 无外部包安装 | accept | 此计划不安装任何外部包 |
</threat_model>

<verification>
1. AgentEvent 字段和默认值正确: `cd framework && pytest tests/test_agent_base.py -v`
2. Agent ABC 不可实例化: 同上
3. LoopEvent 继承 AgentEvent: `python -c "from agent_framework.agents.agent_loop import LoopEvent; from agent_framework.agents.base import AgentEvent; assert issubclass(LoopEvent, AgentEvent)"`
4. AgentLoop 继承 Agent: `python -c "from agent_framework.agents.agent_loop import AgentLoop; from agent_framework.agents.base import Agent; assert issubclass(AgentLoop, Agent)"`
5. 687 测试全部通过: `cd framework && pytest tests/ -x -q`
</verification>

<success_criteria>
1. AgentEvent dataclass 有 type(str)/step(int)/data(dict) 三个字段，data 默认空 dict
2. Agent ABC 仅定义 run() 抽象方法，不约束 __init__
3. LoopEvent 继承 AgentEvent，只新增 plan 字段
4. AgentLoop 继承 Agent，run() 签名不变
5. sub_agent.py/runner.py/manager.py 各含 `from agent_framework.agents.base import Agent`
6. test_agent_base.py 覆盖 AGENT-01 和 AGENT-02
7. 687 测试全部通过，零回归
</success_criteria>

<output>
Create `.planning/phases/06-agent-types/06-01-SUMMARY.md` when done
</output>
