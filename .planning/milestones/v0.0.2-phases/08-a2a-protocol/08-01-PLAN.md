---
phase: 08-a2a-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - framework/agent_framework/a2a/__init__.py
  - framework/agent_framework/a2a/models.py
  - framework/tests/test_a2a_models.py
autonomous: true
requirements: [A2A-01, A2A-02]

must_haves:
  truths:
    - "AgentCard 可从 .md frontmatter 文本创建，包含 name/description/url/version/capabilities"
    - "A2ATask 状态机有 pending/running/completed/failed/canceled 五种状态，终态属性可查"
    - "A2AMessage 可表示 role + text 的消息"
    - "load_agent_card() 可解析 .md frontmatter 文本并返回 AgentCard 实例"
  artifacts:
    - path: "framework/agent_framework/a2a/__init__.py"
      provides: "a2a 子包导出"
      exports: ["AgentCard", "A2ATask", "A2AMessage", "A2ATaskStatus", "load_agent_card"]
    - path: "framework/agent_framework/a2a/models.py"
      provides: "A2A 数据模型"
      contains: "class AgentCard"
    - path: "framework/tests/test_a2a_models.py"
      provides: "数据模型测试"
      min_lines: 60
  key_links:
    - from: "framework/agent_framework/a2a/models.py"
      to: "framework/agent_framework/memory/frontmatter.py"
      via: "parse_frontmatter() 调用"
      pattern: "parse_frontmatter"
    - from: "framework/agent_framework/a2a/__init__.py"
      to: "framework/agent_framework/a2a/models.py"
      via: "导入并重新导出"
      pattern: "from.*models.*import"
---

<objective>
A2A 数据模型 + AgentCard 加载

Purpose: 定义 A2A 协议的 Pydantic 数据模型（AgentCard, A2ATask, A2AMessage, A2ATaskStatus）和 AgentCard .md frontmatter 解析函数。这些是后续 Plan 02 (Server/Client) 和 Plan 03 (认证) 的基础契约。

Output: a2a 子包（models.py + __init__.py）+ 完整测试套件
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-a2a-protocol/08-CONTEXT.md
@.planning/phases/08-a2a-protocol/08-RESEARCH.md

<interfaces>
<!-- 执行器需要的已有接口 -->

From framework/agent_framework/memory/frontmatter.py:
```python
def parse_frontmatter(text: str) -> dict[str, str]:
    """解析 Markdown 文件的 YAML frontmatter，返回键值对。仅支持扁平键值对。"""
```

From framework/agent_framework/agents/base.py:
```python
@dataclass
class AgentEvent:
    type: str
    step: int
    data: dict[str, Any] = field(default_factory=dict)

class Agent(ABC):
    @abstractmethod
    async def run(self, user_message: str) -> AsyncGenerator[AgentEvent, None]:
        yield  # pragma: no cover
```

From framework/agent_framework/tools/types.py (Plan 02 参考，Plan 01 不直接使用):
```python
class ToolResult(BaseModel):
    content: str
    is_error: bool = False
    metadata: dict[str, Any] = {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 A2A 数据模型 + AgentCard 加载</name>
  <files>framework/agent_framework/a2a/__init__.py, framework/agent_framework/a2a/models.py, framework/tests/test_a2a_models.py</files>

  <read_first>
  - framework/agent_framework/memory/frontmatter.py (parse_frontmatter 函数签名和返回类型)
  - framework/agent_framework/tools/types.py (Pydantic BaseModel 风格参考)
  - framework/agent_framework/agents/base.py (AgentEvent.data 结构，了解 done 事件格式)
  </read_first>

  <behavior>
  - AgentCard 可通过字段构造：name, description, url, version, capabilities
  - A2ATaskStatus 枚举有 PENDING, RUNNING, COMPLETED, FAILED, CANCELED 五个值
  - A2ATaskStatus.COMPLETED/FAILED/CANCELED 的 is_terminal 属性返回 True，其他返回 False
  - A2ATask 包含 id, status, result, error, created_at, updated_at 字段，result 和 error 默认 None
  - A2AMessage 包含 role 和 text 字段
  - load_agent_card() 解析 .md frontmatter 文本，返回 AgentCard
  - load_agent_card() 缺少 name 时抛出 ValueError
  - load_agent_card() 缺少 url 时抛出 ValueError
  - load_agent_card() capabilities 逗号分隔字符串正确 split 为列表
  - load_agent_card() 空或无 capabilities 返回空列表
  </behavior>

  <action>
  Per D-03: 所有 A2A 数据模型使用 Pydantic BaseModel，与 ToolCall/ToolResult/ToolSpec 风格一致。
  Per D-07: capabilities 字段为 list[str]。
  Per D-08: AgentCard 配置使用 .md frontmatter 格式，复用 parse_frontmatter()。

  1. 创建 framework/agent_framework/a2a/ 目录和 __init__.py（导出 AgentCard, A2ATask, A2AMessage, A2ATaskStatus, load_agent_card）。
  2. 创建 models.py，定义：
     - A2ATaskStatus(str, Enum)：PENDING="pending", RUNNING="running", COMPLETED="completed", FAILED="failed", CANCELED="canceled"。添加 is_terminal 属性（COMPLETED/FAILED/CANCELED 返回 True）。
     - AgentCard(BaseModel)：name: str, description: str="", url: str, version: str="1.0", capabilities: list[str]=[]。
     - A2ATask(BaseModel)：id: str, status: A2ATaskStatus=PENDING, result: str|None=None, error: str|None=None, created_at: str, updated_at: str。
     - A2AMessage(BaseModel)：role: str, text: str。
     - load_agent_card(text: str, filename: str="<unknown>") -> AgentCard：调用 parse_frontmatter(text) 获取 meta dict，提取 name（必填，缺失 raise ValueError）、url（必填，缺失 raise ValueError）、description（默认空串）、version（默认 "1.0"）、capabilities（meta.get("capabilities","")，按逗号 split 后 strip，过滤空串）。
  3. 创建 framework/tests/test_a2a_models.py，覆盖所有行为用例。
  4. 先写测试（RED），再实现（GREEN）。运行 pytest tests/test_a2a_models.py -v -x 确认全部通过。
  </action>

  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && pytest tests/test_a2a_models.py -v -x</automated>
  </verify>
  <done>
    - AgentCard 从 Pydantic BaseModel 构造并通过 model_dump() 序列化
    - A2ATaskStatus 五种枚举值 + is_terminal 属性正确
    - A2ATask 包含所有字段且默认值正确
    - A2AMessage 包含 role + text
    - load_agent_card() 从 frontmatter 文本解析成功
    - 缺少 name/url 时抛出 ValueError
    - capabilities 逗号分隔正确解析
    - __init__.py 导出所有公开类型
  </done>

  <acceptance_criteria>
    - `cd framework && pytest tests/test_a2a_models.py -v -x` 全部 PASS
    - `grep -c "class AgentCard" framework/agent_framework/a2a/models.py` 输出 1
    - `grep -c "class A2ATask" framework/agent_framework/a2a/models.py` 输出 1
    - `grep -c "class A2ATaskStatus" framework/agent_framework/a2a/models.py` 输出 1
    - `grep -c "class A2AMessage" framework/agent_framework/a2a/models.py` 输出 1
    - `grep -c "def load_agent_card" framework/agent_framework/a2a/models.py` 输出 1
    - `grep -c "parse_frontmatter" framework/agent_framework/a2a/models.py` 输出 1
    - `grep "AgentCard" framework/agent_framework/a2a/__init__.py` 命中
    - `grep "load_agent_card" framework/agent_framework/a2a/__init__.py` 命中
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .md 文件 -> load_agent_card() | 外部配置文本解析，需验证必填字段 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | load_agent_card() 输入 | mitigate | Pydantic BaseModel 验证所有字段类型，必填字段缺失 raise ValueError |
| T-08-SC | Tampering | pip install | accept | 零新增依赖，所有包已存在 |

</threat_model>

<verification>
```bash
cd framework && pytest tests/test_a2a_models.py -v -x
```
</verification>

<success_criteria>
- A2ATaskStatus 五种状态枚举正确，is_terminal 属性工作
- AgentCard Pydantic 模型可构造、序列化、反序列化
- load_agent_card() 从 frontmatter 文本解析，必填字段验证
- A2ATask 和 A2AMessage 数据模型完整
- 所有测试通过
</success_criteria>

<output>
Create `.planning/phases/08-a2a-protocol/08-01-SUMMARY.md` when done
</output>
