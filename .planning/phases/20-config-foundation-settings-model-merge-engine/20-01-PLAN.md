---
phase: 20-config-foundation-settings-model-merge-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - framework/agent_framework/config/__init__.py
  - framework/agent_framework/config/settings.py
  - framework/agent_framework/config/merge.py
  - framework/tests/test_settings.py
  - framework/tests/test_merge.py
autonomous: true
requirements:
  - CFG-02
  - CFG-03
  - CFG-06

must_haves:
  truths:
    - "merge_settings() 正确处理三种策略：标量覆盖取最高优先级值、dict 递归浅合并、list[str] 并集去重保序"
    - "Settings 使用全默认值实例化成功（无 config 文件时 fresh-install safe）"
    - "ENV_VAR_MAP 定义了所有标量 Settings 字段的 APP_* 环境变量映射"
    - "apply_env_vars() 能将环境变量 dict 注入到合并后 dict 中覆盖标量字段"
    - "全部 1002 现有测试通过（零回归）"
  artifacts:
    - path: "framework/agent_framework/config/__init__.py"
      provides: "barrel 导出 config 模块公共 API"
      contains: "__all__"
    - path: "framework/agent_framework/config/settings.py"
      provides: "Settings + 4 个嵌套子模型 + ENV_VAR_MAP + apply_env_vars"
      contains: "class Settings"
    - path: "framework/agent_framework/config/merge.py"
      provides: "merge_settings() 递归类型感知合并函数"
      contains: "def merge_settings"
    - path: "framework/tests/test_settings.py"
      provides: "Settings 模型 + ENV_VAR_MAP 测试"
      min_lines: 60
    - path: "framework/tests/test_merge.py"
      provides: "merge_settings() 全策略测试"
      min_lines: 80
  key_links:
    - from: "framework/tests/test_settings.py"
      to: "framework/agent_framework/config/settings.py"
      via: "from agent_framework.config.settings import ..."
      pattern: "from agent_framework\\.config\\.settings import"
    - from: "framework/tests/test_merge.py"
      to: "framework/agent_framework/config/merge.py"
      via: "from agent_framework.config.merge import merge_settings"
      pattern: "from agent_framework\\.config\\.merge import merge_settings"
---

<objective>
创建 framework/agent_framework/config/ 模块的数据基础：Settings Pydantic 模型（CFG-03）、merge_settings() 类型感知合并函数（CFG-02）、环境变量映射机制（CFG-06）。

Purpose: 作为 v0.0.6 所有后续阶段（21-24）的共享依赖，提供统一的配置 schema 和合并工具。
Output: 3 个生产文件 + 2 个测试文件，全部为纯新增代码。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-config-foundation-settings-model-merge-engine/20-CONTEXT.md
@.planning/phases/20-config-foundation-settings-model-merge-engine/20-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@docs/plans/2026-06-11-config-path-mechanism-design.md
@backend/app/config/__init__.py

<interfaces>
<!-- 执行器不需要现有代码接口 — config/ 是叶依赖（零导入框架其他模块） -->
<!-- 以下是生产代码必须遵循的约定（来自 CONVENTIONS.md + ARCHITECTURE.md）： -->

约定约束：
1. 模块级中文 docstring："""全局运行时配置 — Settings Pydantic 模型。"""
2. from __future__ import annotations 必须在文件首行
3. import 顺序：future -> stdlib -> third-party -> framework-internal（本模块无 framework-internal 导入）
4. barrel __init__.py 带 __all__ 列表
5. config/ 是叶依赖 — 不导入 agent_framework 下任何其他模块
6. pydantic BaseModel（非 BaseSettings）— pydantic-settings 未安装

已有模式参考（backend/app/config/__init__.py）：
  - SecretStr 用于 api_key
  - env_prefix = "APP_" 命名约定
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Settings 模型 + ENV_VAR_MAP + barrel 导出</name>
  <files>
    framework/agent_framework/config/__init__.py,
    framework/agent_framework/config/settings.py,
    framework/tests/test_settings.py
  </files>
  <read_first>
    @.planning/phases/20-config-foundation-settings-model-merge-engine/20-CONTEXT.md
    @.planning/phases/20-config-foundation-settings-model-merge-engine/20-RESEARCH.md
    @.planning/codebase/CONVENTIONS.md
    @backend/app/config/__init__.py
  </read_first>
  <behavior>
    - Test: Settings() 无参数实例化成功，所有字段有默认值（model="claude-sonnet-4-20250514"、llm.provider="anthropic"、server.port=30002 等）
    - Test: Settings.model_validate({"model": "gpt-4"}) 创建部分覆盖的实例，未覆盖字段保持默认值
    - Test: Settings.model_validate({"llm": {"provider": "openai", "api_key": "sk-test"}}) 正确构造嵌套 LlmConfig，api_key 是 SecretStr
    - Test: Settings.model_dump(mode="json") 返回 api_key 为明文字符串而非 SecretStr 对象
    - Test: Settings.model_dump()（无 mode="json"）返回 api_key 为 SecretStr 对象
    - Test: ENV_VAR_MAP 包含所有标量字段映射（APP_MODEL、APP_LLM__PROVIDER、APP_LLM__API_KEY、APP_LLM__BASE_URL、APP_SERVER__HOST、APP_SERVER__PORT、APP_LOGGING__LEVEL）
    - Test: apply_env_vars({"model": "a"}, {"APP_MODEL": "b"}) 返回 {"model": "b"}（环境变量覆盖已有值）
    - Test: apply_env_vars({"model": "a"}, {"APP_LLM__PROVIDER": "openai"}) 返回 {"model": "a", "llm": {"provider": "openai"}}（嵌套路径注入）
    - Test: apply_env_vars({"model": "a"}, {"UNKNOWN_VAR": "x"}) 返回 {"model": "a"}（忽略未映射的环境变量）
  </behavior>
  <action>
    创建 config/ 包目录（framework/agent_framework/config/）。

    创建 settings.py：
    - from __future__ import annotations 首行
    - 导入 pydantic BaseModel 和 SecretStr（不导入 pydantic_settings）
    - 定义 4 个嵌套子模型（per D-01）：
      * LlmConfig: provider="anthropic", api_key=SecretStr(""), base_url=None
      * ServerConfig: host="0.0.0.0", port=30002, cors_origins=["http://localhost:30001"]
      * LoggingConfig: level="info"
      * PermissionsConfig: allow=[], deny=[], ask=[]
    - 定义 Settings(BaseModel) 顶层模型（per D-02, D-03）：
      * model: str = "claude-sonnet-4-20250514"
      * llm: LlmConfig = LlmConfig()
      * server: ServerConfig = ServerConfig()
      * logging: LoggingConfig = LoggingConfig()
      * permissions: PermissionsConfig = PermissionsConfig()
    - 模块级 docstring 用中文描述
    - 每个类用中文 one-line docstring
    - 定义 ENV_VAR_MAP: dict[str, str] 常量（per CFG-06），映射 APP_MODEL -> "model"、APP_LLM__PROVIDER -> "llm.provider" 等所有标量字段
    - 定义 apply_env_vars(merged: dict, env: dict[str, str]) -> dict 辅助函数：遍历 env 中在 ENV_VAR_MAP 有映射的键，将点分隔路径（如 "llm.provider"）注入到 merged dict 的对应嵌套位置，返回新 dict（不修改输入）。此函数供 Phase 21 ConfigLoader 调用。

    创建 __init__.py：
    - 从 settings.py 导出 Settings、LlmConfig、ServerConfig、LoggingConfig、PermissionsConfig、ENV_VAR_MAP、apply_env_vars
    - 从 merge.py 导出 merge_settings
    - 定义 __all__ 列表（per CONVENTIONS barrel 模式）
    - 模块级中文 docstring

    创建 test_settings.py：
    - 测试 Settings 全默认值实例化（CFG-03 fresh-install safe）
    - 测试 Settings.model_validate 从 dict 构造和部分覆盖
    - 测试嵌套子模型验证（LlmConfig.api_key 为 SecretStr）
    - 测试 model_dump(mode='json') 和 model_dump() 的 SecretStr 行为
    - 测试 ENV_VAR_MAP 包含所有标量字段映射
    - 测试 apply_env_vars 覆盖已有值
    - 测试 apply_env_vars 嵌套路径注入
    - 测试 apply_env_vars 忽略未映射变量
    - 测试 apply_env_vars 不修改输入 dict（不可变性）
    - 测试 config/ 不导入框架其他模块（叶依赖约束 INT-03）
  </action>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && .venv/bin/pytest tests/test_settings.py -v</automated>
  </verify>
  <done>
    - Settings() 无参数实例化成功，所有字段有正确默认值
    - Settings.model_validate 从 dict 构造正常，嵌套子模型类型正确
    - SecretStr 在 model_dump(mode='json') 时暴露明文，默认 dump 保留对象
    - ENV_VAR_MAP 包含 7 个映射：APP_MODEL、APP_LLM__PROVIDER、APP_LLM__API_KEY、APP_LLM__BASE_URL、APP_SERVER__HOST、APP_SERVER__PORT、APP_LOGGING__LEVEL
    - apply_env_vars 正确将环境变量注入到嵌套 dict 路径
    - config/__init__.py barrel 导出所有公共符号
    - 全部 test_settings.py 测试通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: merge_settings() 合并引擎 + 测试</name>
  <files>
    framework/agent_framework/config/merge.py,
    framework/tests/test_merge.py
  </files>
  <read_first>
    @.planning/phases/20-config-foundation-settings-model-merge-engine/20-CONTEXT.md
    @.planning/phases/20-config-foundation-settings-model-merge-engine/20-RESEARCH.md
    @.planning/codebase/CONVENTIONS.md
  </read_first>
  <behavior>
    - Test: merge_settings() 无参数返回空 dict
    - Test: merge_settings({}) 返回空 dict
    - Test: 标量覆盖 — merge_settings({"model": "a"}, {"model": "b"}) 返回 {"model": "b"}
    - Test: 标量覆盖 — merge_settings({"model": "a"}, {"model": "b"}, {"model": "c"}) 返回 {"model": "c"}（三个层级）
    - Test: dict 递归浅合并 — merge_settings({"llm": {"provider": "x", "timeout": 30}}, {"llm": {"provider": "y"}}) 返回 {"llm": {"provider": "y", "timeout": 30}}
    - Test: list[str] 并集去重保序（per D-04） — merge_settings({"allow": ["a", "b"]}, {"allow": ["b", "c"]}) 返回 {"allow": ["a", "b", "c"]}
    - Test: list 并集顺序 — 低优先级在前、高优先级在后（per D-04） — merge_settings({"allow": ["a"]}, {"allow": ["b"]}) 返回 ["a", "b"]
    - Test: 去重标准为严格字符串全等（per D-05） — merge_settings({"allow": ["Bash(git *)"]}, {"allow": ["bash(git *)"]}) 返回 ["Bash(git *)", "bash(git *)"]（大小写不同不去重）
    - Test: 嵌套 dict 中 list 并集 — merge_settings({"permissions": {"allow": ["a"]}}, {"permissions": {"allow": ["b"]}}) 返回 {"permissions": {"allow": ["a", "b"]}}
    - Test: 混合类型覆盖 — merge_settings({"key": [1, 2]}, {"key": "scalar"}) 返回 {"key": "scalar"}（类型不一致时高优先级覆盖）
    - Test: merge_settings 不修改输入 dict（不可变性）
    - Test: 完整集成场景 — global + project + local 三级合并得到正确结果
  </behavior>
  <action>
    创建 merge.py：
    - from __future__ import annotations 首行
    - 定义 merge_settings(*dicts: dict) -> dict 函数
    - 签名接受多个 dict 参数（从低到高优先级），返回合并后的新 dict
    - 合并策略（per CFG-02, D-04, D-05, D-06）：
      * 两个 dict -> 递归浅合并（递归进入嵌套 dict，确保嵌套的 list[str] 也做并集）— 这是关键：非递归会导致嵌套 permissions.allow 被整体替换
      * 两个 list -> 并集去重保序，低优先级元素在前，使用 set 去重但保留 list 顺序
      * 其他（标量或类型不一致）-> 高优先级直接覆盖
    - 空 dicts 列表时返回空 dict {}
    - 不修改任何输入 dict（所有操作在新 dict 上进行）
    - 仅处理 list[str] 数组（per D-06），不处理对象列表
    - 去重使用严格字符串全等（per D-05）
    - 中文模块 docstring，函数 docstring 描述三种策略和参数/返回值

    创建 test_merge.py：
    - 测试空输入和空 dict
    - 测试标量覆盖（两级和三级）
    - 测试 dict 递归浅合并（key 级别合并，不整体替换）
    - 测试 list[str] 并集去重保序
    - 测试严格字符串去重（大小写敏感）
    - 测试嵌套 dict 中 list 并集（permissions.allow 嵌套场景）
    - 测试混合类型覆盖（list -> scalar 类型不一致）
    - 测试不修改输入 dict（不可变性验证）
    - 测试完整三级合并集成场景（global + project + local）
    - 测试单层和多层嵌套深度
  </action>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && .venv/bin/pytest tests/test_merge.py -v</automated>
  </verify>
  <done>
    - merge_settings() 正确处理三种合并策略：标量覆盖、dict 递归浅合并、list[str] 并集去重保序
    - 嵌套 dict 中的 list 也正确并集（非整体替换）
    - 去重使用严格字符串全等，大小写敏感
    - 函数不修改任何输入 dict
    - 空 dict 输入返回空 dict
    - 类型不一致时高优先级覆盖
    - 全部 test_merge.py 测试通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config/ module -> pydantic | 内部依赖，已验证版本 2.13.4 |
| apply_env_vars(env) | 外部环境变量输入，仅处理预定义键 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Information Disclosure | LlmConfig.api_key | mitigate | 使用 SecretStr 包裹，model_dump() 不暴露明文 |
| T-20-02 | Spoofing | apply_env_vars | mitigate | 仅处理 ENV_VAR_MAP 预定义键，忽略未知键 |
| T-20-03 | Tampering | merge_settings input | accept | merge 不修改输入 dict，返回新对象；调用者负责输入来源 |
| T-20-SC | Tampering | pip installs | accept | 无新包安装，仅使用已有 pydantic 和 pytest |
</threat_model>

<verification>
1. framework/tests/test_settings.py 全部通过
2. framework/tests/test_merge.py 全部通过
3. 全部 1002 现有测试通过（.venv/bin/pytest tests/ -v）
4. config/ 模块不导入 agent_framework 其他模块（grep 验证叶依赖约束）
</verification>

<success_criteria>
1. merge_settings() 三种策略正确：标量覆盖、dict 递归浅合并、list[str] 并集去重保序
2. Settings() 全默认值实例化成功（无 config 文件安全）
3. ENV_VAR_MAP 定义所有标量字段映射，apply_env_vars() 正确注入嵌套路径
4. config/ 模块是叶依赖（不导入框架其他模块）
5. 全部 1002 现有测试通过（零回归）
</success_criteria>

<output>
Create `.planning/phases/20-config-foundation-settings-model-merge-engine/20-01-SUMMARY.md` when done
</output>
