---
phase: 02-security
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - framework/agent_framework/llm/providers/openai_provider.py
  - framework/agent_framework/llm/providers/anthropic_provider.py
  - framework/agent_framework/llm/providers/deepseek_provider.py
  - framework/tests/test_providers.py
  - docs/reviews/SECURITY-REVIEW.md
autonomous: true
requirements: [R1]

must_haves:
  truths:
    - "An external observer calling repr() or str() on any provider's _api_key sees only masked output, never the actual secret"
    - "HTTP requests to LLM APIs still authenticate successfully because the real key is used in headers"
    - "The security audit report lists all 6 known issues with correct severity ratings and fix statuses"
    - "Developers can read the audit report to understand what was fixed and what remains"
    - "All existing tests pass with zero regressions"
  artifacts:
    - path: "framework/agent_framework/llm/providers/openai_provider.py"
      provides: "OpenAIProvider with SecretStr _api_key"
      contains: "SecretStr"
    - path: "framework/agent_framework/llm/providers/anthropic_provider.py"
      provides: "AnthropicProvider with SecretStr _api_key"
      contains: "SecretStr"
    - path: "framework/agent_framework/llm/providers/deepseek_provider.py"
      provides: "DeepSeekProvider with SecretStr _api_key"
      contains: "SecretStr"
    - path: "docs/reviews/SECURITY-REVIEW.md"
      provides: "Structured security audit report"
      contains: "CRITICAL"
  key_links:
    - from: "framework/agent_framework/llm/providers/*.py"
      to: "httpx.AsyncClient headers"
      via: "self._api_key.get_secret_value()"
      pattern: "get_secret_value\\(\\)"
    - from: "framework/tests/test_providers.py"
      to: "provider._api_key"
      via: "_make_provider helper wraps SecretStr"
      pattern: "SecretStr\\("
---

<objective>
Migrate API key storage to SecretStr in all 3 providers (SEC-03) and produce the structured SECURITY-REVIEW.md covering all 6 security issues.

Purpose: API keys stored as plain strings can leak through repr(), logging, serialization, or debugger inspection. SecretStr auto-redacts in all these channels. The SECURITY-REVIEW.md is the required deliverable for R1.

Output: 3 provider files with SecretStr-wrapped _api_key, updated test helper, and docs/reviews/SECURITY-REVIEW.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-security/02-CONTEXT.md
@.planning/phases/02-security/02-RESEARCH.md
@.planning/phases/02-security/02-01-SUMMARY.md

<interfaces>
<!-- Key types and patterns from the providers -->

From framework/agent_framework/llm/providers/openai_provider.py __init__ (line 103-132):
```python
def __init__(self, *, api_key: str | None = None, base_url: str = ..., default_model: str = ..., timeout: float = 120.0) -> None:
    self._api_key = api_key or os.environ.get("OPENAI_API_KEY", "")
    # ... validation ...
    self._client = httpx.AsyncClient(
        headers={"Authorization": f"Bearer {self._api_key}", ...},
    )
```

From framework/agent_framework/llm/providers/anthropic_provider.py __init__ (line 251-281):
```python
def __init__(self, *, api_key: str | None = None, ...) -> None:
    self._api_key = api_key or os.environ.get("ANTHROPIC_API_KEY", "")
    # ... validation ...
    self._client = httpx.AsyncClient(
        headers={"x-api-key": self._api_key, ...},
    )
```

From framework/agent_framework/llm/providers/deepseek_provider.py __init__ (line 141-170):
```python
def __init__(self, *, api_key: str | None = None, ...) -> None:
    self._api_key = api_key or os.environ.get("DEEPSEEK_API_KEY", "")
    # ... validation ...
    self._client = httpx.AsyncClient(
        headers={"Authorization": f"Bearer {self._api_key}", ...},
    )
```

From framework/tests/test_providers.py _make_provider helper (line 189-197):
```python
def _make_provider(cls, api_key: str = "test-key"):
    with patch.object(cls, "__init__", lambda self, **kw: None):
        provider = object.__new__(cls)
    provider._api_key = api_key
    provider._base_url = "https://mock.test"
    provider._default_model = "mock-model"
    provider._client = AsyncMock()
    return provider
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wrap _api_key with SecretStr in all 3 providers (SEC-03, per D-07/D-08)</name>
  <files>framework/agent_framework/llm/providers/openai_provider.py, framework/agent_framework/llm/providers/anthropic_provider.py, framework/agent_framework/llm/providers/deepseek_provider.py, framework/tests/test_providers.py</files>
  <read_first>
    - framework/agent_framework/llm/providers/openai_provider.py (full file, focus on __init__)
    - framework/agent_framework/llm/providers/anthropic_provider.py (full file, focus on __init__)
    - framework/agent_framework/llm/providers/deepseek_provider.py (full file, focus on __init__)
    - framework/tests/test_providers.py (full file, focus on _make_provider helper)
  </read_first>
  <behavior>
    - Test: For each provider, str(instance._api_key) returns "**********" (not the actual key)
    - Test: For each provider, instance._api_key.get_secret_value() returns the actual key string
    - Test: Existing provider tests all pass after migration
  </behavior>
  <action>
RED phase: Add test_api_key_is_secret_str and test_api_key_repr_masks to test_providers.py. These tests:
- test_api_key_is_secret_str: Create each provider via _make_provider, assert isinstance(provider._api_key, SecretStr)
- test_api_key_repr_masks: Create each provider via _make_provider, assert str(provider._api_key) == "**********"

GREEN phase -- make these changes in order:

1. Update _make_provider helper in test_providers.py: change `provider._api_key = api_key` to `provider._api_key = SecretStr(api_key)`. Add `from pydantic import SecretStr` import.

2. Update openai_provider.py __init__:
   - Add `from pydantic import SecretStr` to imports
   - Change `self._api_key = api_key or os.environ.get(...)` to: `raw_key = api_key or os.environ.get("OPENAI_API_KEY", "")` then validate raw_key, then `self._api_key = SecretStr(raw_key)`
   - Change header from `f"Bearer {self._api_key}"` to `f"Bearer {self._api_key.get_secret_value()}"`

3. Update anthropic_provider.py __init__:
   - Add `from pydantic import SecretStr` to imports
   - Same pattern: store raw_key, validate, wrap with SecretStr
   - Change header from `"x-api-key": self._api_key` to `"x-api-key": self._api_key.get_secret_value()`

4. Update deepseek_provider.py __init__:
   - Add `from pydantic import SecretStr` to imports
   - Same pattern as openai_provider.py
   - Change header from `f"Bearer {self._api_key}"` to `f"Bearer {self._api_key.get_secret_value()}"`

Per D-07: all 3 providers use pydantic.SecretStr.
Per D-08: __repr__/__str__ auto-redact (SecretStr does this by default), key is still usable via get_secret_value().

CRITICAL PITFALL: str(SecretStr) and f"{SecretStr}" both produce "**********". The HTTP header construction MUST use .get_secret_value(). If this is wrong, provider tests pass but real API calls fail with 401.
  </action>
  <acceptance_criteria>
    - All 3 provider files contain `from pydantic import SecretStr`
    - All 3 provider __init__ methods assign `self._api_key = SecretStr(raw_key)` (not plain string)
    - All 3 provider files use `self._api_key.get_secret_value()` in httpx header construction
    - No provider file uses bare `self._api_key` in f-strings or string concatenation for header values
    - _make_provider in test_providers.py wraps with `SecretStr(api_key)`
    - test_api_key_is_secret_str and test_api_key_repr_masks tests pass
    - cd framework && pytest tests/test_providers.py -v passes all tests
    - cd framework && pytest tests/ -v passes full suite with zero regressions
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && pytest tests/test_providers.py -v</automated>
  </verify>
  <done>All 3 providers store _api_key as SecretStr; repr/str auto-redact; HTTP headers use get_secret_value(); all existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Produce SECURITY-REVIEW.md covering all 6 issues (SEC-04/05/06/07, per D-09/D-10/D-11)</name>
  <files>docs/reviews/SECURITY-REVIEW.md</files>
  <read_first>
    - framework/agent_framework/hooks/manager.py (lines 115-140 for hook command execution)
    - framework/agent_framework/tools/router.py (lines 65-76 for ASK decision handling)
    - framework/agent_framework/teams/bus.py (lines 1-48 for predictable paths)
    - framework/agent_framework/safety/hitl.py (lines 40-58 for HITL gap)
    - .planning/codebase/CONCERNS.md (Security Considerations section)
  </read_first>
  <action>
Create docs/reviews/SECURITY-REVIEW.md. This file is the structured security audit report required by R1.

Per D-10: Organize by severity level (CRITICAL, HIGH, MEDIUM, LOW) as top-level sections.
Per D-11: Each issue entry contains: description, file location, severity, and fix status.

Structure the document as follows:

Header with audit date, scope (framework/agent_framework/), and auditor.

## CRITICAL section:
- SEC-01 File Path Sandbox: description of the path traversal vulnerability in read_file/write_file, file location (file_tools.py), impact (arbitrary file read/write), fix status: FIXED in this phase. Brief description of fix (safe_path() integration).

## HIGH section:
- SEC-02 MCP Environment Variable Injection: description of env var override risk via McpServerConfig, file location (config.py, transport.py:57), impact (sensitive env var theft), fix status: FIXED in this phase. Brief description of fix (field_validator blacklist).

## MEDIUM section:
- SEC-03 API Key Plaintext Storage: description of _api_key stored as plain string in 3 providers, file locations (openai_provider.py:111, anthropic_provider.py:259, deepseek_provider.py:149), impact (key leak via repr/logging/serialization), fix status: FIXED in this phase (SecretStr wrapping).
- SEC-04 Hook Command Execution: description of bash -c arbitrary shell execution in HookManager._execute_command, file location (hooks/manager.py:120-122), current mitigation (trusted flag gates execution), fix status: Documented only. The trusted flag provides defense -- untrusted workspaces return empty HookResult. Recommend documenting trust model in user-facing docs.
- SEC-05 Permission ASK Not Wired to HITL: description of ASK decision returning error instead of user prompt, file location (tools/router.py:72-76), impact (tools requiring approval are silently rejected), fix status: Documented only (per D-09). Improvement path: wire ToolRouter to HITLManager for ASK decisions, replacing the error return with an actual user confirmation flow.

## LOW section:
- SEC-06 MessageBus Predictable File Paths: description of inbox files as plain JSONL with no integrity protection, file location (teams/bus.py:24-30), impact (any process with filesystem access can read/modify/inject messages), fix status: Documented only. Recommend restricting team directory permissions and considering message signing for tamper detection.

Add a Summary section at the end: total issues found (6), fixed (3), documented-only (3), with the assessment that the framework is now hardened against the top-severity attack vectors.
  </action>
  <acceptance_criteria>
    - docs/reviews/SECURITY-REVIEW.md exists and is non-empty
    - Contains section "## CRITICAL" with SEC-01 entry
    - Contains section "## HIGH" with SEC-02 entry
    - Contains section "## MEDIUM" with SEC-03, SEC-04, SEC-05 entries
    - Contains section "## LOW" with SEC-06 entry
    - Each issue entry contains: description, file location, severity, fix status
    - SEC-01 and SEC-02 marked as FIXED
    - SEC-03 marked as FIXED
    - SEC-04 and SEC-05 marked as documented only
    - SEC-06 marked as documented only
    - Summary section counts: 6 total, 3 fixed, 3 documented
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework && FILE=docs/reviews/SECURITY-REVIEW.md && test -f "$FILE" && grep -q "## CRITICAL" "$FILE" && grep -q "## HIGH" "$FILE" && grep -q "## MEDIUM" "$FILE" && grep -q "## LOW" "$FILE" && grep -q "SEC-01" "$FILE" && grep -q "SEC-02" "$FILE" && grep -q "SEC-03" "$FILE" && grep -q "SEC-04" "$FILE" && grep -q "SEC-05" "$FILE" && grep -q "SEC-06" "$FILE" && grep -q "FIXED" "$FILE" && grep -q "Documented" "$FILE" && echo "PASS: all 6 SEC entries, 4 severity sections, fix statuses present"</automated>
  </verify>
  <done>SECURITY-REVIEW.md exists in docs/reviews/ with all 6 security issues documented by severity level, fix statuses accurate, summary counts correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider config -> httpx client | API key string crosses from config into HTTP headers |
| Provider repr/logging | Internal state may be exposed through Python repr or logging |
| Review document -> future readers | Security report may be consumed by developers for prioritization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | provider _api_key via repr/logging | mitigate | SecretStr wrapping auto-redacts in str(), repr(), JSON serialization |
| T-02-05 | Elevation of Privilege | HookManager _execute_command | accept | trusted flag gates execution; documented in SECURITY-REVIEW.md; not fixable without breaking hook functionality |
| T-02-06 | Elevation of Privilege | ToolRouter ASK decision path | accept | Gap documented in SECURITY-REVIEW.md with improvement path; requires HITL wiring which is out of scope |
| T-02-07 | Tampering | MessageBus inbox files | accept | Low severity; relies on filesystem permissions; documented with recommendation |
| T-02-SC | Tampering | No new packages installed | accept | Zero new packages; all changes use existing pydantic SecretStr |
</threat_model>

<verification>
1. cd framework && pytest tests/test_providers.py -v -- all tests pass including new SecretStr tests
2. cd framework && pytest tests/ -v -- full suite passes with zero regressions
3. test -f docs/reviews/SECURITY-REVIEW.md -- file exists
4. grep -c "SEC-0" docs/reviews/SECURITY-REVIEW.md -- at least 6 issue entries
5. grep "SecretStr" framework/agent_framework/llm/providers/openai_provider.py framework/agent_framework/llm/providers/anthropic_provider.py framework/agent_framework/llm/providers/deepseek_provider.py -- confirmed in all 3
6. grep "get_secret_value" framework/agent_framework/llm/providers/openai_provider.py framework/agent_framework/llm/providers/anthropic_provider.py framework/agent_framework/llm/providers/deepseek_provider.py -- confirmed in all 3
</verification>

<success_criteria>
- All 3 providers store _api_key as pydantic.SecretStr
- str(provider._api_key) returns "**********" for all providers
- provider._api_key.get_secret_value() returns actual key for all providers
- HTTP headers in all providers use get_secret_value()
- All existing provider tests pass
- SECURITY-REVIEW.md exists with all 6 issues organized by severity
- Fix statuses accurately reflect what was done in this phase
- Full test suite passes with zero regressions
</success_criteria>

<output>
Create `.planning/phases/02-security/02-02-SUMMARY.md` when done
</output>
