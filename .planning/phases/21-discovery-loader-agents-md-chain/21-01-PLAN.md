---
phase: 21-discovery-loader-agents-md-chain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - framework/agent_framework/config/loader.py
  - framework/agent_framework/config/__init__.py
  - framework/tests/test_loader.py
autonomous: true
requirements: [CFG-01, CFG-04, CFG-05]

must_haves:
  truths:
    - "ConfigLoader.load_settings() 返回合并后的 Settings 实例，四级覆盖链 env > local > project > global 生效"
    - "ConfigLoader.discover(module_name) 返回按优先级从低到高排列的已存在目录路径列表"
    - "discover() 支持 8 种模块类型，未知类型 raise ValueError"
    - "settings.json JSON 格式错误时 raise ValueError 提示具体文件路径"
    - "目录不存在时静默跳过，返回空列表或空 dict"
  artifacts:
    - path: "framework/agent_framework/config/loader.py"
      provides: "ConfigLoader 类 — load_settings, discover, load_profile (占位)"
      exports: ["ConfigLoader"]
    - path: "framework/tests/test_loader.py"
      provides: "ConfigLoader + discover 测试"
  key_links:
    - from: "framework/agent_framework/config/loader.py"
      to: "framework/agent_framework/config/merge.py"
      via: "from agent_framework.config.merge import merge_settings"
      pattern: "merge_settings\\("
    - from: "framework/agent_framework/config/loader.py"
      to: "framework/agent_framework/config/settings.py"
      via: "from agent_framework.config.settings import Settings, apply_env_vars"
      pattern: "Settings\\.model_validate\\("
    - from: "framework/agent_framework/config/__init__.py"
      to: "framework/agent_framework/config/loader.py"
      via: "barrel re-export"
      pattern: "from agent_framework\\.config\\.loader import ConfigLoader"
---

<objective>
创建 ConfigLoader 入口类，实现四级覆盖链加载 settings.json 和模块目录路径发现。

Purpose: ConfigLoader 是 v0.0.6 所有后续阶段的统一配置入口。Phase 22-24 的适配器和集成代码都依赖 ConfigLoader.load_settings() 和 ConfigLoader.discover()。
Output: loader.py (ConfigLoader 类) + test_loader.py (测试) + __init__.py 更新
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-config-foundation-settings-model-merge-engine/20-01-SUMMARY.md
@.planning/phases/21-discovery-loader-agents-md-chain/21-CONTEXT.md
@.planning/phases/21-discovery-loader-agents-md-chain/21-RESEARCH.md

<interfaces>
<!-- Phase 20 已实现的接口，ConfigLoader 直接消费 -->

From framework/agent_framework/config/merge.py:
```python
def merge_settings(*dicts: dict) -> dict:
    """合并多个配置字典，从低到高优先级。"""
```

From framework/agent_framework/config/settings.py:
```python
class Settings(BaseModel):
    model: str = "claude-sonnet-4-20250514"
    llm: LlmConfig = LlmConfig()
    server: ServerConfig = ServerConfig()
    logging: LoggingConfig = LoggingConfig()
    permissions: PermissionsConfig = PermissionsConfig()

def apply_env_vars(merged: dict[str, Any], env: dict[str, str]) -> dict[str, Any]:
    """将环境变量注入到合并后 dict 中的标量字段。"""
```

From framework/agent_framework/config/__init__.py:
```python
__all__ = [
    "ENV_VAR_MAP", "LlmConfig", "LoggingConfig", "PermissionsConfig",
    "ServerConfig", "Settings", "apply_env_vars", "merge_settings",
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConfigLoader + discover + load_settings 测试与实现</name>
  <files>
    framework/tests/test_loader.py,
    framework/agent_framework/config/loader.py,
    framework/agent_framework/config/__init__.py
  </files>
  <read_first>
    framework/agent_framework/config/settings.py
    framework/agent_framework/config/merge.py
    framework/agent_framework/config/__init__.py
    framework/tests/test_settings.py
    framework/tests/test_merge.py
  </read_first>
  <behavior>
    - Test 1: ConfigLoader 默认构造 — global_dir=Path.home(), project_dir=Path.cwd()，内部 _global_dir 和 _project_dir 自动拼接 /.agent-framework/
    - Test 2: ConfigLoader 自定义路径 — 传入 tmp_path 参数，_global_dir=tmp_path_global / ".agent-framework", _project_dir=tmp_path_project / ".agent-framework"
    - Test 3: load_settings() 无配置文件 — 返回全默认 Settings 实例（model="claude-sonnet-4-20250514"）
    - Test 4: load_settings() global settings.json — global settings 存在时，对应字段覆盖默认值
    - Test 5: load_settings() 四级覆盖链 — env > local > project > global，验证标量取最高优先级、dict 浅合并、list 并集
    - Test 6: load_settings() 环境变量覆盖 — 设置 APP_MODEL 环境变量，最终 Settings.model 等于环境变量值
    - Test 7: load_settings() JSON 格式错误 — 文件存在但内容非法 JSON 时 raise ValueError，异常信息包含文件路径
    - Test 8: discover("skills") — 两个路径都存在时返回 [global_skills, project_skills]（低到高优先级）
    - Test 9: discover("skills") 仅 global 存在 — 返回 [global_skills]
    - Test 10: discover("skills") 两个都不存在 — 返回空列表 []
    - Test 11: discover("unknown_type") — raise ValueError，异常信息包含 "未知模块类型: unknown_type"
    - Test 12: discover() 全部 8 种模块类型 — 验证 MODULE_DIRS 包含 skills/agents/commands/hooks/rules/profiles/memory/mcp
    - Test 13: 叶依赖验证 — loader.py 不导入 agent_framework 下除 config 外的任何模块
  </behavior>
  <action>
    TDD RED-GREEN 流程实现。

    创建 framework/tests/test_loader.py，按 <behavior> 列表写测试（先全部 RED）。

    创建 framework/agent_framework/config/loader.py，实现：

    1. MODULE_DIRS 常量 (per D-12): dict[str, str] 映射 8 种模块类型名到子目录名 — skills->"skills", agents->"agents", commands->"commands", hooks->"hooks", rules->"rules", profiles->"profiles", memory->"memory", mcp->"mcp"。每个 key-value 相同。

    2. ConfigLoader 类 (per D-04):
       - __init__(self, global_dir: Path = Path.home(), project_dir: Path = Path.cwd()) — 内部属性 _global_dir = global_dir / ".agent-framework", _project_dir = project_dir / ".agent-framework"
       - _read_json(self, path: Path) -> dict — 文件不存在返回 {}，存在则 json.loads() 读取。JSONDecodeError 时 raise ValueError(f"配置文件格式错误: {path}") (Claude's Discretion — 损坏配置应显式报错)
       - load_settings(self) -> Settings (per D-01):
         a. 读取 global/settings.json, project/settings.json, project/settings.local.json (per D-03)
         b. merge_settings(global_dict, project_dict, local_dict) 合并
         c. apply_env_vars(merged, dict(os.environ)) 注入环境变量
         d. Settings.model_validate(final) 创建实例并返回
         无缓存，每次调用重新加载 (per D-01)
       - discover(self, module_name: str) -> list[Path] (per D-02, D-13):
         a. 从 MODULE_DIRS 获取子目录名，未知名称 raise ValueError(f"未知模块类型: {module_name}")
         b. 检查 _global_dir / sub_dir 和 _project_dir / sub_dir 是否为目录 (is_dir())
         c. 返回存在的路径列表，按 [global, project] 顺序（低到高优先级）
         d. 目录不存在时静默跳过 (per D-13)

    3. 更新 framework/agent_framework/config/__init__.py — 添加 from agent_framework.config.loader import ConfigLoader，添加到 __all__ 列表

    叶依赖约束 (per STATE.md + RESEARCH.md anti-patterns): loader.py 仅导入 json, os, pathlib (stdlib) + from agent_framework.config.merge import merge_settings + from agent_framework.config.settings import Settings, apply_env_vars。不导入 agent_framework 其他模块。

    测试用 tmp_path 构建 mock 目录结构 (per D-14)，通过 ConfigLoader 构造函数传入 tmp_path 参数。不使用 monkeypatch.chdir 或 Path.cwd() mock。
  </action>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && python -m pytest tests/test_loader.py -v && python -m pytest tests/ -v --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ConfigLoader.load_settings() 通过四级覆盖链返回正确合并的 Settings 实例
    - ConfigLoader.discover() 返回有序路径列表，8 种模块类型全覆盖
    - 未知模块类型 raise ValueError
    - JSON 格式错误 raise ValueError
    - 所有 1040 现有测试通过，零回归
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> ConfigLoader | settings.json 文件内容为外部输入，JSON 解析可能遇到格式错误或恶意内容 |
| env -> ConfigLoader | os.environ 为外部输入，但 apply_env_vars() 已在 Phase 20 实现类型安全注入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | _read_json() JSON 解析 | mitigate | json.loads() 不执行代码（非 eval）；JSONDecodeError 转为 ValueError 阻止静默消费损坏配置 |
| T-21-02 | Information Disclosure | settings.local.json | accept | 框架层不处理 .gitignore，由用户/项目确保 local 文件被忽略；含敏感信息但仅在本地文件系统 |
| T-21-03 | Tampering | discover() 路径遍历 | mitigate | discover() 只在 _global_dir 和 _project_dir 的固定子目录内操作，module_name 通过 MODULE_DIRS 白名单验证 |
| T-21-SC | Tampering | pip install | accept | 本 phase 零新依赖，无安装操作 |
</threat_model>

<verification>
1. `cd framework && python -m pytest tests/test_loader.py -v` — 所有新增测试通过
2. `cd framework && python -m pytest tests/ -v` — 1040 测试全部通过，零回归
3. `cd framework && python -c "from agent_framework.config import ConfigLoader; print('import OK')"` — barrel 导出正常
4. `cd framework && grep -r "from agent_framework\." agent_framework/config/loader.py | grep -v "from agent_framework\.config\."` — 叶依赖验证（无输出=通过）
</verification>

<success_criteria>
- ConfigLoader.load_settings() 四级覆盖链 (global -> project -> local -> env) 合并逻辑正确，Settings 实例字段值符合优先级
- ConfigLoader.discover("skills") 等返回有序路径列表，目录不存在时返回空列表
- discover() 对未知模块名 raise ValueError
- load_settings() 对 JSON 格式错误 raise ValueError
- config/ 模块保持叶依赖 — loader.py 不导入 agent_framework 非config 模块
- 1040 现有测试零回归
</success_criteria>

<output>
Create `.planning/phases/21-discovery-loader-agents-md-chain/21-01-SUMMARY.md` when done
</output>
