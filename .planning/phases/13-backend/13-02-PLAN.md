---
phase: 13-backend
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - docs/reviews/REVIEW-BACKEND.md
autonomous: false
requirements:
  - BKND-05

must_haves:
  truths:
    - "REVIEW-BACKEND.md 覆盖所有审查维度（死代码/逻辑/设计/安全）的完整结果"
    - "所有 CRITICAL 和 HIGH issue 有明确修复建议"
    - "报告包含汇总章节（issue 统计、优先级分布）"
    - "报告包含跨层问题章节，引用 REVIEW-FRAMEWORK.md 的具体 issue ID"
    - "issue ID 命名规范统一（BKND-DEAD-/BKND-LOGIC-/BKND-ARCH-/BKND-SEC-）"
  artifacts:
    - path: "docs/reviews/REVIEW-BACKEND.md"
      provides: "完整审查报告，含汇总章节和跨层参照"
      min_lines: 150
  key_links:
    - from: "REVIEW-BACKEND.md 跨层问题章节"
      to: "docs/reviews/REVIEW-FRAMEWORK.md"
      via: "FRMW-* issue ID 引用"
      pattern: "FRMW-"
    - from: "Plan 01 的文件章节"
      to: "汇总章节"
      via: "issue 统计聚合"
      pattern: "## 审查汇总"
---

<objective>
Wave 2: 跨层交叉参照 + 报告汇总 + 质量检查

Purpose: 完成审查报告的最后三步：跨层交叉参照（标注 Backend 发现中与 Framework 有关的问题）、汇总各模块审查结果、生成统计摘要、确保报告质量。Per D-08 在报告末尾设"跨层问题"章节，按主题归类并引用 REVIEW-FRAMEWORK.md 的 FRMW-* issue ID。这是 BKND-05 的最终交付物。

Output: docs/reviews/REVIEW-BACKEND.md 完整审查报告（含跨层参照 + 汇总章节）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-backend/13-CONTEXT.md
@.planning/phases/12-framework/12-RESEARCH.md
@.planning/phases/13-backend/13-01-SUMMARY.md
@docs/reviews/REVIEW-FRAMEWORK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 跨层交叉参照 + 报告汇总 + 质量检查</name>
  <files>docs/reviews/REVIEW-BACKEND.md</files>
  <read_first>
    - docs/reviews/REVIEW-BACKEND.md (Task 1 产出的完整报告，需审查所有章节)
    - docs/reviews/REVIEW-FRAMEWORK.md (Phase 12 框架层审查报告 — 跨层交叉参照的对照基准，重点读取 ruff 基线、各模块章节的 FRMW-* issue、审查汇总)
    - .planning/codebase/CONCERNS.md (确保 Backend 相关已知问题被覆盖)
    - .planning/REQUIREMENTS.md (BKND-01~05 定义，确保所有需求被追踪)
  </read_first>
  <action>
    Per D-04（docs/reviews/ 目录）、D-08（跨层专题章节）、D-09（单向参照 BKND→FRMW）、BKND-05 要求，完成审查报告的最后整理。

    1. 跨层交叉参照（Per D-08 + D-09）：在 REVIEW-BACKEND.md 末尾添加 `## 跨层问题` 章节。扫描所有 BKND-* issue，识别与框架层有关的问题。按主题归类（如"错误处理不匹配"、"安全策略不连续"、"框架 API 误用"），每个主题下：
       - 列出 BKND-* issue ID 和简述
       - 引用 REVIEW-FRAMEWORK.md 中相关的 FRMW-* issue ID
       - 说明跨层关联的具体表现
       仅标注 Backend 发现中与 Framework 有关的问题，不反向扩展（Per D-09 单向参照）。

       预期跨层关联主题（基于代码预读）：
       - "API Key 管理"：Backend config 存储明文 key，Framework providers 使用 SecretStr — BKND-SEC-* 关联 FRMW-SEC-*
       - "错误处理策略不匹配"：Backend SSE 返回 str(exc)，Framework AgentLoop yield LoopEvent(type="error") — BKND-ARCH-* 关联 FRMW-ARCH-*
       - "同步 I/O 在 async 上下文中"：Backend session.py 使用同步文件读写，Framework memory/ 层有同样问题 — BKND-ARCH-* 关联 FRMW-ARCH-*
       - "AgentLoop 参数传递不完整"：Backend AgentFactory.create_loop 仅传 4 个参数，AgentLoop 支持 15+ 个参数 — BKND-ARCH-* 关联 FRMW-ARCH-*（AgentLoop.__init__ 参数膨胀）

    2. 修复建议质量检查：逐一审查所有 CRITICAL 和 HIGH issue，确保每个都有：
       - 明确的修复方向（不是"考虑修复"而是"改为使用 X 模式"）
       - 具体的文件和函数定位
       - 修复风险评估

    3. 在报告头部（ruff 基线章节之后、文件章节之前）添加汇总章节：
       - `## 审查汇总`
       - Issue 总数统计
       - 按严重性分布表格：CRITICAL / HIGH / MEDIUM / LOW 各多少个
       - 按文件分布表格：每个文件的 issue 数量
       - 按类型分布表格：BKND-DEAD / BKND-LOGIC / BKND-ARCH / BKND-SEC 各多少个
       - 与 REQUIREMENTS.md BKND-01~05 的追踪矩阵：每个 BKND-XX 对应哪些 issue

    4. CONCERNS.md 覆盖检查：CONCERNS.md 记录"Backend 完全 scaffold"和"No tests for backend"，确认这些在报告中已有对应说明（scaffold 已确认状态变化，测试范围排除 per D-11）。

    5. Issue ID 连续性检查：确保 BKND-DEAD-01~N、BKND-LOGIC-01~N、BKND-ARCH-01~N、BKND-SEC-01~N 编号连续无跳号。如不连续，重新编号。

    6. 最终质量检查：
       - 每个 issue 包含 6 个字段：ID、描述、文件位置（文件:行号）、影响、修复建议、优先级
       - 每个文件章节内按 CRITICAL → HIGH → MEDIUM → LOW 排序
       - 无 RUF001/RUF002 相关的 LOW 级别噪音条目
       - 报告头部元数据完整（日期、范围、工具版本）
       - 数据流追踪章节完整（5 个 API 端点）
       - 跨层问题章节有具体的 FRMW-* issue ID 引用
  </action>
  <verify>
    <automated>grep "## 审查汇总" docs/reviews/REVIEW-BACKEND.md && grep "## 跨层问题" docs/reviews/REVIEW-BACKEND.md && grep -c "FRMW-" docs/reviews/REVIEW-BACKEND.md | grep -v "^0$" && grep -c "BKND-" docs/reviews/REVIEW-BACKEND.md | grep -v "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - 报告包含 "## 审查汇总" 章节
    - 汇总章节包含按严重性/文件/类型的三个分布表格
    - 报告包含 "## 跨层问题" 章节
    - 跨层问题章节包含具体的 FRMW-* issue ID 引用（至少 3 个 FRMW- 引用）
    - 跨层问题按主题归类（至少 2 个主题）
    - 所有 CRITICAL issue 有明确的修复建议（非"考虑修复"）
    - 所有 HIGH issue 有明确的修复建议
    - BKND-DEAD/BKND-LOGIC/BKND-ARCH/BKND-SEC 编号各自连续
    - 每个 issue 包含 6 个必要字段
    - 包含 BKND-01~05 追踪矩阵
  </acceptance_criteria>
  <done>
    REVIEW-BACKEND.md 完整审查报告产出。包含 ruff 基线 + scaffold 确认 + 5 个文件章节 + 数据流追踪 + 审查汇总（统计表格 + 追踪矩阵）+ 跨层问题（主题归类 + FRMW-* 引用）。所有 CRITICAL/HIGH issue 有明确修复建议。issue 编号连续，无重复无遗漏。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 用户验证审查报告质量</name>
  <what-built>
    docs/reviews/REVIEW-BACKEND.md — Backend 应用层完整代码审查报告，覆盖 6 个有实质代码的文件（约 706 行）、4 个审查维度（死代码/逻辑漏洞/设计问题/安全漏洞）、5 个 API 端点数据流追踪、跨层交叉参照（引用 REVIEW-FRAMEWORK.md），含 ruff 自动扫描基线和人工审查 issue。
  </what-built>
  <how-to-verify>
    1. 打开 docs/reviews/REVIEW-BACKEND.md
    2. 检查报告头部元数据（日期、范围、工具版本）
    3. 检查 "## ruff 自动扫描基线" 章节是否包含完整的 ruff 输出
    4. 检查 "## Scaffold 文件确认" 章节是否正确列出 7 个空文件
    5. 检查 "## 审查汇总" 章节的统计表格是否完整
    6. 抽查 1-2 个文件章节（如 session.py、chat.py），验证 issue 字段完整性和分级合理性
    7. 检查 "## 数据流追踪" 章节是否覆盖 5 个 API 端点
    8. 检查 "## 跨层问题" 章节是否引用了 REVIEW-FRAMEWORK.md 的 FRMW-* issue
    9. 验证所有 CRITICAL 和 HIGH issue 的修复建议是否具体可行
    10. 检查是否有遗漏的安全问题（认证、CORS、输入验证、信息泄露）
  </how-to-verify>
  <resume-signal>输入 "approved" 批准报告，或描述需要修改的具体问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 报告 → 读者 | 审查报告本身无安全风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-SC | Tampering | 无新包安装 | accept | 本 plan 不安装任何包 |
</threat_model>

<verification>
- `test -f docs/reviews/REVIEW-BACKEND.md` — 报告文件存在
- `grep "## 审查汇总" docs/reviews/REVIEW-BACKEND.md` — 汇总章节存在
- `grep "## 跨层问题" docs/reviews/REVIEW-BACKEND.md` — 跨层参照章节存在
- `grep -c "FRMW-" docs/reviews/REVIEW-BACKEND.md` — 包含框架层引用
- `wc -l docs/reviews/REVIEW-BACKEND.md` — 报告行数合理（预计 200-500 行）
- `cd framework && pytest tests/ -x -q` — 确认源码未被修改
</verification>

<success_criteria>
- REVIEW-BACKEND.md 是完整的 Backend 应用层审查报告
- 包含 ruff 自动扫描基线 + scaffold 确认 + 5 个文件章节 + 数据流追踪 + 审查汇总 + 跨层问题
- 所有 CRITICAL/HIGH issue 有明确修复建议
- issue 统计表格完整（严重性/文件/类型分布）
- BKND-01~05 追踪矩阵完整
- 跨层问题章节引用了 REVIEW-FRAMEWORK.md 的具体 FRMW-* issue ID
- 用户验证通过（checkpoint:human-verify approved）
</success_criteria>

<output>
Create `.planning/phases/13-backend/13-02-SUMMARY.md` when done
</output>
