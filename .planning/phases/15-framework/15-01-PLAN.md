---
phase: 15-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - framework/agent_framework/agents/agent_loop.py
  - framework/agent_framework/agents/config.py
  - framework/agent_framework/agents/reflection.py
  - framework/agent_framework/agents/sub_agent.py
  - framework/agent_framework/hooks/manager.py
  - framework/agent_framework/llm/base.py
  - framework/agent_framework/llm/providers/anthropic_provider.py
  - framework/agent_framework/llm/providers/deepseek_provider.py
  - framework/agent_framework/llm/providers/openai_provider.py
  - framework/agent_framework/llm/retry.py
  - framework/agent_framework/llm/streaming.py
  - framework/agent_framework/llm/transform/_deepseek.py
  - framework/agent_framework/llm/transform/_openai.py
  - framework/agent_framework/orchestrator/worker_agent.py
  - framework/agent_framework/tasks/runner.py
  - framework/agent_framework/teams/manager.py
  - framework/agent_framework/tools/context/token_counter.py
autonomous: true
requirements:
  - FW-DEAD-01
  - FW-DEAD-02
  - FW-DEAD-03
  - FW-DEAD-04
  - FW-DEAD-05
  - FW-DEAD-06
  - FW-SEC-01

must_haves:
  truths:
    - "ruff check --select F401,F821 framework/agent_framework/ 输出为零"
    - "agent_loop.py:288 logger.debug 不再触发 F821"
    - "llm/base.py httpx 引用合法存在于 TYPE_CHECKING guard 内"
    - "全部 964+ 测试通过"
  artifacts:
    - path: "framework/agent_framework/agents/agent_loop.py"
      provides: "logger 定义 + field import 移除"
      contains: "import logging"
    - path: "framework/agent_framework/llm/base.py"
      provides: "httpx TYPE_CHECKING import"
      contains: "TYPE_CHECKING"
  key_links:
    - from: "agents/agent_loop.py"
      to: "logging module"
      via: "import logging + logger = logging.getLogger(__name__)"
      pattern: "import logging"
    - from: "llm/base.py"
      to: "httpx"
      via: "TYPE_CHECKING guard import"
      pattern: "TYPE_CHECKING.*httpx"
---

<objective>
清理 framework/agent_framework/ 中全部 32 个 ruff F401/F821 问题。

Purpose: 消除死代码，修复 logger 未定义 NameError，修复 httpx 在 TYPE_CHECKING guard 外引用。纯机械性清理，无功能变更。

Output: 17 个文件修改，ruff check 零 warning，964+ 测试通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-framework/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix F821 undefined names — logger and httpx</name>
  <files>
    framework/agent_framework/agents/agent_loop.py,
    framework/agent_framework/llm/base.py
  </files>
  <action>
Fix the two F821 (undefined name) errors. These are the only issues that cannot be auto-fixed by ruff.

**1. agent_loop.py — add logger definition (per D-06 / FW-DEAD-06)**

The file uses `logger.debug(...)` at line 288 but has no `import logging` or `logger` definition. Add the standard framework logging pattern near the top of the file, after the existing imports (after the `from agent_framework...` block, before any class/function definitions). Requirement: FW-DEAD-06.

```python
import logging

logger = logging.getLogger(__name__)
```

This matches the pattern used across the framework (confirmed in CONVENTIONS.md).

While in this file, also remove the unused `field` from the `dataclasses` import on line 9:
- Change `from dataclasses import dataclass, field` to `from dataclasses import dataclass`
- This is the F401 issue at agents/agent_loop.py:9

**2. llm/base.py — add httpx import under TYPE_CHECKING guard (per D-01 / FW-SEC-01)**

The file already has `from __future__ import annotations` at line 1. The function `handle_http_error` at line 173 uses `httpx.Response` as a type annotation in a parameter. Since `from __future__ import annotations` makes ALL annotations strings (deferred evaluation), the `"httpx.Response"` string annotation is already legal at runtime. But httpx needs to be importable for type checkers.

Check if the file has a `TYPE_CHECKING` block already. If yes, add `import httpx` inside it. If not, add a TYPE_CHECKING guard after the existing imports. Requirement: FW-SEC-01.

```python
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    import httpx
```

Since `from __future__ import annotations` is already present, no quotes are needed around `httpx.Response` — it is already treated as a string. The TYPE_CHECKING guard ensures httpx is available for static analysis without requiring it at runtime.
  </action>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework && ruff check --select F821 framework/agent_framework/agents/agent_loop.py framework/agent_framework/llm/base.py && echo "PASS: zero F821 in target files"</automated>
  </verify>
  <done>F821 errors resolved: agent_loop.py has `import logging` + `logger = logging.getLogger(__name__)`, llm/base.py has httpx under TYPE_CHECKING guard, unused `field` removed from agent_loop.py dataclasses import.</done>
</task>

<task type="auto">
  <name>Task 2: Remove 29 remaining F401 unused imports</name>
  <files>
    framework/agent_framework/agents/config.py,
    framework/agent_framework/agents/reflection.py,
    framework/agent_framework/agents/sub_agent.py,
    framework/agent_framework/hooks/manager.py,
    framework/agent_framework/llm/providers/anthropic_provider.py,
    framework/agent_framework/llm/providers/deepseek_provider.py,
    framework/agent_framework/llm/providers/openai_provider.py,
    framework/agent_framework/llm/retry.py,
    framework/agent_framework/llm/streaming.py,
    framework/agent_framework/llm/transform/_deepseek.py,
    framework/agent_framework/llm/transform/_openai.py,
    framework/agent_framework/orchestrator/worker_agent.py,
    framework/agent_framework/tasks/runner.py,
    framework/agent_framework/teams/manager.py,
    framework/agent_framework/tools/context/token_counter.py
  </files>
  <action>
Remove all remaining F401 unused imports. Use `ruff --fix` for automatic removal, then verify the result (per D-03).

Step 1 — Run ruff auto-fix on the F401 selector:

```
cd /Users/huhu/project/agent_framework && ruff check --select F401 --fix framework/agent_framework/
```

This automatically removes all unused import names. For multi-name imports (e.g., `from typing import Any, AsyncGenerator` where only `AsyncGenerator` is used), ruff removes only the unused name, preserving the rest.

Step 2 — Verify the result. After ruff --fix runs, confirm zero F401 remain:

```
cd /Users/huhu/project/agent_framework && ruff check --select F401 framework/agent_framework/
```

Step 3 — Spot-check the most impactful changes. Read the diff or the changed files to confirm ruff did not remove something that IS used (false positive). Key files to check:
- `llm/providers/anthropic_provider.py` — 6 imports removed (most changes in one file)
- `llm/providers/openai_provider.py` — 5 imports removed
- `llm/providers/deepseek_provider.py` — 3 imports removed
- `tools/context/token_counter.py` — 2 imports removed

If ruff removes a name that IS actually used in the file (unlikely but possible), restore that specific import.
  </action>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework && ruff check --select F401,F821 framework/agent_framework/ && echo "PASS: zero F401/F821 warnings"</automated>
  </verify>
  <done>All 32 F401/F821 issues resolved. `ruff check --select F401,F821 framework/` outputs nothing (zero warnings).</done>
</task>

<task type="auto">
  <name>Task 3: Full test suite verification</name>
  <files>None (verification only)</files>
  <action>
Run the full framework test suite to confirm no regressions from the import cleanup (per D-04).

```
cd /Users/huhu/project/agent_framework/framework && pytest tests/ -v --tb=short 2>&1 | tail -30
```

Check:
1. All 964+ tests pass (no failures, no errors)
2. No import errors or NameErrors at collection time (which would indicate an over-zealous ruff --fix removed a needed import)
3. Count the total tests reported matches or exceeds 964

If any test fails:
- Read the test output to identify the failure
- Check if an import was incorrectly removed
- Restore the needed import
- Re-run the failing test file to confirm the fix
  </action>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && pytest tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <done>964+ tests pass, zero failures, zero errors. No regressions from import cleanup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev tool → source | ruff --fix modifies source files automatically |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-15-01 | Tampering | ruff --fix | mitigate | Verify with `ruff check` and full pytest after auto-fix runs |
| T-15-02 | Tampering | import removal | accept | F401 imports are provably unused by static analysis; test suite provides runtime safety net |
| T-15-SC | Tampering | pip installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
1. `cd /Users/huhu/project/agent_framework && ruff check --select F401,F821 framework/agent_framework/` — outputs nothing (zero warnings)
2. `cd /Users/huhu/project/agent_framework/framework && pytest tests/ -q` — 964+ tests pass, zero failures
3. `grep -n "import logging" framework/agent_framework/agents/agent_loop.py` — logger import present
4. `grep -n "TYPE_CHECKING" framework/agent_framework/llm/base.py` — httpx under TYPE_CHECKING guard
</verification>

<success_criteria>
- `ruff check --select F401,F821 framework/agent_framework/` outputs zero warnings (all 32 issues resolved)
- `agent_loop.py` has `import logging` and `logger = logging.getLogger(__name__)`
- `llm/base.py` has httpx import inside TYPE_CHECKING guard
- Full pytest suite passes with 964+ tests, zero failures
</success_criteria>

<output>
Create `.planning/phases/15-framework/15-01-SUMMARY.md` when done
</output>
