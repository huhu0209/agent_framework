---
phase: 11
plan: "11-03"
name: React-PixiJS ref 桥接 + 事件日志列表 + 端到端集成
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - frontend/src/components/ui/EventLog.tsx
  - frontend/src/components/layout/AppLayout.tsx
  - frontend/src/components/ui/ConfigForm.tsx
  - frontend/src/components/ui/TeamControls.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - CONC-03
  - CONC-05
  - CNFG-02
  - CNFG-03

must_haves:
  truths:
    - "Canvas renders the PixiJS office scene inside the React layout"
    - "Incoming VizEvent updates both the Canvas sprite and the event log simultaneously"
    - "Event log displays timestamp, event type badge, and agent name for each entry"
    - "Event log auto-scrolls to latest entry and caps at 50 entries"
    - "Start Team button sends start_team command via WebSocket with form data"
    - "Stop Team button sends stop_team command via WebSocket"
    - "End-to-end flow: form fill -> Start Team -> backend creates agent -> VizEvent pushes -> Canvas animates + event log updates"
  artifacts:
    - path: "frontend/src/components/ui/EventLog.tsx"
      provides: "Scrollable event log with timestamp, type badge, agent name, auto-scroll, 50-entry cap"
      contains: "export function EventLog"
    - path: "frontend/src/components/layout/AppLayout.tsx"
      provides: "Canvas bridge via useRef calling canvas.init/updateState/destroy"
      contains: "canvasInit\\|canvasUpdateState\\|canvasDestroy"
  key_links:
    - from: "frontend/src/components/layout/AppLayout.tsx"
      to: "frontend/src/canvas/index.ts"
      via: "useRef container div, useEffect calls canvas.init, processes eventLog增量 via canvas.updateState"
      pattern: "updateState"
    - from: "frontend/src/components/ui/EventLog.tsx"
      to: "frontend/src/state/context.tsx"
      via: "useAppState reads eventLog array"
      pattern: "state\\.eventLog"
    - from: "frontend/src/components/ui/TeamControls.tsx"
      to: "frontend/src/hooks/useWebSocket.ts"
      via: "sendMessage sends start_team/stop_team JSON commands"
      pattern: "sendMessage.*start_team\\|stop_team"
---

<objective>
Complete the end-to-end integration: React-PixiJS bridge, event log component, and wire up team controls to send WebSocket commands.

Purpose: Connect all pieces built in Plans 11-01 and 11-02 into a working end-to-end pipeline. The Canvas renders real PixiJS content driven by incoming VizEvents. The event log displays all events in real time. The Start/Stop buttons send actual commands to the backend.

Output: EventLog component, Canvas bridge wiring in AppLayout (replacing placeholder), TeamControls sending real WebSocket commands, and ConfigForm feeding agent config into the start_team command.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-frontend-react/11-CONTEXT.md
@.planning/phases/11-frontend-react/11-RESEARCH.md
@.planning/phases/11-frontend-react/11-UI-SPEC.md
@.planning/phases/09-backend/09-03-SUMMARY.md
@frontend/src/canvas/index.ts
@frontend/src/canvas/types.ts
@frontend/package.json

<interfaces>

From frontend/src/canvas/index.ts (Phase 10 — imperative API):
```typescript
export async function init(container: HTMLElement, options?: { width?: number; height?: number }): Promise<void>
export function updateState(event: VizEvent): void
export function destroy(): void
```

From frontend/src/canvas/types.ts:
```typescript
export interface VizEvent { type: VizEventType; agent: string; payload: Record<string, unknown>; timestamp: number; }
```

From frontend/src/state/types.ts (created by Plan 11-01):
```typescript
export interface AppState {
  connection: { status: ConnectionStatus; retryCount: number };
  agents: Map<string, AgentState>;
  eventLog: VizEvent[];
  formData: { name: string; role: string; systemPrompt: string };
  isTeamRunning: boolean;
}
```

From frontend/src/state/context.tsx (created by Plan 11-01):
```typescript
export function AppProvider({ children }: { children: React.ReactNode }): JSX.Element
export function useAppState(): { state: AppState; dispatch: React.Dispatch<AppAction> }
```

From frontend/src/hooks/useWebSocket.ts (created by Plan 11-02):
```typescript
export function useWebSocket(options: UseWebSocketOptions): { sendMessage: (data: Record<string, unknown>) => void }
```

Backend WebSocket command format (from ws_server.py + CONTEXT.md):
- start_team: { type: "start_team", agent: { name: string, role: string, system_prompt: string } }
- stop_team: { type: "stop_team", name: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EventLog component + Canvas bridge + wire TeamControls and ConfigForm to WebSocket</name>
  <files>
    frontend/src/components/ui/EventLog.tsx
    frontend/src/components/layout/AppLayout.tsx
    frontend/src/components/ui/ConfigForm.tsx
    frontend/src/components/ui/TeamControls.tsx
    frontend/src/App.tsx
  </files>
  <read_first>
    frontend/src/canvas/index.ts
    frontend/src/canvas/types.ts
    frontend/src/state/types.ts
    frontend/src/state/context.tsx
    frontend/src/hooks/useWebSocket.ts
    frontend/src/components/layout/AppLayout.tsx
    frontend/src/components/ui/ConfigForm.tsx
    frontend/src/components/ui/TeamControls.tsx
    frontend/src/App.tsx
    .planning/phases/11-frontend-react/11-CONTEXT.md (D-02, D-03, D-11, D-12)
    .planning/phases/11-frontend-react/11-RESEARCH.md (Pattern 3: React-PixiJS bridge, Pattern 4: message protocol)
    .planning/phases/11-frontend-react/11-UI-SPEC.md (EventLog spec, Event Log Entry Format, CanvasContainer spec)
    framework/agent_framework/viz/ws_server.py
  </read_first>
  <action>
    Create EventLog component, replace Canvas placeholder with real PixiJS bridge, and wire up command sending.

    **components/ui/EventLog.tsx** — Export function component EventLog. Per D-03 and UI-SPEC EventLog spec:
    - Read eventLog from useAppState hook
    - Section heading "Event Log" in 16px semibold system-ui
    - Fixed-height scrollable container: 256px height (h-64), overflow-y-auto
    - If eventLog is empty, show empty state per Copywriting Contract: "Waiting for events" heading + "Events will appear here once the team is running." body text
    - Each entry is a single line: timestamp + event type badge + agent name
      - Timestamp: 12px Stone Gray (#87867f), formatted via `new Date(event.timestamp).toLocaleTimeString('en-GB')` (forces 24h HH:MM:SS)
      - Event type badge: 12px text, colored background with 4px border-radius, 2px 6px padding. Color map per UI-SPEC Event Log Entry Format:
        - thinking: bg #dbeafe, text #1d4ed8
        - tool_call: bg #ffedd5, text #c2410c
        - idle: bg #dcfce7, text #15803d
        - done: bg #dcfce7, text #15803d
        - error: bg #fef2f2, text #b53333
        - shutdown: bg #f3f4f6, text #6b7280
      - Agent name: 14px Near Black (#141413), weight 400
    - Auto-scroll to bottom: use useRef on the scroll container div. Use useEffect that runs when eventLog length changes, scrolls container to scrollTop = scrollHeight. Use a separate ref to track previous eventLog length to avoid unnecessary scrolls.
    - Container padding: 12px (p-3)
    - Entry spacing: 4px vertical gap (space-y-1)
    - Container background: Ivory (#faf9f5), border Border Cream (#f0eee6) with 12px border-radius

    **Update components/layout/AppLayout.tsx** — Replace the Canvas placeholder div with a real Canvas bridge:
    - Import init as canvasInit, updateState as canvasUpdateState, destroy as canvasDestroy from `../../canvas/index`
    - Import type VizEvent from `../../canvas/types`
    - Create a CanvasContainer sub-component (defined inside AppLayout.tsx or as inline component) that:
      - Accepts events prop (readonly VizEvent[])
      - Uses useRef for container div (HTMLDivElement)
      - Uses useRef for lastProcessedIndex (number, starts at 0) and isInitialized (boolean, starts false)
      - useEffect for init: if containerRef.current exists and !isInitialized.current, set isInitialized to true, call await canvasInit(containerRef.current, { width: 800, height: 600 }). Return cleanup: canvasDestroy(), set isInitialized back to false. This handles StrictMode double-mount correctly.
      - useEffect for event processing: iterate from lastProcessedIndex.current to events.length, calling canvasUpdateState for each new event. Update lastProcessedIndex after loop.
      - Renders div with ref={containerRef}, style width 800 height 600, className "flex-shrink-0"
    - In AppLayout, pass state.eventLog as events to CanvasContainer
    - Add EventLog component to the right panel, below AgentList
    - Right panel order per D-02: ConfigForm, TeamControls, AgentList, EventLog

    **Update components/ui/ConfigForm.tsx** — Add an onFormSubmit callback prop or expose form data for TeamControls to use. The simplest approach: make the form dispatch UPDATE_FORM on every keystroke (already done in Plan 11-01), so formData in state is always current. No changes needed to ConfigForm itself if TeamControls reads formData from state directly.

    **Update components/ui/TeamControls.tsx** — Wire up real WebSocket command sending:
    - Accept `sendMessage` prop (function taking Record<string, unknown>)
    - On Start Team click: read formData from useAppState (state.formData). Dispatch TEAM_STARTED. Call sendMessage with { type: "start_team", agent: { name: formData.name, role: formData.role, system_prompt: formData.systemPrompt } }
    - On Stop Team click: read formData.name from useAppState. Dispatch TEAM_STOPPED. Call sendMessage with { type: "stop_team", name: formData.name }
    - Validate that Name field is not empty before sending start_team (show no-op or visual cue if empty)

    **Update AppLayout.tsx wiring** — Pass sendMessage from useWebSocket hook (already called in AppLayout from Plan 11-02) down to TeamControls as a prop. The sendMessage function comes from the useWebSocket hook call that was added in Plan 11-02.

    **Update App.tsx** — Verify AppProvider still wraps AppLayout. No structural changes needed unless the component tree needs adjustment.

    IMPORTANT anti-patterns to avoid (from RESEARCH.md):
    - Do NOT call canvas API from the reducer — reducer is a pure function
    - Do NOT use React re-render to drive PixiJS — use ref + useEffect with eventLog increment tracking
    - Do NOT skip StrictMode cleanup — canvasDestroy MUST be called in useEffect cleanup
    - Do NOT process all events on every render — track lastProcessedIndex with useRef to process only new events
  </action>
  <verify>
    cd /Users/huhu/project/agent_framework/frontend && npx tsc --noEmit --pretty 2>&1 | head -30 && npx vite build 2>&1 | tail -10
  </verify>
  <done>
    - EventLog renders scrollable list with timestamp, event type badge, and agent name per entry
    - EventLog auto-scrolls to bottom on new events
    - EventLog caps display at 50 entries (enforced by reducer, EventLog renders what reducer provides)
    - Event type badges use correct colors per UI-SPEC Event Log Entry Format
    - Canvas container renders real PixiJS scene (office + cat sprite) via canvas.init bridge
    - New VizEvent entries in eventLog trigger canvas.updateState for each new event
    - Canvas cleanup (destroy) called on unmount, handles StrictMode double-mount
    - Start Team button sends start_team command via sendMessage with formData
    - Stop Team button sends stop_team command via sendMessage with agent name
    - Start button validates Name field is not empty before sending
    - Right panel layout order: ConfigForm -> TeamControls -> AgentList -> EventLog
    - Empty states render correct copy from Copywriting Contract
    - TypeScript compilation passes
    - Vite build succeeds
  </done>
</task>

</tasks>

<verification>
1. TypeScript type check: `cd frontend && npx tsc --noEmit` — zero errors
2. Vite build: `cd frontend && npx vite build` — succeeds
3. End-to-end manual test (requires Phase 9 backend running):
   a. Start backend: `cd framework && python -m agent_framework.viz.ws_server` (or equivalent)
   b. Start frontend: `cd frontend && npm run dev`
   c. Open browser: verify Canvas shows office scene with cat sprite
   d. Fill in Name + Role in form, click Start Team
   e. Verify: event log entries appear, Canvas cat animates, Agent list shows agent with status
   f. Click Stop Team, verify agent shuts down
</verification>

<success_criteria>
- Canvas renders real PixiJS office scene (not placeholder) driven by VizEvent via ref bridge
- EventLog displays all events with timestamp, type badge, agent name, auto-scroll, 50-entry cap
- Start/Stop Team buttons send real WebSocket commands to backend
- End-to-end flow works: form -> Start Team -> backend agent -> VizEvent push -> Canvas animate + EventLog update + AgentList status change
- All UI-SPEC colors, typography, spacing, and copy match the design contract
- TypeScript compilation and Vite build pass with zero errors
</success_criteria>

<output>
Create `.planning/phases/11-frontend-react/11-03-SUMMARY.md` when done
</output>
