---
phase: 18-backend
plan: 03
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - backend/app/api/v1/chat.py
  - backend/main.py
  - backend/app/config/__init__.py
autonomous: true
requirements:
  - BK-SEC-01
  - BK-SEC-02
  - BK-SEC-03
  - BK-SEC-04
  - BK-SEC-05

must_haves:
  truths:
    - "SSE error events return categorized user-friendly messages, never raw str(exc)"
    - "session.messages still stores raw str(exc) for server debugging"
    - "Invalid session_id in path parameters returns 422 automatically"
    - "CORS only allows GET/POST/DELETE/PATCH methods and Content-Type header"
    - "Redis ConnectionError/TimeoutError logs ERROR and degrades; ValueError propagates"
    - "Settings.llm_api_key is SecretStr, not plain string"
  artifacts:
    - path: "backend/app/api/v1/chat.py"
      provides: "ErrorCategory enum + _classify_error + session_id Path validation"
      contains: "class ErrorCategory"
    - path: "backend/main.py"
      provides: "Tightened CORS config + specific Redis exception handling"
      contains: "redis_lib.ConnectionError"
    - path: "backend/app/config/__init__.py"
      provides: "SecretStr for llm_api_key"
      contains: "SecretStr"
  key_links:
    - from: "backend/app/api/v1/chat.py"
      to: "framework/agent_framework/llm/base.py"
      via: "isinstance checks against LLMAdapterError hierarchy"
      pattern: "isinstance.*LLMAdapterError"
    - from: "backend/app/api/v1/chat.py"
      to: "backend/app/models/__init__.py"
      via: "SESSION_ID_RE used in Path(pattern=...)"
      pattern: "Path.*SESSION_ID_RE"
    - from: "backend/main.py"
      to: "redis library"
      via: "Specific exception catch for ConnectionError/TimeoutError"
      pattern: "redis_lib\\.ConnectionError"
    - from: "backend/app/config/__init__.py"
      to: "pydantic SecretStr"
      via: "field type and get_secret_value() in consumer"
      pattern: "SecretStr"
---

<objective>
Fix all 5 backend security issues: SSE error sanitization, session_id validation, CORS tightening, Redis exception handling, and API key protection.

Purpose: Prevent information disclosure via error messages, path traversal via unvalidated session_id, CORS exploitation, silent Redis failures, and API key leaks.
Output: 3 files modified with all BK-SEC requirements satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-backend/18-CONTEXT.md
@.planning/phases/18-backend/18-RESEARCH.md
@.planning/phases/18-backend/18-01-SUMMARY.md

<interfaces>
<!-- LLM exception hierarchy for ErrorCategory mapping -->

From framework/agent_framework/llm/base.py:
```python
class LLMAdapterError(Exception): ...
class RateLimitError(LLMAdapterError): ...        # BK-SEC-01: maps to LLM_RATE_LIMIT
class ServiceUnavailableError(LLMAdapterError): ... # BK-SEC-01: maps to LLM_TIMEOUT
class InvalidRequestError(LLMAdapterError): ...    # BK-SEC-01: maps to LLM_TIMEOUT
class CircuitOpenError(LLMAdapterError): ...       # BK-SEC-01: maps to LLM_TIMEOUT
```

From backend/app/models/__init__.py:
```python
SESSION_ID_RE = re.compile(r"^[0-9a-f]{32}$")
```

From backend/app/config/__init__.py (current):
```python
class Settings(BaseSettings):
    llm_api_key: str = ""
    @field_validator("llm_api_key")
    @classmethod
    def api_key_must_not_be_empty(cls, v: str) -> str:
        if not v.strip():
            raise ValueError("APP_LLM_API_KEY is required")
        return v
```

From backend/main.py (current CORS):
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=ALLOWED_ORIGINS,
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

From backend/main.py (current Redis):
```python
try:
    rdb = redis_lib.Redis.from_url(settings.redis_url, decode_responses=True)
    rdb.ping()
except Exception:
    logger.warning("Redis unavailable, caching disabled")
    rdb = None
```

From backend/app/services/agent_factory.py (consumer of Settings.llm_api_key):
```python
adapter = create_adapter(
    provider=settings.llm_provider,
    api_key=settings.llm_api_key,  # Must change to .get_secret_value()
    ...
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ErrorCategory SSE error sanitization + session_id Path validation</name>
  <files>backend/app/api/v1/chat.py</files>
  <read_first>
    - backend/app/api/v1/chat.py (full file -- SSE error handler, all session_id endpoints)
    - backend/app/models/__init__.py (SESSION_ID_RE constant)
    - framework/agent_framework/llm/base.py (lines 87-165 -- exception class hierarchy)
  </read_first>
  <action>
    1. At the top of chat.py (after existing imports), add the ErrorCategory enum and helpers per D-01, D-02, D-03:
       - `import enum` and `import asyncio` (asyncio may already be imported)
       - `class ErrorCategory(enum.Enum)` with values: `LLM_TIMEOUT`, `LLM_RATE_LIMIT`, `TOOL_ERROR`, `SESSION_NOT_FOUND`, `UNKNOWN_ERROR`
       - `_ERROR_MESSAGES: dict[ErrorCategory, str]` mapping each category to a Chinese user-friendly message
       - `def _classify_error(exc: Exception) -> ErrorCategory` using isinstance checks against the LLM exception hierarchy: RateLimitError -> LLM_RATE_LIMIT, (ServiceUnavailableError, CircuitOpenError, other LLMAdapterError) -> LLM_TIMEOUT, asyncio.TimeoutError -> TOOL_ERROR, default -> UNKNOWN_ERROR
       - Import exception classes from agent_framework.llm.base: `LLMAdapterError`, `RateLimitError`, `ServiceUnavailableError`, `CircuitOpenError`

    2. In the SSE `event_stream()` exception handler (around line 132-134), replace the current logic:
       - Keep `session.messages.append({"role": "error", "content": str(exc), ...})` -- raw error for server debugging per D-02
       - Change the SSE yield: `yield _sse("error", {"step": 0, "error": _ERROR_MESSAGES[_classify_error(exc)]})` -- categorized message for client per D-01

    3. Add FastAPI `Path` import: `from fastapi import APIRouter, HTTPException, Path, Request`

    4. Import SESSION_ID_RE from models: add to existing import line `from app.models import ...`

    5. Add `session_id: str = Path(pattern=SESSION_ID_RE.pattern)` parameter to all endpoints with `{session_id}` in path per D-12:
       - `get_history` (line 155): add pattern-validated session_id param, remove unvalidated one
       - `delete_session` (line 189): same
       - `rename_session` (line 202): same
       FastAPI will auto-return 422 for non-matching session_id values.
  </action>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && pytest tests/ -v --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - chat.py contains `class ErrorCategory(enum.Enum)` with 5 values
    - chat.py contains `_classify_error` function using isinstance checks
    - SSE exception handler yields `_ERROR_MESSAGES[_classify_error(exc)]` to client
    - SSE exception handler still appends `str(exc)` to session.messages
    - All 3 endpoints with `{session_id}` path param use `Path(pattern=SESSION_ID_RE.pattern)`
    - Framework tests unaffected
  </acceptance_criteria>
  <done>
    - SSE errors return categorized messages, not raw exceptions (BK-SEC-01)
    - session_id validated at routing level via FastAPI Path pattern (BK-SEC-02)
    - session.messages retains raw str(exc) for debugging (D-02)
  </done>
</task>

<task type="auto">
  <name>Task 2: CORS tightening + Redis exception handling + SecretStr</name>
  <files>backend/main.py, backend/app/config/__init__.py, backend/app/services/agent_factory.py</files>
  <read_first>
    - backend/main.py (CORS middleware config around line 59-65, Redis try/except around line 33-38)
    - backend/app/config/__init__.py (Settings class with llm_api_key: str)
    - backend/app/services/agent_factory.py (from_settings uses settings.llm_api_key)
  </read_first>
  <action>
    1. In main.py CORS config (BK-SEC-03 per D-15):
       - Change `allow_methods=["*"]` to `allow_methods=["GET", "POST", "DELETE", "PATCH"]`
       - Change `allow_headers=["*"]` to `allow_headers=["Content-Type"]`
       - Note: `X-Session-Id` is a RESPONSE header (set by the server in SSE responses), not a request header sent by the client. It does NOT need to be in allow_headers. The frontend only reads it from `res.headers.get('X-Session-Id')` -- it never sends it as a request header.

    2. In main.py Redis initialization (BK-SEC-04 per D-13):
       - Replace `except Exception:` with `except (redis_lib.ConnectionError, redis_lib.TimeoutError) as exc:`
       - Change `logger.warning(...)` to `logger.error("Redis connection failed: %s. Caching disabled.", exc)` for these specific exceptions
       - Do NOT catch ValueError -- malformed URL should propagate and crash the app (correct behavior for config errors)
       - If there's no other catch block, consider: what if Redis throws something else unexpected? Add a second `except Exception as exc:` that also logs ERROR, so the app still degrades gracefully for truly unexpected errors. But ValueError should still propagate.

    3. In backend/app/config/__init__.py (BK-SEC-05 per D-14):
       - Add `SecretStr` to pydantic imports: `from pydantic import SecretStr, field_validator`
       - Change `llm_api_key: str = ""` to `llm_api_key: SecretStr = SecretStr("")`
       - Update the validator: parameter type becomes `v: SecretStr`, check with `v.get_secret_value().strip()`, return `v` (already SecretStr)
       - Default value: `SecretStr("")` -- empty secret that fails validation

    4. In backend/app/services/agent_factory.py:
       - In `from_settings`, change `api_key=settings.llm_api_key` to `api_key=settings.llm_api_key.get_secret_value()`
       - This is the ONLY place that extracts the secret value -- create_adapter() requires a str
  </action>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && pytest tests/ -v --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - main.py: `allow_methods` is explicit list `["GET", "POST", "DELETE", "PATCH"]`
    - main.py: `allow_headers` is `["Content-Type"]` (X-Session-Id NOT included -- it's a response header)
    - main.py: Redis catch uses `redis_lib.ConnectionError, redis_lib.TimeoutError` specifically
    - main.py: Redis failure logs at ERROR level, not WARNING
    - config/__init__.py: `llm_api_key` type is `SecretStr`
    - config/__init__.py: validator uses `v.get_secret_value()` for the empty check
    - agent_factory.py: `api_key=settings.llm_api_key.get_secret_value()`
    - 964+ framework tests pass
  </acceptance_criteria>
  <done>
    - CORS allows only actual-used methods and headers (BK-SEC-03)
    - Redis ConnectionError/TimeoutError logged as ERROR with degrade; ValueError propagates (BK-SEC-04)
    - API key stored as SecretStr, extracted only in AgentFactory (BK-SEC-05)
    - Framework tests unaffected
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> SSE endpoint | Untrusted client receives error messages -- must be sanitized |
| Client -> REST endpoints | session_id path param from untrusted input |
| Client -> CORS | Cross-origin requests must be restricted |
| Config -> Redis | Redis connection can fail -- must degrade gracefully |
| Environment -> Config | API key from env var must be protected from logging |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | SSE event_stream | mitigate | ErrorCategory enum replaces str(exc) in SSE events (D-01) |
| T-18-02 | Tampering | session_id path params | mitigate | FastAPI Path(pattern=SESSION_ID_RE) validates format (D-12) |
| T-18-03 | Spoofing | CORS middleware | mitigate | Explicit methods/headers whitelist (D-15) |
| T-18-04 | Denial of Service | Redis connection init | mitigate | Specific exception handling with graceful degradation (D-13) |
| T-18-05 | Information Disclosure | Settings.llm_api_key | mitigate | Pydantic SecretStr prevents repr/str/serialization leaks (D-14) |
| T-18-SC | Tampering | No new package installs | accept | All dependencies already verified |
</threat_model>

<verification>
1. `cd /Users/huhu/project/agent_framework/framework && pytest tests/ -v --tb=short` -- 964+ pass
2. `grep 'allow_methods' backend/main.py` -- shows explicit method list, not wildcard
3. `grep 'SecretStr' backend/app/config/__init__.py` -- confirms SecretStr type
4. `grep 'get_secret_value' backend/app/services/agent_factory.py` -- confirms key extraction
5. `grep 'ErrorCategory' backend/app/api/v1/chat.py` -- confirms enum exists
6. `grep 'Path.*SESSION_ID_RE' backend/app/api/v1/chat.py` -- confirms path validation
7. `grep 'redis_lib.ConnectionError' backend/main.py` -- confirms specific exception handling
</verification>

<success_criteria>
- SSE errors return ErrorCategory-based user messages (BK-SEC-01)
- session_id validated via FastAPI Path pattern (BK-SEC-02)
- CORS restricted to GET/POST/DELETE/PATCH + Content-Type (BK-SEC-03)
- Redis exceptions handled specifically: ConnectionError/TimeoutError -> degrade, ValueError -> crash (BK-SEC-04)
- API key protected by SecretStr (BK-SEC-05)
- 964+ framework tests pass
</success_criteria>

<output>
Create `.planning/phases/18-backend/18-03-SUMMARY.md` when done
</output>
