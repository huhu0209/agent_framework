---
phase: 22-simple-module-adapters-skills-hooks-commands
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - framework/agent_framework/skills/registry.py
  - framework/agent_framework/hooks/manager.py
  - framework/agent_framework/commands/dispatcher.py
  - framework/tests/test_skills_registry.py
  - framework/tests/test_hook_manager.py
  - framework/tests/test_command_dispatcher.py
autonomous: true
requirements: [ADP-01, ADP-02, ADP-03, ADP-09]

must_haves:
  truths:
    - "SkillRegistry.from_loader(loader) creates registry populated from discover('skills') paths, project-level same-name skill overrides global"
    - "HookManager.from_loader(loader) loads hooks from all discovered hooks.json files, global loaded first then project appended"
    - "CommandDispatcher.from_loader(loader) creates dispatcher with SkillRegistry loaded via SkillRegistry.from_loader()"
    - "All existing constructor signatures unchanged — from_loader() is purely additive"
    - "All 1079 existing tests pass"
  artifacts:
    - path: "framework/agent_framework/skills/registry.py"
      provides: "SkillRegistry.from_loader @classmethod"
      contains: "def from_loader"
    - path: "framework/agent_framework/hooks/manager.py"
      provides: "HookManager.from_loader @classmethod"
      contains: "def from_loader"
    - path: "framework/agent_framework/commands/dispatcher.py"
      provides: "CommandDispatcher.from_loader @classmethod"
      contains: "def from_loader"
  key_links:
    - from: "SkillRegistry.from_loader"
      to: "ConfigLoader.discover"
      via: "loader.discover('skills')"
      pattern: "loader\.discover\(['\"]skills['\"]\)"
    - from: "HookManager.from_loader"
      to: "HookManager.load_from_json"
      via: "manager.load_from_json(hook_file)"
      pattern: "load_from_json"
    - from: "CommandDispatcher.from_loader"
      to: "SkillRegistry.from_loader"
      via: "SkillRegistry.from_loader(loader)"
      pattern: "SkillRegistry\.from_loader"
---

<objective>
Add from_loader() @classmethod factory methods to SkillRegistry, HookManager, and CommandDispatcher.

Purpose: Enable these modules to self-initialize from ConfigLoader.discover() paths, establishing the adapter pattern that Phase 23 will replicate for more complex modules.
Output: Three new @classmethod methods (each 3-8 lines), dedicated test cases, zero regression on 1079 existing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-simple-module-adapters-skills-hooks-commands/22-CONTEXT.md
@framework/agent_framework/config/loader.py
@framework/agent_framework/skills/registry.py
@framework/agent_framework/hooks/manager.py
@framework/agent_framework/commands/dispatcher.py

@.planning/phases/21-discovery-loader-agents-md-chain/21-01-SUMMARY.md
</context>

<interfaces>
<!-- Executor needs these contracts — extracted from codebase, no exploration needed. -->

From framework/agent_framework/config/loader.py:
```python
class ConfigLoader:
    def __init__(self, global_dir: Path = Path.home(), project_dir: Path = Path.cwd()) -> None
    def discover(self, module_name: str) -> list[Path]
        # Returns existing directories in [global, project] order (low-to-high priority)
        # module_name must be in MODULE_DIRS: skills/agents/commands/hooks/rules/profiles/memory/mcp
        # Unknown type raises ValueError
```

From framework/agent_framework/skills/registry.py:
```python
class SkillRegistry:
    def __init__(self, skills_dirs: list[Path]) -> None
        # Scans dirs in order, first-found wins for same-name skill
    def get_names(self) -> list[str]
    def get_manifest(self, name: str) -> SkillManifest | None
```

From framework/agent_framework/hooks/manager.py:
```python
class HookManager:
    def __init__(self, trusted: bool = False) -> None
    def load_from_json(self, path: Path) -> None
        # Reads hooks.json, registers HookConfig entries. Missing file = no-op.
        # Invalid entries: warning + skip, no crash.
```

From framework/agent_framework/commands/dispatcher.py:
```python
class CommandDispatcher:
    def __init__(self, skill_registry: SkillRegistry | None = None) -> None
    def resolve(self, user_input: str) -> CommandResult
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SkillRegistry.from_loader() — tests + implementation</name>
  <files>
    framework/tests/test_skills_registry.py,
    framework/agent_framework/skills/registry.py
  </files>
  <read_first>
    - framework/agent_framework/skills/registry.py (class to modify)
    - framework/agent_framework/config/loader.py (ConfigLoader.discover() contract)
    - framework/tests/test_skills_registry.py (existing test patterns, fixtures)
  </read_first>
  <behavior>
    - Test from_loader with no discoverable dirs: returns empty SkillRegistry, get_names() == []
    - Test from_loader with global-only dir: registry populated with global skills
    - Test from_loader with both global and project dirs, project has same-name skill: project skill wins (project-level overrides global per D-01)
    - Test from_loader passes reversed discover() order so __init__ first-found-wins gives project priority
    - Test return type is SkillRegistry instance
    - Test existing constructor signature unchanged (ADP-09)
  </behavior>
  <action>
    RED phase: Add a new test class `TestFromLoader` at the end of test_skills_registry.py.

    Each test creates a ConfigLoader instance with tmp_path dirs: set up global_dir=tmp_path/"home", project_dir=tmp_path/"project". Create `.agent-framework/skills/` subdirectories with SKILL.md files as needed.

    Test scenarios (all use ConfigLoader with controlled tmp_path dirs):
    1. `test_from_loader_no_dirs` — No .agent-framework dirs exist. `SkillRegistry.from_loader(loader)` returns registry where `get_names() == []`.
    2. `test_from_loader_global_only` — Only global dir has a skill (SKILL.md with name="global-skill"). Registry `get_names() == ["global-skill"]`.
    3. `test_from_loader_project_overrides_global` — Both dirs have same-name skill "shared". Global version has description="global", project has description="project". After `from_loader`, `get_manifest("shared").description == "project"`.
    4. `test_from_loader_returns_skill_registry` — `isinstance(result, SkillRegistry)` is True.
    5. `test_from_loader_with_disjoint_skills` — Global has "skill-a", project has "skill-b". Both appear: `sorted(get_names()) == ["skill-a", "skill-b"]`.

    Run tests — they MUST fail (RED).

    GREEN phase: Add `from_loader` @classmethod to SkillRegistry in registry.py.

    Implementation per D-01 and D-02:
    - Method signature: `@classmethod def from_loader(cls, loader: ConfigLoader) -> SkillRegistry`
    - Body: call `paths = loader.discover("skills")`, then `return cls(skills_dirs=list(reversed(paths)))`
    - Reversing [global, project] -> [project, global] so that __init__'s first-found-wins gives project skills priority
    - Import ConfigLoader at top: `from agent_framework.config.loader import ConfigLoader`
    - Silent override on name collision per D-02 (existing behavior, no logging needed)

    Run tests — they MUST pass (GREEN). Run full suite: `pytest tests/test_skills_registry.py -v`.
  </action>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && pytest tests/test_skills_registry.py -v -k "FromLoader or from_loader" --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def from_loader" framework/agent_framework/skills/registry.py` returns 1
    - `grep "@classmethod" framework/agent_framework/skills/registry.py | grep -c "from_loader"` returns 1
    - `grep "list(reversed(paths))" framework/agent_framework/skills/registry.py` returns match
    - All new TestFromLoader tests pass
    - All existing TestSkillRegistry tests pass (zero regression)
  </acceptance_criteria>
  <done>SkillRegistry.from_loader(loader) creates registry from discover("skills") paths with project-overrides-global semantics. All existing tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HookManager.from_loader() — tests + implementation</name>
  <files>
    framework/tests/test_hook_manager.py,
    framework/agent_framework/hooks/manager.py
  </files>
  <read_first>
    - framework/agent_framework/hooks/manager.py (class to modify, load_from_json contract)
    - framework/agent_framework/config/loader.py (ConfigLoader.discover() contract)
    - framework/tests/test_hook_manager.py (existing test patterns)
  </read_first>
  <behavior>
    - Test from_loader with no hook dirs: returns HookManager with no hooks registered
    - Test from_loader with global hooks.json only: hooks loaded from global dir
    - Test from_loader with both global and project hooks.json: hooks from both files registered, global first per D-03
    - Test from_loader with hooks dir existing but no hooks.json file: no crash, empty manager per D-04
    - Test from_loader trusted parameter defaults to False per D-04
    - Test from_loader trusted=True passes through to constructor
    - Test hook ordering: global hooks fire before project hooks per D-05
  </behavior>
  <action>
    RED phase: Add new test functions at the end of test_hook_manager.py.

    Each test creates ConfigLoader with tmp_path dirs. Set up `.agent-framework/hooks/` subdirectories with hooks.json files containing `{"hooks": {"PreToolUse": [{"matcher": "...", "hooks": [{"type": "command", "command": "..."}]}]}}` format.

    Test scenarios:
    1. `test_from_loader_no_dirs` — No dirs. `HookManager.from_loader(loader)` returns manager. Fire PreToolUse returns empty list (untrusted by default).
    2. `test_from_loader_global_only` — Global hooks.json has PreToolUse hook. After `from_loader`, manager has the hook registered (check via `len(manager._hooks[HookEvent.PRE_TOOL_USE])`).
    3. `test_from_loader_both_dirs` — Global has PreToolUse hook, project has PostToolUse hook. Both registered: `len(manager._hooks[HookEvent.PRE_TOOL_USE]) == 1` and `len(manager._hooks[HookEvent.POST_TOOL_USE]) == 1`.
    4. `test_from_loader_same_event_both_dirs` — Global hooks.json and project hooks.json both have PreToolUse hooks. Both registered: `len(manager._hooks[HookEvent.PRE_TOOL_USE]) == 2`, global one first per D-05.
    5. `test_from_loader_dir_exists_no_json` — hooks dir exists but no hooks.json. Manager is empty, no exception.
    6. `test_from_loader_trusted_default_false` — `manager.trusted == False`.
    7. `test_from_loader_trusted_true` — `HookManager.from_loader(loader, trusted=True).trusted == True`.

    Run tests — they MUST fail (RED).

    GREEN phase: Add `from_loader` @classmethod to HookManager in manager.py.

    Implementation per D-03, D-04, D-05, D-09:
    - Method signature: `@classmethod def from_loader(cls, loader: ConfigLoader, trusted: bool = False) -> HookManager`
    - Body: create `manager = cls(trusted=trusted)`, then loop `for hook_dir in loader.discover("hooks"):`, check `if (hook_dir / "hooks.json").exists():`, call `manager.load_from_json(hook_dir / "hooks.json")`, return manager
    - discover() returns [global, project] low-to-high, so global hooks.json loaded first, project second — matching D-03 and D-05
    - Import ConfigLoader at top: `from agent_framework.config.loader import ConfigLoader`
    - trusted defaults to False, matching existing constructor default per D-04 concern resolution

    Run tests — they MUST pass (GREEN). Run full suite: `pytest tests/test_hook_manager.py -v`.
  </action>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && pytest tests/test_hook_manager.py -v -k "from_loader" --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def from_loader" framework/agent_framework/hooks/manager.py` returns 1
    - `grep "trusted: bool = False" framework/agent_framework/hooks/manager.py | grep -c "from_loader"` returns 1
    - All new from_loader tests pass
    - All existing hook manager tests pass (zero regression)
  </acceptance_criteria>
  <done>HookManager.from_loader(loader) loads hooks from discover("hooks") paths, global first then project appended. trusted parameter defaults False. All existing tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CommandDispatcher.from_loader() — tests + implementation</name>
  <files>
    framework/tests/test_command_dispatcher.py,
    framework/agent_framework/commands/dispatcher.py
  </files>
  <read_first>
    - framework/agent_framework/commands/dispatcher.py (class to modify)
    - framework/agent_framework/skills/registry.py (SkillRegistry.from_loader() from Task 1)
    - framework/agent_framework/config/loader.py (ConfigLoader.discover() contract)
    - framework/tests/test_command_dispatcher.py (existing test patterns)
  </read_first>
  <behavior>
    - Test from_loader creates dispatcher with SkillRegistry loaded from discover("skills") per D-06
    - Test from_loader with no skill dirs: dispatcher resolves /skill-name to NONE (no registry skills)
    - Test from_loader with skill dirs: dispatcher can resolve /skill-name to LOAD_SKILL action
    - Test return type is CommandDispatcher instance
    - Test existing constructor still works with None skill_registry per ADP-09
  </behavior>
  <action>
    RED phase: Add new test class `TestFromLoader` at the end of test_command_dispatcher.py.

    Each test creates ConfigLoader with tmp_path dirs. Skills are SKILL.md files under `.agent-framework/skills/` subdirectories.

    Test scenarios:
    1. `test_from_loader_no_skill_dirs` — No dirs. `CommandDispatcher.from_loader(loader)` returns dispatcher. `dispatcher.resolve("/unknown")` returns action=NONE.
    2. `test_from_loader_with_skills` — Global dir has a skill named "test-skill" (user-invocable=true). Dispatcher `resolve("/test-skill")` returns action=LOAD_SKILL.
    3. `test_from_loader_returns_dispatcher` — `isinstance(result, CommandDispatcher)` is True.
    4. `test_from_loader_skill_registry_populated` — Dispatcher created via from_loader has skill registry with expected names (verify by resolving a known skill name).
    5. `test_existing_constructor_still_works` — `CommandDispatcher(skill_registry=None)` still works, `CommandDispatcher(skill_registry=SkillRegistry(skills_dirs=[tmp_path]))` still works. Verifies ADP-09.

    Run tests — they MUST fail (RED).

    GREEN phase: Add `from_loader` @classmethod to CommandDispatcher in dispatcher.py.

    Implementation per D-06, D-07, D-08:
    - Method signature: `@classmethod def from_loader(cls, loader: ConfigLoader) -> CommandDispatcher`
    - Body: `skill_registry = SkillRegistry.from_loader(loader)`, then `return cls(skill_registry=skill_registry)`
    - No new command file format or directory scanning per D-07
    - SkillRegistry.from_loader() already imported (SkillRegistry is already imported in dispatcher.py)
    - Import ConfigLoader at top: `from agent_framework.config.loader import ConfigLoader`

    Run tests — they MUST pass (GREEN). Run full suite: `pytest tests/test_command_dispatcher.py -v`.
  </action>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && pytest tests/test_command_dispatcher.py -v -k "FromLoader or from_loader" --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def from_loader" framework/agent_framework/commands/dispatcher.py` returns 1
    - `grep "SkillRegistry.from_loader" framework/agent_framework/commands/dispatcher.py` returns match
    - All new TestFromLoader tests pass
    - All existing TestCommandDispatcher tests pass (zero regression)
  </acceptance_criteria>
  <done>CommandDispatcher.from_loader(loader) creates dispatcher with SkillRegistry loaded via from_loader. Existing constructor unchanged. All existing tests pass.</done>
</task>

</tasks>

<verification>
1. All three modules have from_loader @classmethod:
   ```bash
   cd /Users/huhu/project/agent_framework/framework
   grep "def from_loader" agent_framework/skills/registry.py agent_framework/hooks/manager.py agent_framework/commands/dispatcher.py
   ```
   Expected: 3 matches.

2. All 1079 existing tests pass unchanged:
   ```bash
   cd /Users/huhu/project/agent_framework/framework && pytest tests/ -q --tb=short
   ```
   Expected: 1079+ passed, 0 failed.

3. No existing constructor signatures modified (ADP-09):
   ```bash
   cd /Users/huhu/project/agent_framework/framework
   grep "def __init__" agent_framework/skills/registry.py agent_framework/hooks/manager.py agent_framework/commands/dispatcher.py
   ```
   Expected: signatures identical to pre-phase state.

4. Config import direction correct (adapters import config, not reverse):
   ```bash
   cd /Users/huhu/project/agent_framework/framework
   grep "from agent_framework" agent_framework/config/loader.py | grep -v "from agent_framework\.config\."
   ```
   Expected: no output (leaf dependency preserved).
</verification>

<success_criteria>
1. SkillRegistry.from_loader(loader) returns SkillRegistry populated from discover("skills") paths with project-overrides-global (reversed scan order)
2. HookManager.from_loader(loader, trusted=False) returns HookManager with hooks loaded from all discovered hooks.json files, global loaded first
3. CommandDispatcher.from_loader(loader) returns CommandDispatcher with SkillRegistry loaded via SkillRegistry.from_loader()
4. All three from_loader() methods are @classmethod, purely additive — existing constructors untouched
5. All 1079+ existing framework tests pass with zero regression
</success_criteria>

<output>
Create `.planning/phases/22-simple-module-adapters-skills-hooks-commands/22-01-SUMMARY.md` when done
</output>
