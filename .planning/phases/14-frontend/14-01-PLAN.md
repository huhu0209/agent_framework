---
phase: 14-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/reviews/REVIEW-FRONTEND.md
autonomous: true
requirements:
  - FRNT-01
  - FRNT-02
  - FRNT-03
  - FRNT-04

must_haves:
  truths:
    - "ESLint 自动扫描结果已记录，覆盖所有源文件"
    - "每个源文件都经过人工逐文件审查（逻辑、设计、安全）"
    - "所有发现按 FRNT-DEAD/FRNT-LOGIC/FRNT-ARCH/FRNT-SEC ID 编号"
    - "每个 issue 有严重性分级（CRITICAL/HIGH/MEDIUM/LOW）"
  artifacts:
    - path: "docs/reviews/REVIEW-FRONTEND.md"
      provides: "前端审查报告（ESLint 基线 + 全文件审查结果）"
      contains: "ESLint Auto-Scan"
      min_lines: 300
  key_links:
    - from: "docs/reviews/REVIEW-FRONTEND.md"
      to: "frontend/src/"
      via: "逐文件审查"
      pattern: "FRNT-(DEAD|LOGIC|ARCH|SEC)-"
---

<objective>
ESLint 自动扫描基线 + 全部前端源文件人工逐文件审查

Purpose: 完成 Frontend 代码审查的主体工作——自动扫描死代码基线，然后逐文件审查逻辑漏洞、设计问题、安全漏洞。产出 REVIEW-FRONTEND.md 的前半部分（ESLint 基线 + 所有文件审查结果）。

Output: docs/reviews/REVIEW-FRONTEND.md（ESLint 基线章节 + 全文件审查章节，不含跨层问题章节和最终汇总）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-frontend/14-CONTEXT.md
@docs/reviews/REVIEW-FRAMEWORK.md
@docs/reviews/REVIEW-BACKEND.md

报告格式参照 REVIEW-FRAMEWORK.md 和 REVIEW-BACKEND.md 的结构：
1. Header（审计日期、范围、工具）
2. ESLint Auto-Scan Baseline（死代码/未使用 import）
3. 逐文件审查章节（每个文件一个章节，内含 CRITICAL/HIGH/MEDIUM/LOW 分级 issues）
4. Issue ID 格式：FRNT-DEAD-NN, FRNT-LOGIC-NN, FRNT-ARCH-NN, FRNT-SEC-NN
</context>

<tasks>

<task type="auto">
  <name>Task 1: ESLint 自动扫描基线 + 全文件人工审查 + 写入报告前半部分</name>
  <files>docs/reviews/REVIEW-FRONTEND.md</files>
  <read_first>
    frontend/src/store.ts
    frontend/src/App.tsx
    frontend/src/main.tsx
    frontend/src/types.ts
    frontend/src/index.css
    frontend/src/components/AgentResponse.tsx
    frontend/src/components/ChatHeader.tsx
    frontend/src/components/ChatInput.tsx
    frontend/src/components/ChatLayout.tsx
    frontend/src/components/MessageList.tsx
    frontend/src/components/SessionSidebar.tsx
    frontend/src/components/SidebarToggle.tsx
    frontend/src/components/SystemNotification.tsx
    frontend/src/components/TextResponseBlock.tsx
    frontend/src/components/ThinkingBlock.tsx
    frontend/src/components/ToolCallBlock.tsx
    frontend/src/components/UserBubble.tsx
    frontend/src/components/markdown/MarkdownAnchor.tsx
    frontend/src/components/markdown/MarkdownPre.tsx
    frontend/src/components/markdown/MarkdownTable.tsx
    frontend/eslint.config.js
    frontend/package.json
    docs/reviews/REVIEW-FRAMEWORK.md
    docs/reviews/REVIEW-BACKEND.md
  </read_first>
  <behavior>
    - ESLint scan: `cd frontend && npx eslint src/ --format compact` produces complete dead code/unused import report
    - Every source file read and analyzed for: dead code, logic bugs, design issues, security vulnerabilities
    - Issues numbered sequentially (FRNT-DEAD-01, FRNT-LOGIC-01, FRNT-ARCH-01, FRNT-SEC-01, ...)
    - Each issue has: ID, Description, File, Impact, Fix, Priority (CRITICAL/HIGH/MEDIUM/LOW)
    - Report structure matches REVIEW-FRAMEWORK.md / REVIEW-BACKEND.md format
    - Per CONTEXT D-01: review actual Chat UI code only, ignore ROADMAP PixiJS/WebSocket references
    - Per CONTEXT D-02: only review source files, exclude test files
    - Per CONTEXT D-07: cover zustand store design, component structure, re-render issues, error boundaries, virtual list usage, markdown XSS safety, event handling
    - Per CONTEXT D-08: group results by directory (root files, components/, components/markdown/)
    - Per CONTEXT D-09: severity aligned with Phase 12/13 definitions
  </behavior>
  <action>
    执行以下三步，产出 REVIEW-FRONTEND.md 的完整审查内容（ESLint 基线 + 全文件审查 + 审查汇总表），但不包含跨层问题章节（留给 Plan 02）。

    **Step 1: ESLint 自动扫描**
    在 frontend/ 目录下运行 `npx eslint src/ --format compact`（排除 test 文件：`--ignore-pattern '**/*.test.*' --ignore-pattern '**/test-setup.ts'`），记录所有 unused imports、unused variables、React hooks 违规等。将扫描结果写入报告的 "ESLint Auto-Scan Baseline" 章节，格式与 REVIEW-FRAMEWORK.md 的 "ruff Auto-Scan Baseline" 一致（表格 + 逐条展开）。

    **Step 2: 逐文件人工审查**
    对以下 20 个源文件逐一审查（按目录分组，每组内从大到小排列）：

    根目录文件：
    - store.ts (389 lines) — 重点：zustand store 结构合理性、API 调用错误处理、状态派生逻辑、race condition、fetch 错误是否静默吞掉、API_BASE 硬编码
    - index.css (61 lines) — 重点：全局样式影响范围、Tailwind 使用合理性
    - types.ts (40 lines) — 重点：类型定义完整性、与 API 响应结构是否匹配
    - App.tsx (18 lines) — 重点：路由配置、布局组合
    - main.tsx (10 lines) — 入口文件，快速检查

    components/ 文件：
    - SessionSidebar.tsx (256 lines) — 重点：组件设计复杂度、状态管理、事件处理、内存泄漏风险
    - MessageList.tsx (95 lines) — 重点：虚拟化列表使用正确性、scroll 行为、re-render 优化
    - ToolCallBlock.tsx (84 lines) — 重点：渲染逻辑、状态处理
    - ChatInput.tsx (68 lines) — 重点：表单处理、keyboard 事件、disabled 状态
    - AgentResponse.tsx (67 lines) — 重点：条件渲染逻辑
    - TextResponseBlock.tsx (43 lines) — 重点：markdown 渲染配置、XSS 防护
    - SidebarToggle.tsx (41 lines) — 快速检查
    - ThinkingBlock.tsx (31 lines) — 快速检查
    - ChatHeader.tsx (24 lines) — 快速检查
    - ChatLayout.tsx (17 lines) — 快速检查
    - UserBubble.tsx (15 lines) — 快速检查
    - SystemNotification.tsx (15 lines) — 快速检查

    components/markdown/ 文件：
    - MarkdownPre.tsx (79 lines) — 重点：代码块渲染、copy 功能、highlight.js 集成
    - MarkdownAnchor.tsx (31 lines) — 快速检查
    - MarkdownTable.tsx (22 lines) — 快速检查

    每个文件的审查维度（per CONTEXT D-07）：
    - 死代码/未使用导入/未使用变量（FRNT-DEAD）
    - 逻辑漏洞：状态管理缺陷、竞态条件、错误处理缺失（FRNT-LOGIC）
    - 设计问题：组件职责不清、props drilling、re-render 问题、缺失错误边界、过度工程（FRNT-ARCH）
    - 安全漏洞：XSS（markdown 渲染）、敏感信息暴露、不安全的 URL 构建（FRNT-SEC）

    **Step 3: 写入报告**
    将 ESLint 基线和逐文件审查结果写入 docs/reviews/REVIEW-FRONTEND.md。报告结构：

    ```
    # Frontend Code Review Report

    **Audit Date:** 2026-06-09
    **Scope:** frontend/src/ (20 files, ~1521 lines, excluding tests)
    **Auditor:** Automated ESLint scan + manual review (Phase 14, Plan 01)
    **Tools:** ESLint 10.3 + typescript-eslint 8.59 + react-hooks + react-refresh + manual code inspection

    ---

    ## ESLint Auto-Scan Baseline
    [ESLint 扫描结果表格和逐条展开]

    ## Root Files

    ### store.ts
    [CRITICAL/HIGH/MEDIUM/LOW issues]

    ### types.ts
    ...

    ### App.tsx
    ...

    ### main.tsx
    ...

    ### index.css
    ...

    ## components/

    ### SessionSidebar.tsx
    ...

    [... 其余组件 ...]

    ## components/markdown/

    ### MarkdownPre.tsx
    ...

    [... 其余 markdown 组件 ...]

    ## 审查汇总

    [统计表：按严重性和类别汇总 issue 数量]

    | 严重性 | FRNT-DEAD | FRNT-LOGIC | FRNT-ARCH | FRNT-SEC | 合计 |
    |--------|-----------|------------|-----------|----------|------|
    | CRITICAL | ? | ? | ? | ? | ? |
    | HIGH | ? | ? | ? | ? | ? |
    | MEDIUM | ? | ? | ? | ? | ? |
    | LOW | ? | ? | ? | ? | ? |

    [审查覆盖确认清单]
    ```

    注意：
    - 报告暂不含"跨层问题"章节和最终 quality checklist（留给 Plan 02 补充）
    - 不做任何代码修改，仅审查和报告
    - Issue 编号按发现顺序递增，每种类型独立编号（FRNT-DEAD-01, FRNT-DEAD-02..., FRNT-LOGIC-01...）
  </action>
  <verify>
    <automated>test -f docs/reviews/REVIEW-FRONTEND.md && grep -c "FRNT-" docs/reviews/REVIEW-FRONTEND.md</automated>
  </verify>
  <done>
    1. docs/reviews/REVIEW-FRONTEND.md 存在
    2. 包含 "ESLint Auto-Scan Baseline" 章节
    3. 包含所有 20 个源文件的审查章节
    4. 所有 issues 使用 FRNT-DEAD/FRNT-LOGIC/FRNT-ARCH/FRNT-SEC 前缀
    5. 包含审查汇总统计表
    6. 报告至少 300 行
  </done>
  <acceptance_criteria>
    - "ESLint Auto-Scan Baseline" section present in docs/reviews/REVIEW-FRONTEND.md
    - All 20 source files (store.ts, types.ts, App.tsx, main.tsx, index.css, 11 components, 3 markdown components) have dedicated review sections
    - Every issue has an ID matching pattern FRNT-(DEAD|LOGIC|ARCH|SEC)-NN
    - Every issue has File, Impact, Fix, and Priority fields
    - Summary statistics table with severity breakdown (CRITICAL/HIGH/MEDIUM/LOW) x category (DEAD/LOGIC/ARCH/SEC)
    - No test files reviewed (no references to store.test.ts, test-setup.ts, *.test.tsx)
    - No references to PixiJS or WebSocket in any issue (per D-01: review actual code only)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 本地审查 | 在本地文件系统上运行 ESLint 和读取源文件，无外部网络请求 |
| ESLint 执行 | 在 frontend/ 运行 `npx eslint`，使用已安装的 devDependencies，无新包安装 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-SC | Tampering | npm/npx install | accept | No new packages installed; ESLint already in devDependencies |
</threat_model>

<verification>
1. `test -f docs/reviews/REVIEW-FRONTEND.md` — 报告文件存在
2. `grep -c "FRNT-" docs/reviews/REVIEW-FRONTEND.md` — 至少有编号 issues
3. `grep -c "ESLint Auto-Scan" docs/reviews/REVIEW-FRONTEND.md` — ESLint 基线章节存在
4. `grep -c "^### " docs/reviews/REVIEW-FRONTEND.md` — 足够多的文件审查章节
</verification>

<success_criteria>
1. REVIEW-FRONTEND.md 产出，含 ESLint 基线 + 全部 20 个源文件审查结果
2. 所有 issues 按 FRNT-DEAD/FRNT-LOGIC/FRNT-ARCH/FRNT-SEC 分组编号
3. 每类 issue 至少有 1 个发现（前端代码不可能在 4 个维度都完美无缺）
4. 严重性分级与 Phase 12/13 定义一致
5. 审查汇总统计表完整
</success_criteria>

<output>
Create `.planning/phases/14-frontend/14-01-SUMMARY.md` when done
</output>
