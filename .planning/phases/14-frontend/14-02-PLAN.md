---
phase: 14-frontend
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - docs/reviews/REVIEW-FRONTEND.md
autonomous: false
requirements:
  - FRNT-05

must_haves:
  truths:
    - "跨层问题章节完整，按主题归类，引用 REVIEW-FRAMEWORK.md 和 REVIEW-BACKEND.md 的具体 issue ID"
    - "质量检查清单通过（5 项成功标准全 green）"
    - "用户验证了最终报告内容"
  artifacts:
    - path: "docs/reviews/REVIEW-FRONTEND.md"
      provides: "完整前端审查报告（含跨层问题 + 质量检查）"
      contains: "跨层问题"
  key_links:
    - from: "docs/reviews/REVIEW-FRONTEND.md"
      to: "docs/reviews/REVIEW-FRAMEWORK.md"
      via: "跨层引用"
      pattern: "FRMW-"
    - from: "docs/reviews/REVIEW-FRONTEND.md"
      to: "docs/reviews/REVIEW-BACKEND.md"
      via: "跨层引用"
      pattern: "BKND-"
---

<objective>
跨层交叉参照 + 报告最终汇总 + 质量检查 + 用户验证

Purpose: 将 Frontend 审查发现与 Framework/Backend 审查结果进行交叉参照，发现跨层系统性问题。完成报告的最终质量检查，确保覆盖所有审查维度。用户验证报告质量。

Output: docs/reviews/REVIEW-FRONTEND.md 的完整版本（含跨层问题章节 + 质量检查通过）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-frontend/14-CONTEXT.md
@docs/reviews/REVIEW-FRONTEND.md
@docs/reviews/REVIEW-FRAMEWORK.md
@docs/reviews/REVIEW-BACKEND.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 跨层问题交叉参照 + 质量检查</name>
  <files>docs/reviews/REVIEW-FRONTEND.md</files>
  <read_first>
    docs/reviews/REVIEW-FRONTEND.md
    docs/reviews/REVIEW-FRAMEWORK.md
    docs/reviews/REVIEW-BACKEND.md
    frontend/src/store.ts
    frontend/src/types.ts
    frontend/package.json
  </read_first>
  <behavior>
    - Cross-layer issues section identifies themes connecting Frontend findings to Framework/Backend issues
    - Each theme references specific FRNT-XX, FRMW-XX, and BKND-XX issue IDs
    - Quality checklist verifies all 5 ROADMAP success criteria (adjusted for actual code)
    - Final report is self-contained and complete
  </behavior>
  <action>
    在 Plan 01 产出的 REVIEW-FRONTEND.md 基础上，补充两个最终章节。

    **Step 1: 跨层问题章节**
    参照 REVIEW-BACKEND.md 的 "跨层问题" 章节格式，新增 "跨层问题" 章节（放在审查汇总之后）。

    参照方向（per D-11）：FRNT -> BKND/FRMW（仅标注 Frontend 发现中与 Backend/Framework 有关的问题，不反向扩展）。

    每个主题按以下结构组织（与 REVIEW-BACKEND.md 一致）：
    - 主题标题
    - 主题描述段落
    - 跨层对照表（层 | Issue | 描述 | 严重性）
    - "跨层表现" 段落：说明三层之间的具体关联
    - "修复建议" 段落

    需要关注的跨层主题方向：
    - 前端 store.ts 中的 fetch API 调用 vs Backend API 端点设计（CORS、错误响应格式、session 管理）
    - 前端 types.ts 中的类型定义 vs Backend models（数据结构一致性）
    - 前端错误处理 vs Backend SSE error event（BKND-SEC-05 相关：前端如何处理后端错误响应）
    - 前端 markdown XSS 风险 vs Backend 输出内容可信度
    - 前端 API_BASE 配置 vs Backend CORS 配置（BKND-SEC-01 相关）

    仅列出确实存在关联的主题。如果某些方向无实质关联，跳过不要凑数。

    **Step 2: 质量检查清单**
    在跨层问题之后，新增 "Quality Checklist" 章节。按 ROADMAP Phase 14 的成功标准逐项验证（per D-01 调整）：

    | # | 成功标准 | 状态 |
    |---|---------|------|
    | 1 | REVIEW-FRONTEND.md 产出，覆盖所有审查维度（死代码、逻辑漏洞、设计问题、安全问题） | ? |
    | 2 | React 组件树完整审查（props drilling、re-render 问题、zustand store 使用） | ? |
    | 3 | PixiJS 资源管理审查 — N/A（当前前端无 PixiJS 代码，per D-01） | N/A |
    | 4 | WebSocket 客户端安全审查 — N/A（当前前端无 WebSocket 代码，per D-01） | N/A |
    | 5 | 跨层问题标注（与 REVIEW-FRAMEWORK.md、REVIEW-BACKEND.md 交叉参照） | ? |

    每项标注 green (通过) 或说明原因。

    **Step 3: 更新报告尾部**
    在文件末尾添加：
    ```
    ---
    *Report completed: 2026-06-09 -- Phase 14, Plan 01 (file review) + Plan 02 (cross-layer + quality)*
    ```
  </action>
  <verify>
    <automated>grep -c "跨层问题" docs/reviews/REVIEW-FRONTEND.md && grep -c "Quality Checklist" docs/reviews/REVIEW-FRONTEND.md</automated>
  </verify>
  <done>
    1. REVIEW-FRONTEND.md 包含 "跨层问题" 章节，按主题归类
    2. 跨层引用使用 FRMW-XX 和 BKND-XX issue ID
    3. REVIEW-FRONTEND.md 包含 "Quality Checklist" 章节
    4. 所有 5 项成功标准已验证（3 项实际审查 + 2 项 N/A 标注）
    5. 报告以 completion timestamp 结尾
  </done>
  <acceptance_criteria>
    - "跨层问题" section present with at least 1 theme connecting Frontend findings to Framework/Backend issues
    - Each cross-layer theme contains a table with columns: 层 | Issue | 描述 | 严重性
    - Each cross-layer theme references at least one FRNT-XX issue and one FRMW-XX or BKND-XX issue
    - "Quality Checklist" section with 5 rows matching ROADMAP success criteria
    - Rows 3 and 4 marked "N/A" with explanation referencing D-01
    - Report ends with timestamp line containing "Phase 14"
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>REVIEW-FRONTEND.md 完整前端审查报告（ESLint 基线 + 全文件审查 + 跨层问题 + 质量检查）</what-built>
  <how-to-verify>
    1. 打开 docs/reviews/REVIEW-FRONTEND.md
    2. 检查 ESLint Auto-Scan Baseline 章节是否完整
    3. 抽查 2-3 个文件审查章节（建议 store.ts 和 SessionSidebar.tsx），验证 issue 描述准确、严重性合理
    4. 检查跨层问题章节，验证引用的 FRMW-XX 和 BKND-XX issue ID 与实际报告匹配
    5. 检查 Quality Checklist 是否通过
    6. 确认报告无遗漏重要维度
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 本地文件编辑 | 仅修改 docs/reviews/REVIEW-FRONTEND.md，无代码变更 |
| 跨层参照 | 读取已有的 REVIEW-FRAMEWORK.md 和 REVIEW-BACKEND.md，无外部请求 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-SC | Tampering | None | accept | Pure documentation work, no code changes, no package installs |

</threat_model>

<verification>
1. `grep -c "跨层问题" docs/reviews/REVIEW-FRONTEND.md` — 跨层章节存在
2. `grep -c "Quality Checklist" docs/reviews/REVIEW-FRONTEND.md` — 质量检查章节存在
3. `grep -cE "FRMW-|BKND-" docs/reviews/REVIEW-FRONTEND.md` — 至少引用了 Framework/Backend 的 issue
4. 用户验证通过
</verification>

<success_criteria>
1. REVIEW-FRONTEND.md 完整产出（ESLint + 全文件审查 + 跨层问题 + 质量检查）
2. 跨层问题按主题归类，引用具体 issue ID
3. Quality Checklist 全部通过（3 green + 2 N/A）
4. 用户验证通过
5. v0.0.4 milestone 三个审查报告全部完成
</success_criteria>

<output>
Create `.planning/phases/14-frontend/14-02-SUMMARY.md` when done
</output>
