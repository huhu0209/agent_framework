---
phase: 17-framework
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - framework/agent_framework/tools/builtin/search_tools.py
  - framework/agent_framework/tools/builtin/__init__.py
autonomous: true
requirements:
  - FW-LOGIC-05

must_haves:
  truths:
    - "SearchClient class encapsulates _semaphore and _client as instance state"
    - "Module-level mutable globals (_client, _semaphore) eliminated"
    - "ToolRegistry registers SearchClient instance method, not module function"
    - "web_search still works with Tavily API"
  artifacts:
    - path: "framework/agent_framework/tools/builtin/search_tools.py"
      provides: "SearchClient class with search() method"
      contains: "class SearchClient"
    - path: "framework/agent_framework/tools/builtin/__init__.py"
      provides: "Updated registration using SearchClient instance"
      contains: "SearchClient"
  key_links:
    - from: "framework/agent_framework/tools/builtin/__init__.py"
      to: "framework/agent_framework/tools/builtin/search_tools.py"
      via: "SearchClient().search as handler"
      pattern: "SearchClient"
---

<objective>
Refactor search_tools from module-level globals to a SearchClient class instance.

Purpose: Eliminate module-level mutable global state (_client, _semaphore). Per D-09, encapsulate into SearchClient class. Makes the module testable and mockable without global state side effects.

Output: SearchClient class with search() method, registered as instance method in builtin registry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-framework/17-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SearchClient class encapsulating global state (FW-LOGIC-05, per D-09)</name>
  <files>framework/agent_framework/tools/builtin/search_tools.py, framework/agent_framework/tools/builtin/__init__.py</files>
  <read_first>
    - framework/agent_framework/tools/builtin/search_tools.py
    - framework/agent_framework/tools/builtin/__init__.py
    - framework/agent_framework/tools/types.py (for ToolHandler signature)
  </read_first>
  <acceptance_criteria>
    - grep "class SearchClient" framework/agent_framework/tools/builtin/search_tools.py returns 1 match
    - grep -c "^_client" framework/agent_framework/tools/builtin/search_tools.py returns 0 (module-level _client removed)
    - grep -c "^_semaphore" framework/agent_framework/tools/builtin/search_tools.py returns 0 (module-level _semaphore removed)
    - grep "SearchClient" framework/agent_framework/tools/builtin/__init__.py returns at least 1 match
    - cd framework && pytest tests/ --tb=short 2>&1 | tail -3 shows all tests pass
  </acceptance_criteria>
  <action>
    Per D-09: Replace module-level globals with a SearchClient class.

    Changes to framework/agent_framework/tools/builtin/search_tools.py:
    1. Remove module-level `_semaphore: asyncio.Semaphore = asyncio.Semaphore(5)` at line 13.
    2. Remove module-level `_client: AsyncTavilyClient | None = None` at line 16.
    3. Remove `_get_client()` function (lines 19-27) and `reset_client()` function (lines 30-33).
    4. Remove `web_search()` function (lines 36-61).
    5. Create a SearchClient class:
       - `__init__(self, max_concurrent: int = 5)` -- creates `self._semaphore = asyncio.Semaphore(max_concurrent)` and `self._client: AsyncTavilyClient | None = None`
       - `def _get_client(self) -> AsyncTavilyClient` -- lazy init, reads TAVILY_API_KEY from env, same logic as old _get_client but on self._client
       - `async def search(self, args: dict, ctx: ToolUseContext) -> ToolResult` -- exact same logic as old web_search, but uses `self._semaphore` and `self._get_client()`
       - `def reset(self) -> None` -- sets self._client = None (replaces reset_client)
    6. Keep the module docstring the same.
    7. Create a module-level convenience function that delegates to a default SearchClient instance for backward compatibility: `_default_client = SearchClient()` and `web_search = _default_client.search`. This preserves backward compatibility for any code that imports web_search directly.
    8. Also export `reset_client = _default_client.reset` for backward compatibility.

    Changes to framework/agent_framework/tools/builtin/__init__.py:
    1. Change import from `from .search_tools import web_search` to `from .search_tools import SearchClient`.
    2. In create_builtin_registry(), create a SearchClient instance: `search_client = SearchClient()`.
    3. Register the web_search spec with `handler=search_client.search` instead of `handler=web_search`.
    4. All other registrations remain unchanged.
  </action>
  <verify>
    <automated>cd framework && pytest tests/ -v --tb=short 2>&1 | tail -10</automated>
  </verify>
  <done>
    - SearchClient class encapsulates _semaphore and _client as instance attributes
    - No module-level mutable globals (_client, _semaphore) remain
    - create_builtin_registry() creates SearchClient instance and registers search_client.search
    - Backward-compatible web_search function available via default instance
    - All 988 tests pass
  </done>
</task>

</tasks>

<verification>
cd framework && pytest tests/ -v --tb=short 2>&1 | tail -10
grep "class SearchClient" framework/agent_framework/tools/builtin/search_tools.py
grep "^_client\|^_semaphore" framework/agent_framework/tools/builtin/search_tools.py
grep "SearchClient" framework/agent_framework/tools/builtin/__init__.py
</verification>

<success_criteria>
- SearchClient class exists with __init__, _get_client, search, reset methods
- Module-level _client and _semaphore variables removed
- ToolRegistry registers SearchClient instance method
- All 988 tests pass
</success_criteria>

<output>
Create `.planning/phases/17-framework/17-02-SUMMARY.md` when done
</output>
