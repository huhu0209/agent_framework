---
phase: 17-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - framework/agent_framework/safety/permissions.py
  - framework/agent_framework/tools/validator.py
  - framework/agent_framework/tools/types.py
  - framework/agent_framework/tools/mcp/config.py
autonomous: true
requirements:
  - FW-LOGIC-02
  - FW-LOGIC-06
  - FW-LOGIC-07
  - FW-LOGIC-10

must_haves:
  truths:
    - "PermissionPipeline accepts critical_tools via constructor parameter"
    - "ToolValidator rejects enum violations and unknown parameters"
    - "ToolUseContext.extra is annotated with ToolContextExtra TypedDict"
    - "MCP ToolSpec handler absence is documented as intentional schema-only design"
  artifacts:
    - path: "framework/agent_framework/safety/permissions.py"
      provides: "PermissionPipeline with critical_tools constructor parameter"
      contains: "critical_tools"
    - path: "framework/agent_framework/tools/validator.py"
      provides: "enum and unknown parameter validation"
      contains: "enum"
    - path: "framework/agent_framework/tools/types.py"
      provides: "ToolContextExtra TypedDict for extra field"
      contains: "ToolContextExtra"
    - path: "framework/agent_framework/tools/mcp/config.py"
      provides: "Documentation of MCP ToolSpec schema-only design"
      contains: "schema-only"
  key_links:
    - from: "framework/agent_framework/safety/permissions.py"
      to: "PermissionPipeline.__init__"
      via: "critical_tools parameter"
      pattern: "critical_tools.*frozenset"
---

<objective>
Four independent small fixes: inject critical_tools into PermissionPipeline constructor, add enum and unknown parameter validation to ToolValidator, type-annotate ToolUseContext.extra with TypedDict, document MCP ToolSpec schema-only design.

Purpose: These are self-contained changes with no cross-dependencies that unblock the router and agent_loop work in Plans 03-04.

Output: Four files modified, each fix verified by existing tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-framework/17-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _CRITICAL_TOOLS constructor injection (FW-LOGIC-02, per D-05)</name>
  <files>framework/agent_framework/safety/permissions.py</files>
  <read_first>
    - framework/agent_framework/safety/permissions.py
  </read_first>
  <behavior>
    - Test: PermissionPipeline with critical_tools={"rm", "execute_code"} rejects "rm" with DENY decision
    - Test: PermissionPipeline with critical_tools=set() passes through to profile disallowed check
    - Test: PermissionPipeline with default critical_tools (empty) does not deny any tool
    - Test: Existing tests still pass after constructor parameter addition
  </behavior>
  <action>
    Per D-05: Convert _CRITICAL_TOOLS from module-level global to PermissionPipeline.__init__ parameter.

    Changes to framework/agent_framework/safety/permissions.py:
    1. Remove the module-level `_CRITICAL_TOOLS: set[str] = set()` at line 40.
    2. Add `critical_tools: frozenset[str] = frozenset()` parameter to PermissionPipeline.__init__. Store as `self._critical_tools`.
    3. In the check() method at line 58, replace `_CRITICAL_TOOLS` reference with `self._critical_tools`.
    4. Keep all other logic identical -- profile disallowed, MODE cascade, ALLOW whitelist, ASK annotation.

    No changes to PermissionPipeline consumers (ToolRouter, AgentLoop) in this task -- they will be updated in Plan 03. The default `frozenset()` is backward compatible.
  </action>
  <verify>
    <automated>cd framework && pytest tests/ -v -k "permission or pipeline" --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Module-level `_CRITICAL_TOOLS` variable removed
    - `PermissionPipeline.__init__` accepts `critical_tools: frozenset[str] = frozenset()`
    - `check()` uses `self._critical_tools` instead of global
    - All existing permission tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ToolValidator enum + unknown validation (FW-LOGIC-06, per D-08)</name>
  <files>framework/agent_framework/tools/validator.py</files>
  <read_first>
    - framework/agent_framework/tools/validator.py
    - framework/agent_framework/tools/types.py (for ToolParameterSchema structure)
  </read_first>
  <behavior>
    - Test: ToolValidator rejects value not in enum list (e.g., event_type="invalid" when enum=["decision","preference"])
    - Test: ToolValidator rejects parameter not in schema properties (unknown parameter)
    - Test: ToolValidator passes valid enum values
    - Test: ToolValidator passes when no enum is defined in schema
    - Test: ToolValidator passes when parameter is in schema properties (not unknown)
  </behavior>
  <action>
    Per D-08: Add two new validation checks to ToolValidator.validate(), after the existing type check block.

    In framework/agent_framework/tools/validator.py, after the type check loop (line 34-49), add:

    1. Enum validation: For each arg that has a corresponding property schema with an "enum" key, check if value is in the enum list. If not, return ToolResult with is_error=True and message like "Parameter validation failed: '{field_name}' value '{value}' not in enum {enum_list}".

    2. Unknown parameter detection: For each arg key, check if it exists in schema.properties. If not, return ToolResult with is_error=True and message like "Parameter validation failed: unknown parameter '{field_name}'".

    Implementation notes:
    - Enum check uses `prop_schema.get("enum")` to get the allowed values list
    - Unknown check: `if field_name not in schema.properties` (properties is a dict)
    - Both checks return early with ToolResult on failure
    - Do NOT add other JSON Schema keywords (minimum, maximum, pattern, etc.) -- per D-08 only enum + unknown
  </action>
  <verify>
    <automated>cd framework && pytest tests/ -v -k "validator" --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    - ToolValidator.validate() checks enum constraints for parameters with enum in schema
    - ToolValidator.validate() rejects unknown parameters not in schema.properties
    - No other validation rules added (per D-08)
    - All existing tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: ToolContextExtra TypedDict for extra field (FW-LOGIC-07, per D-07)</name>
  <files>framework/agent_framework/tools/types.py</files>
  <read_first>
    - framework/agent_framework/tools/types.py
  </read_first>
  <action>
    Per D-07: Define a ToolContextExtra TypedDict for documentation and type-hinting, keeping the Pydantic field as dict[str, Any] for backward compatibility.

    Changes to framework/agent_framework/tools/types.py:
    1. Add `from typing import TypedDict` to imports (already has `from typing import Any, Awaitable, Callable`).
    2. Define ToolContextExtra as a TypedDict with total=False and the following known keys:
       - skill_registry: Any (SkillRegistry is framework-internal, avoid circular import)
       - memory_dir: str
       - memory_store: Any (MemoryStore is framework-internal)
       - planning_session: Any (PlanningSession is framework-internal)
       - worker_manager: Any
    3. Keep ToolUseContext.extra annotation as `dict[str, Any]` (Pydantic compatibility).
    4. Add a docstring comment on the extra field: `# See ToolContextExtra for known keys`.
    5. The TypedDict serves as documentation and can be used by type checkers in handler code that casts `ToolUseContext.extra` to `ToolContextExtra`.
  </action>
  <verify>
    <automated>cd framework && pytest tests/ -v --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ToolContextExtra TypedDict defined with known keys: skill_registry, memory_dir, memory_store, planning_session, worker_manager
    - ToolUseContext.extra annotation unchanged (dict[str, Any]) for Pydantic compatibility
    - Docstring or comment on extra field references ToolContextExtra
    - All 988 tests pass
  </done>
</task>

<task type="auto">
  <name>Task 4: Document MCP ToolSpec schema-only design (FW-LOGIC-10, per D-10)</name>
  <files>framework/agent_framework/tools/mcp/config.py</files>
  <read_first>
    - framework/agent_framework/tools/mcp/config.py
  </read_first>
  <action>
    Per D-10: Zero code changes -- only documentation.

    In framework/agent_framework/tools/mcp/config.py, add a comment block above the `_register_tools` method explaining:

    1. MCP ToolSpec objects are schema-only definitions (no handler function).
    2. The handler is None because execution routes through _dispatch_mcp() via the "mcp__" prefix convention in ToolRouter.dispatch().
    3. Direct execution of MCP ToolSpec via ToolExecutor would fail -- they must go through ToolRouter's mcp__ prefix routing.

    Add a similar comment in the ToolSpec instantiation inside _register_tools where handler is not set, clarifying that this is intentional.
  </action>
  <verify>
    <automated>grep -c "schema-only" framework/agent_framework/tools/mcp/config.py</automated>
  </verify>
  <done>
    - _register_tools method has comment explaining MCP ToolSpec is schema-only
    - ToolSpec instantiation has inline comment noting handler=None is intentional
    - No code behavior changes
  </done>
</task>

</tasks>

<verification>
cd framework && pytest tests/ -v --tb=short 2>&1 | tail -10
grep "critical_tools" framework/agent_framework/safety/permissions.py | head -5
grep "enum" framework/agent_framework/tools/validator.py | head -5
grep "ToolContextExtra" framework/agent_framework/tools/types.py
grep "schema-only" framework/agent_framework/tools/mcp/config.py
</verification>

<success_criteria>
- PermissionPipeline.__init__ accepts critical_tools parameter (D-05)
- ToolValidator rejects enum violations and unknown parameters (D-08)
- ToolContextExtra TypedDict defined with known keys (D-07)
- MCP _register_tools has schema-only documentation (D-10)
- All 988 tests pass
- No module-level _CRITICAL_TOOLS global in permissions.py
</success_criteria>

<output>
Create `.planning/phases/17-framework/17-01-SUMMARY.md` when done
</output>
