---
phase: 12-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/reviews/REVIEW-FRAMEWORK.md
autonomous: true
requirements:
  - FRMW-01
  - FRMW-04

must_haves:
  truths:
    - "ruff 自动扫描完成，产生死代码/安全/复杂度基线数据"
    - "llm/ 模块所有源文件逐行审查完成"
    - "llm/ 模块死代码清单完整（含 ruff 未检测到的）"
    - "llm/ 模块安全漏洞清单完整"
  artifacts:
    - path: "docs/reviews/REVIEW-FRAMEWORK.md"
      provides: "审查报告骨架 + ruff 基线 + llm/ 章节初稿"
      contains: "## ruff 自动扫描基线"
  key_links:
    - from: "ruff check output"
      to: "REVIEW-FRAMEWORK.md"
      via: "FRMW-DEAD-* issue IDs"
      pattern: "FRMW-DEAD-"
    - from: "llm/ 源码审查"
      to: "REVIEW-FRAMEWORK.md"
      via: "模块章节"
      pattern: "## llm/"
---

<objective>
Wave 1: ruff 全量自动扫描 + llm/ 模块人工审查

Purpose: 建立自动检测基线数据，完成框架层最大模块（llm/, 2765 行）的逐文件审查。ruff 输出作为 FRMW-01 死代码检测的输入之一；llm/ 模块审查覆盖逻辑漏洞、设计问题、安全漏洞三个维度（FRMW-02/03/04）。

Output: docs/reviews/REVIEW-FRAMEWORK.md 报告骨架（含 ruff 基线数据 + llm/ 模块章节）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-framework/12-CONTEXT.md
@.planning/phases/12-framework/12-RESEARCH.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ruff 自动扫描 + 报告骨架</name>
  <files>docs/reviews/REVIEW-FRAMEWORK.md</files>
  <read_first>
    - .planning/phases/12-framework/12-RESEARCH.md (ruff 已验证的命令和基线数据)
    - docs/reviews/SECURITY-REVIEW.md (issue 格式参考：Description/File Location/Impact/Fix Status)
    - docs/reviews/ARCH-REVIEW.md (issue 格式参考：Description/File Location/Current Impact/Improvement Direction/Priority)
    - .planning/codebase/CONCERNS.md (已知问题清单，作为审查检查输入)
  </read_first>
  <behavior>
    - 运行 ruff 四类扫描：F 系列（死代码）、S 系列（安全）、C901（复杂度）、PLR0913（参数过多）
    - 将所有扫描结果结构化整理到报告骨架中
    - 报告头部包含元数据（审查日期、范围、工具版本）
    - 为每个模块创建占位章节（llm/ tools/ agents/ teams/ memory/ safety/ orchestrator/ hooks/ skills/ tasks/ commands/ prompts/ a2a/ transcript/ viz/）
  </behavior>
  <action>
    Per D-04 和 D-05，使用 ruff 做自动死代码检测，工具先行。

    1. 运行四组 ruff 命令并捕获完整输出：
       - `ruff check --select F401,F811,F841,F821 --no-fix framework/agent_framework/` — 死代码（未使用 import/变量/函数、未定义名称）
       - `ruff check --select S --no-fix framework/agent_framework/` — 安全模式（flake8-bandit）
       - `ruff check --select C901 --no-fix framework/agent_framework/` — 复杂度
       - `ruff check --select PLR0913 --no-fix framework/agent_framework/` — 参数过多

    2. 创建 docs/reviews/REVIEW-FRAMEWORK.md 报告骨架，包含：
       - 报告元数据头部：审查日期（2026-06-09）、范围（framework/agent_framework/ ~100 文件 10,475 行）、审查工具（ruff 0.15.16 + 人工审查）
       - `## ruff 自动扫描基线` 章节：将四组 ruff 输出按文件路径分组整理，每个条目标注规则代码（如 F401、S311）、文件路径和行号、具体问题描述
       - ruff 结果中排除 __init__.py 中的 re-export import（按 RESEARCH.md Pitfall 1，除非不在 __all__ 列表中）
       - 忽略 RUF001/RUF002 告警（按 RESEARCH.md Pitfall 4，中文项目正常行为）
       - 按模块创建占位章节：`## llm/`、`## tools/`、`## agents/`、`## teams/`、`## memory/`、`## safety/`、`## orchestrator/`、`## hooks/`、`## skills/`、`## tasks/`、`## commands/`、`## prompts/`、`## a2a/`、`## transcript/`、`## viz/`，每个章节内按严重性分组（CRITICAL / HIGH / MEDIUM / LOW）

    3. 每个 ruff 条目立即创建 issue 记录到对应模块章节中，使用 FRMW-DEAD- 前缀（死代码）和 FRMW-SEC- 前缀（安全），包含字段：ID、描述、文件位置（文件:行号）、影响、修复建议、优先级（按 D-07 分级标准）。注意 ruff 的安全告警需要人工评估严重性：S311（random）在非加密场景通常是 LOW，S324（sha1）在非安全场景通常是 MEDIUM，S110/S112（silent catch）需要逐个评估为 MEDIUM 或 HIGH。

    Per D-09，issue ID 格式：FRMW-DEAD-01（死代码）、FRMW-SEC-01（安全）、FRMW-LOGIC-01（逻辑）、FRMW-ARCH-01（设计）。
  </action>
  <verify>
    <automated>test -f docs/reviews/REVIEW-FRAMEWORK.md && grep -c "## " docs/reviews/REVIEW-FRAMEWORK.md | grep -v "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - docs/reviews/REVIEW-FRAMEWORK.md 文件存在
    - 报告包含 "## ruff 自动扫描基线" 章节
    - 报告包含所有 16 个模块的占位章节（`## llm/` 到 `## viz/`）
    - ruff F401/F811/F841/F821 结果按模块归类到各章节
    - ruff S 系列结果按模块归类到各章节
    - __init__.py 中在 __all__ 列表里的 re-export import 不在 FRMW-DEAD 条目中
    - 每个 issue 包含 ID、描述、文件位置（文件:行号）三个必要字段
  </acceptance_criteria>
  <done>
    REVIEW-FRAMEWORK.md 报告骨架创建完成，ruff 全量扫描结果结构化整理到对应模块章节。每个 ruff 条目有 FRMW-DEAD-* 或 FRMW-SEC-* issue ID。16 个模块占位章节就位。
  </done>
</task>

<task type="auto">
  <name>Task 2: llm/ 模块逐文件人工审查</name>
  <files>docs/reviews/REVIEW-FRAMEWORK.md</files>
  <read_first>
    - framework/agent_framework/llm/base.py (LLM Adapter 抽象基类, 195 行)
    - framework/agent_framework/llm/types.py (LLM 类型定义, 257 行)
    - framework/agent_framework/llm/resilient.py (ResilientLLMAdapter, 187 行)
    - framework/agent_framework/llm/retry.py (重试 + 熔断器, 262 行)
    - framework/agent_framework/llm/streaming.py (流式响应处理, 293 行)
    - framework/agent_framework/llm/providers/anthropic_provider.py (375 行)
    - framework/agent_framework/llm/providers/openai_provider.py (228 行)
    - framework/agent_framework/llm/providers/deepseek_provider.py (267 行)
    - framework/agent_framework/llm/transform/_normalize.py (消息规范化, 含已知 mutation bug)
    - framework/agent_framework/llm/transform/_anthropic.py (196 行)
    - framework/agent_framework/llm/transform/_openai.py (183 行)
    - framework/agent_framework/llm/transform/_deepseek.py (DeepSeek 转换)
    - .planning/codebase/CONCERNS.md (参考已知 bug：base.py:173 httpx TYPE_CHECKING 问题)
    - .planning/codebase/CONVENTIONS.md (编码规范对照标准)
  </read_first>
  <behavior>
    - llm/ 模块 10 个源文件全部逐行审查
    - 检查每个文件的条件分支覆盖、循环终止、异常处理
    - 检查共享状态同步、async 锁使用
    - 检查 SOLID 原则、过度工程、代码重复
    - 检查输入验证、路径安全、敏感信息泄露
    - 补充 ruff 无法检测的死代码（条件分支不可达、仅被 tests 使用的导出等）
  </behavior>
  <action>
    Per D-01（全面重新审查）、D-02（全模块等权）、D-03（独立审查），逐文件审查 llm/ 模块所有源码（10 文件，2765 行）。

    审查 llm/ 模块下的所有 .py 文件（不含 __init__.py re-exports）。每个文件按统一检查清单审查：
    1. 逻辑正确性：条件分支是否覆盖所有路径？循环终止条件是否正确？返回值语义是否与文档一致？
    2. 竞态条件：共享状态是否正确同步？async 锁使用是否正确？CircuitBreaker 状态转换是否原子？
    3. 错误处理：异常是否正确捕获和传播？silent catch 是否合理？retry_with_backoff 的 retryable 判断是否完整？
    4. 设计模式：是否违反 SOLID？是否过度工程？provider 之间是否有代码重复？transform 函数的职责划分是否合理？
    5. 安全问题：API key 处理是否安全（SecretStr）？httpx 客户端配置是否安全？streaming 处理是否有资源泄漏风险？
    6. 死代码补充：ruff 无法检测到的死代码（如仅被 tests 使用的导出函数、不可达的条件分支、未使用的异常类）

    已知需重点关注的 CONCERNS.md 条目（必须独立重新发现或引用）：
    - base.py:173 — httpx 在 TYPE_CHECKING guard 外引用（ruff F821 确认）
    - _normalize.py:45 — normalize_messages mutate last.content in-place（违反不可变性原则）
    - providers — API key 已改为 SecretStr（验证修复是否完整）

    每个 issue 写入 REVIEW-FRAMEWORK.md 的 `## llm/` 章节下，使用 FRMW-LOGIC-*（逻辑漏洞）、FRMW-ARCH-*（设计问题）、FRMW-SEC-*（安全漏洞）、FRMW-DEAD-*（死代码补充）前缀。按 D-08 模块内按严重性排序（CRITICAL → HIGH → MEDIUM → LOW）。

    Per D-07 分级标准：
    - CRITICAL = 数据丢失 / 安全漏洞 / 系统不可用
    - HIGH = 逻辑错误 / 竞态条件 / 严重设计缺陷
    - MEDIUM = 代码质量 / 可维护性 / 轻微设计不合理
    - LOW = 代码风格 / 命名 / 文档 / 微小优化

    每个 issue 包含（per D-09）：ID、描述、文件位置（文件:行号）、影响、修复建议、优先级。与 FRMW-01~05 需求追踪关联（在 issue 中标注关联的 FRMW-XX 编号）。
  </action>
  <verify>
    <automated>grep -c "FRMW-" docs/reviews/REVIEW-FRAMEWORK.md | grep -v "^0$" && grep -c "## llm/" docs/reviews/REVIEW-FRAMEWORK.md</automated>
  </verify>
  <acceptance_criteria>
    - REVIEW-FRAMEWORK.md 的 llm/ 章节包含至少 10 个 FRMW-* issue（10 个文件，每个文件至少发现 1 个问题）
    - 包含 FRMW-LOGIC-* issue（逻辑漏洞）
    - 包含 FRMW-ARCH-* issue（设计问题）
    - 包含 FRMW-SEC-* issue（安全漏洞，不限于 ruff S 系列结果）
    - 包含 FRMW-DEAD-* issue（ruff 未检测到的死代码）
    - _normalize.py:45 的 in-place mutation 问题被记录为 FRMW-LOGIC-* issue
    - base.py:173 的 httpx TYPE_CHECKING 问题被记录
    - 每个 issue 包含：ID、描述、文件:行号、影响、修复建议、优先级
    - 所有 issue 按 CRITICAL → HIGH → MEDIUM → LOW 排序
  </acceptance_criteria>
  <done>
    llm/ 模块 10 个源文件全部逐行审查完成。REVIEW-FRAMEWORK.md 的 llm/ 章节包含逻辑漏洞、设计问题、安全漏洞、死代码四类 issue，每个 issue 字段完整（ID/描述/文件:行号/影响/修复建议/优先级），按严重性排序。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ruff CLI → 输出 | ruff 输出可信（成熟工具），但需人工过滤误报 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-SC | Tampering | ruff install | accept | 系统级工具，非项目依赖，无需安装到项目 |
</threat_model>

<verification>
- `test -f docs/reviews/REVIEW-FRAMEWORK.md` — 报告文件存在
- `grep -c "## " docs/reviews/REVIEW-FRAMEWORK.md` — 包含模块章节
- `grep -c "FRMW-" docs/reviews/REVIEW-FRAMEWORK.md` — 包含 issue 记录
- `cd framework && pytest tests/ -x -q` — 确认审查未修改任何源码（应与审查前一致）
</verification>

<success_criteria>
- REVIEW-FRAMEWORK.md 包含 ruff 自动扫描基线章节，四类规则结果按模块分组
- 16 个模块占位章节全部创建
- llm/ 模块章节包含完整的人工审查 issue（逻辑/设计/安全/死代码四类）
- 每个 issue 包含完整字段（ID/描述/文件:行号/影响/修复建议/优先级）
- 所有 issue 按 D-07 分级标准正确分级
- 审查过程中未修改 framework/agent_framework/ 下任何源码文件
</success_criteria>

<output>
Create `.planning/phases/12-framework/12-01-SUMMARY.md` when done
</output>
