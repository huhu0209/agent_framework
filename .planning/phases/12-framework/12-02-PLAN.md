---
phase: 12-framework
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - docs/reviews/REVIEW-FRAMEWORK.md
autonomous: true
requirements:
  - FRMW-01
  - FRMW-02
  - FRMW-03
  - FRMW-04

must_haves:
  truths:
    - "tools/ 模块所有源文件逐行审查完成，死代码/逻辑/设计/安全四维度覆盖"
    - "agents/ 模块所有源文件逐行审查完成，死代码/逻辑/设计/安全四维度覆盖"
    - "tools/ 模块安全审查包含路径遍历、MCP 环境注入、权限模型检查"
    - "agents/ 模块安全审查包含 agent_loop 已知 bug 重新发现"
  artifacts:
    - path: "docs/reviews/REVIEW-FRAMEWORK.md"
      provides: "tools/ + agents/ 章节完整 issue 清单"
      contains: "## tools/"
  key_links:
    - from: "CONCERNS.md 安全条目"
      to: "REVIEW-FRAMEWORK.md tools/ 章节"
      via: "安全漏洞重新发现"
      pattern: "safe_path|MCP|PermissionPipeline"
---

<objective>
Wave 2: tools/ + agents/ 模块逐文件人工审查

Purpose: 审查框架层核心引擎模块（tools/ 1511 行 + agents/ 1135 行，共 2646 行）。tools/ 是工具调度核心，包含路由、执行、权限、MCP 集成等安全敏感代码。agents/ 是 Agent 循环核心，包含最复杂的逻辑（agent_loop.py 469 行，C901 复杂度 30）。

Output: REVIEW-FRAMEWORK.md 的 tools/ 和 agents/ 章节完成
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-framework/12-CONTEXT.md
@.planning/phases/12-framework/12-RESEARCH.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/12-framework/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: tools/ 模块逐文件审查</name>
  <files>docs/reviews/REVIEW-FRAMEWORK.md</files>
  <read_first>
    - framework/agent_framework/tools/router.py (工具路由 + 权限 + hook, 183 行)
    - framework/agent_framework/tools/executor.py (工具执行器)
    - framework/agent_framework/tools/registry.py (工具注册表)
    - framework/agent_framework/tools/types.py (ToolCall/ToolResult/ToolSpec/ToolUseContext)
    - framework/agent_framework/tools/validator.py (参数验证)
    - framework/agent_framework/tools/degrader.py (降级策略)
    - framework/agent_framework/tools/builtin/__init__.py (内置工具注册, 135 行)
    - framework/agent_framework/tools/builtin/file_tools.py (文件读写工具 — 安全敏感)
    - framework/agent_framework/tools/builtin/memory_tools.py (记忆工具)
    - framework/agent_framework/tools/builtin/plan_tools.py (规划工具)
    - framework/agent_framework/tools/builtin/search_tools.py (搜索工具 — mock 实现)
    - framework/agent_framework/tools/context/compactor.py (上下文压缩, 204 行)
    - framework/agent_framework/tools/context/result_truncator.py (结果截断)
    - framework/agent_framework/tools/context/token_counter.py (token 计数)
    - framework/agent_framework/tools/mcp/client.py (MCP 客户端)
    - framework/agent_framework/tools/mcp/config.py (MCP 配置, 133 行)
    - framework/agent_framework/tools/mcp/transport.py (MCP 传输, 146 行)
    - .planning/codebase/CONCERNS.md (安全条目：路径遍历、MCP 注入、PermissionPipeline)
  </read_first>
  <behavior>
    - tools/ 模块 12 个源文件全部逐行审查
    - 重点检查：router.py 4 层职责混合、file_tools.py 路径安全、MCP 环境注入、search_tools mock 实现
    - 检查 PermissionPipeline 的 _CRITICAL_TOOLS 空集合问题
    - 检查 ToolRouter.dispatch 的 agent__ 前缀 stub
    - 检查 context/ 子模块的资源管理
  </behavior>
  <action>
    Per D-01/D-02/D-03，逐文件审查 tools/ 模块所有源码（12 文件，1511 行）。

    按统一检查清单审查每个文件：逻辑正确性、竞态条件、错误处理、设计模式、安全问题、死代码补充。

    已知需重点关注的 CONCERNS.md 条目（必须独立重新发现或引用）：
    - file_tools.py — 路径沙箱是否已正确调用 safe_path()（v0.0.1 SEC-01 已修复，验证修复完整性）
    - mcp/transport.py:57 — 环境变量注入（v0.0.1 SEC-02 已修复，验证修复完整性）
    - mcp/config.py — env validator 是否覆盖所有敏感 key
    - router.py:72-76 — ASK 决策返回 error 而非触发 HITL
    - router.py:179-183 — _dispatch_agent 返回 hardcoded "not implemented"
    - permissions.py:40 — _CRITICAL_TOOLS 全局集合始终为空
    - search_tools.py:8-17 — web_search mock 实现
    - context/result_truncator.py:34 — 同步文件 I/O 阻塞事件循环
    - compactor.py:126-156 — 压缩每次额外 LLM 调用

    对安全相关代码特别关注：
    - 验证 safe_path() 是否在所有文件操作入口被调用
    - 验证 MCP env validator 是否完整覆盖敏感 key 模式
    - 检查 ToolUseContext.extra 的 dict[str, Any] 类型安全问题
    - 检查 degrader.py 的降级策略是否有信息泄露风险

    每个 issue 写入 REVIEW-FRAMEWORK.md 的 `## tools/` 章节，使用 FRMW-LOGIC-*/FRMW-ARCH-*/FRMW-SEC-*/FRMW-DEAD-* 前缀，按 D-07 分级，按 D-08 严重性排序，每个 issue 包含完整字段（per D-09）。
  </action>
  <verify>
    <automated>grep -c "## tools/" docs/reviews/REVIEW-FRAMEWORK.md && grep "FRMW-.*tools/" docs/reviews/REVIEW-FRAMEWORK.md | wc -l | grep -v "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - REVIEW-FRAMEWORK.md 的 tools/ 章节包含至少 12 个 FRMW-* issue
    - 包含路径安全相关的 FRMW-SEC-* issue（验证 safe_path 覆盖）
    - 包含 MCP 安全相关的 FRMW-SEC-* issue
    - 包含 router.py 职责混合的 FRMW-ARCH-* issue
    - 包含 _CRITICAL_TOOLS 空集合的 FRMW-LOGIC-* issue
    - 包含 mock 搜索工具的 FRMW-ARCH-* 或 FRMW-LOGIC-* issue
    - 包含同步 I/O 阻塞的 FRMW-ARCH-* issue
    - 每个 issue 包含完整字段
  </acceptance_criteria>
  <done>
    tools/ 模块 12 个源文件全部逐行审查完成。REVIEW-FRAMEWORK.md 的 tools/ 章节包含逻辑漏洞、设计问题、安全漏洞、死代码四类 issue，安全审查覆盖路径遍历、MCP 注入、权限模型。
  </done>
</task>

<task type="auto">
  <name>Task 2: agents/ 模块逐文件审查</name>
  <files>docs/reviews/REVIEW-FRAMEWORK.md</files>
  <read_first>
    - framework/agent_framework/agents/agent_loop.py (Agent 主循环, 469 行, C901=30)
    - framework/agent_framework/agents/reflection.py (反思 agent, 223 行)
    - framework/agent_framework/agents/plan_and_solve.py (规划-求解 agent, 201 行)
    - framework/agent_framework/agents/sub_agent.py (子 agent)
    - framework/agent_framework/agents/config.py (Agent 配置)
    - framework/agent_framework/agents/base.py (空文件, 0 行)
    - .planning/codebase/CONCERNS.md (已知 bug：agent_loop.py:87 Path import 缺失、15 参数构造器)
  </read_first>
  <behavior>
    - agents/ 模块 6 个源文件全部逐行审查
    - 重点检查：agent_loop.py 超高复杂度、15 参数构造器、Path import 缺失
    - 检查反思/规划/子 agent 逻辑正确性
    - 检查 base.py 空文件是否为死代码
  </behavior>
  <action>
    Per D-01/D-02/D-03，逐文件审查 agents/ 模块所有源码（6 文件，1135 行）。

    按统一检查清单审查每个文件：逻辑正确性、竞态条件、错误处理、设计模式、安全问题、死代码补充。

    agent_loop.py 是全框架最复杂的文件（469 行，C901 复杂度 30），需特别注意：
    - run() 方法的每个分支路径是否覆盖所有 LoopEvent 类型
    - planning 状态管理的转换逻辑是否完整
    - memory_flush 和 semantic_extractor 的集成是否正确
    - sub_agent 调用的生命周期管理
    - team_manager 集成的消息传递逻辑
    - skill_dirs 参数的 Path import 缺失问题（CONCERNS.md 已知 bug）
    - 15 参数构造器的设计问题（已记录为 ARCH-01）

    reflection.py 和 plan_and_solve.py 是扩展 agent 模式：
    - 检查反思循环的终止条件是否完善（是否可能无限循环）
    - 检查规划-求解的任务分解逻辑
    - 检查 sub_agent 的消息传递和错误传播

    base.py 是空文件：确认是否为死代码（应记录为 FRMW-DEAD-* issue）

    每个 issue 写入 REVIEW-FRAMEWORK.md 的 `## agents/` 章节，使用 FRMW-LOGIC-*/FRMW-ARCH-*/FRMW-SEC-*/FRMW-DEAD-* 前缀，按 D-07 分级，按 D-08 严重性排序，每个 issue 包含完整字段（per D-09）。
  </action>
  <verify>
    <automated>grep -c "## agents/" docs/reviews/REVIEW-FRAMEWORK.md && grep "FRMW-.*agent" docs/reviews/REVIEW-FRAMEWORK.md | wc -l | grep -v "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - REVIEW-FRAMEWORK.md 的 agents/ 章节包含至少 8 个 FRMW-* issue
    - 包含 agent_loop.py 复杂度相关的 FRMW-ARCH-* issue
    - 包含 Path import 缺失的 FRMW-LOGIC-* issue
    - 包含 15 参数构造器的 FRMW-ARCH-* issue
    - 包含 base.py 空文件的 FRMW-DEAD-* issue
    - 包含 run() 方法分支覆盖相关的 FRMW-LOGIC-* issue
    - 每个 issue 包含完整字段
  </acceptance_criteria>
  <done>
    agents/ 模块 6 个源文件全部逐行审查完成。REVIEW-FRAMEWORK.md 的 agents/ 章节包含逻辑漏洞、设计问题、安全漏洞、死代码四类 issue，重点覆盖 agent_loop.py 超高复杂度和已知 bug。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 审查者 → 源码 | 只读操作，不修改任何源码 |
| MCP config → transport | 环境变量注入风险（v0.0.1 已修复，需验证） |
| file_tools → filesystem | 路径遍历风险（v0.0.1 已修复，需验证） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | file_tools.py | mitigate | 验证 safe_path() 调用覆盖完整性 |
| T-12-02 | Info Disclosure | mcp/config.py | mitigate | 验证 env validator 覆盖完整 |
| T-12-03 | Elevation of Privilege | hooks/manager.py | accept | 审查报告记录，不修复 |
| T-12-04 | Spoofing | teams/bus.py | accept | 文件系统权限依赖，记录风险 |
| T-12-05 | Denial of Service | tasks/runner.py S110 | mitigate | 评估 try-except-pass 合理性 |
| T-12-06 | Info Disclosure | providers SecretStr | mitigate | 验证 get_secret_value() 调用点安全 |
| T-12-SC | Tampering | 无新包安装 | accept | 本 plan 不安装任何包 |
</threat_model>

<verification>
- `grep "## tools/" docs/reviews/REVIEW-FRAMEWORK.md` — tools/ 章节存在
- `grep "## agents/" docs/reviews/REVIEW-FRAMEWORK.md` — agents/ 章节存在
- `grep -c "FRMW-" docs/reviews/REVIEW-FRAMEWORK.md` — issue 数量增加
- `cd framework && pytest tests/ -x -q` — 确认源码未被修改
</verification>

<success_criteria>
- tools/ 章节包含完整的四维度审查 issue（逻辑/设计/安全/死代码）
- agents/ 章节包含完整的四维度审查 issue
- 安全审查覆盖路径遍历、MCP 注入、权限模型（tools/）
- agent_loop.py 的已知 bug 和复杂度问题被独立重新发现并记录
- base.py 空文件被标记为死代码
- 审查过程中未修改任何源码
</success_criteria>

<output>
Create `.planning/phases/12-framework/12-02-SUMMARY.md` when done
</output>
