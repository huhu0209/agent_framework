---
phase: 12-framework
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - docs/reviews/REVIEW-FRAMEWORK.md
autonomous: true
requirements:
  - FRMW-01
  - FRMW-02
  - FRMW-03
  - FRMW-04

must_haves:
  truths:
    - "memory/ + safety/ + teams/ + tasks/ 模块全部逐行审查完成"
    - "安全审查覆盖 CommandPolicy 占位、PermissionPipeline 空集合、HITL 未接线"
    - "竞态条件审查覆盖 MessageBus 非原子读写、TaskManager 批量写入"
    - "memory/ 同步 I/O 阻塞问题被记录"
  artifacts:
    - path: "docs/reviews/REVIEW-FRAMEWORK.md"
      provides: "memory/ + safety/ + teams/ + tasks/ 章节完整 issue 清单"
      contains: "## memory/"
  key_links:
    - from: "CONCERNS.md 竞态条件条目"
      to: "REVIEW-FRAMEWORK.md teams/ 章节"
      via: "bus.py 非原子读写"
      pattern: "read_inbox|write_text"
---

<objective>
Wave 2: memory/ + safety/ + teams/ + tasks/ 模块逐文件人工审查

Purpose: 审查框架层状态管理与安全模块（memory/ 944 行 + safety/ 315 行 + teams/ 345 行 + tasks/ 576 行，共 2180 行）。这四个模块管理框架的核心状态：记忆持久化、安全边界、团队通信、任务生命周期。包含多个已知竞态条件和设计缺陷。

Output: REVIEW-FRAMEWORK.md 的 memory/、safety/、teams/、tasks/ 章节完成
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-framework/12-CONTEXT.md
@.planning/phases/12-framework/12-RESEARCH.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/12-framework/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: memory/ + safety/ 模块逐文件审查</name>
  <files>docs/reviews/REVIEW-FRAMEWORK.md</files>
  <read_first>
    - framework/agent_framework/memory/store.py (记忆存储)
    - framework/agent_framework/memory/log_manager.py (对话日志管理, 含同步 I/O 注释)
    - framework/agent_framework/memory/index_manager.py (记忆索引)
    - framework/agent_framework/memory/semantic_writer.py (语义记忆写入, 133 行, sha1 问题)
    - framework/agent_framework/memory/semantic_extractor.py (语义提取)
    - framework/agent_framework/memory/retriever.py (记忆检索)
    - framework/agent_framework/memory/search.py (记忆搜索)
    - framework/agent_framework/memory/flush.py (记忆刷盘)
    - framework/agent_framework/memory/frontmatter.py (frontmatter 解析)
    - framework/agent_framework/memory/types.py (记忆类型定义)
    - framework/agent_framework/safety/boundary.py (安全边界, CommandPolicy 占位)
    - framework/agent_framework/safety/permissions.py (权限管道, _CRITICAL_TOOLS 空集合)
    - framework/agent_framework/safety/verification.py (验证, 仅 regex_match 实现)
    - framework/agent_framework/safety/hitl.py (人机回环, get_event_loop 弃用)
    - .planning/codebase/CONCERNS.md (已知 bug：同步 I/O、sha1、verification stub、HITL 弃用 API)
  </read_first>
  <behavior>
    - memory/ 9 个源文件 + safety/ 4 个源文件全部逐行审查
    - memory/ 重点：同步 I/O 阻塞、sha1 使用、文件 I/O 原子性
    - safety/ 重点：CommandPolicy 占位、_CRITICAL_TOOLS 空集合、verification 仅 regex_match、HITL 弃用 API
  </behavior>
  <action>
    Per D-01/D-02/D-03，逐文件审查 memory/（9 文件，944 行）和 safety/（4 文件，315 行）。

    memory/ 模块按统一检查清单审查。重点关注：
    - 所有 memory 文件使用同步 Path.read_text()/open() 而非异步 I/O，在 async 框架中阻塞事件循环
    - semantic_writer.py:47 使用 hashlib.sha1() 生成文件名（ruff S324 检测，非安全场景但需评估）
    - index_manager.py 的 _atomic_write 实现是否真正原子（使用 rename 模式）
    - log_manager.py 是否有文件锁机制防止并发写入
    - store.py 的记忆检索是否有竞态条件
    - flush.py 的刷盘时机和错误恢复
    - 搜索和检索的正确性

    safety/ 模块重点关注：
    - boundary.py:28-36 — CommandPolicy 是占位接口，无任何实施逻辑
    - permissions.py:40 — _CRITICAL_TOOLS 全局集合始终为空，DENY 步骤永远通过
    - verification.py:48-53 — VerificationRule 支持 5 种检查类型但只实现了 regex_match，其余返回 None（静默通过）
    - hitl.py:47 — asyncio.get_event_loop() 在 Python 3.10+ 已弃用

    每个 issue 写入对应模块章节，使用 FRMW-LOGIC-*/FRMW-ARCH-*/FRMW-SEC-*/FRMW-DEAD-* 前缀，按 D-07 分级，按 D-08 严重性排序，每个 issue 包含完整字段（per D-09）。
  </action>
  <verify>
    <automated>grep "## memory/" docs/reviews/REVIEW-FRAMEWORK.md && grep "## safety/" docs/reviews/REVIEW-FRAMEWORK.md && grep -c "FRMW-" docs/reviews/REVIEW-FRAMEWORK.md | grep -v "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - REVIEW-FRAMEWORK.md 的 memory/ 章节包含至少 6 个 FRMW-* issue
    - REVIEW-FRAMEWORK.md 的 safety/ 章节包含至少 4 个 FRMW-* issue
    - memory/ 包含同步 I/O 阻塞的 FRMW-ARCH-* issue
    - safety/ 包含 verification stub 的 FRMW-LOGIC-* issue
    - safety/ 包含 _CRITICAL_TOOLS 空集合的 FRMW-SEC-* issue
    - safety/ 包含 CommandPolicy 占位的 FRMW-ARCH-* issue
    - safety/ 包含 HITL 弃用 API 的 FRMW-LOGIC-* issue
    - 每个 issue 包含完整字段
  </acceptance_criteria>
  <done>
    memory/（9 文件）和 safety/（4 文件）全部逐行审查完成。REVIEW-FRAMEWORK.md 对应章节包含四维度 issue。memory/ 重点覆盖同步 I/O 和 sha1 问题，safety/ 重点覆盖占位接口和未实施的安全机制。
  </done>
</task>

<task type="auto">
  <name>Task 2: teams/ + tasks/ 模块逐文件审查</name>
  <files>docs/reviews/REVIEW-FRAMEWORK.md</files>
  <read_first>
    - framework/agent_framework/teams/manager.py (TeamManager 主循环)
    - framework/agent_framework/teams/bus.py (MessageBus, 非原子读写)
    - framework/agent_framework/teams/tools.py (团队工具)
    - framework/agent_framework/teams/types.py (团队类型定义)
    - framework/agent_framework/tasks/manager.py (TaskManager, 248 行, _apply_changes 复杂变异)
    - framework/agent_framework/tasks/runner.py (TaskRunner, 119 行, try-except-pass)
    - framework/agent_framework/tasks/tools.py (任务工具, 147 行)
    - framework/agent_framework/tasks/types.py (任务类型定义)
    - .planning/codebase/CONCERNS.md (已知 bug：bus.py 非原子读写、manager.py 类型标注错误、runner.py silent catch)
  </read_first>
  <behavior>
    - teams/ 4 个源文件 + tasks/ 4 个源文件全部逐行审查
    - teams/ 重点：MessageBus 非原子读写、manager loop 生命周期、inbox 文件无限制增长
    - tasks/ 重点：_apply_changes 复杂变异逻辑、类型标注错误、runner silent catch
  </behavior>
  <action>
    Per D-01/D-02/D-03，逐文件审查 teams/（4 文件，345 行）和 tasks/（4 文件，576 行）。

    teams/ 模块重点关注：
    - bus.py:24-30 — inbox 文件存储在可预测路径，无完整性保护
    - bus.py:38-39 — read_inbox 非原子读写（read + write("")，崩溃时消息丢失）
    - bus.py:50 — try-except-continue（ruff S112 检测）
    - manager.py — TeamManager._loop 的生命周期管理（空闲超时、关闭请求处理）
    - tools.py — 团队工具的正确性和安全性
    - types.py — 数据模型完整性

    tasks/ 模块重点关注：
    - manager.py:185-226 — _apply_changes 复杂变异逻辑（字段更新 + 依赖管理 + 批量写入在一个方法中）
    - manager.py:199 — pending_writes 类型标注错误 list[tuple[Task]] vs 实际 list[Task]
    - manager.py:228-236 — _clear_dependency 中间失败导致不一致状态
    - manager.py — 每次操作扫描所有 task JSON 文件，无内存索引
    - runner.py — try-except-pass（ruff S110 检测）
    - tools.py — 任务工具的正确性
    - types.py — RuntimeTask 可变 dataclass 的设计

    每个 issue 写入对应模块章节，使用 FRMW-LOGIC-*/FRMW-ARCH-*/FRMW-SEC-*/FRMW-DEAD-* 前缀，按 D-07 分级，按 D-08 严重性排序，每个 issue 包含完整字段（per D-09）。
  </action>
  <verify>
    <automated>grep "## teams/" docs/reviews/REVIEW-FRAMEWORK.md && grep "## tasks/" docs/reviews/REVIEW-FRAMEWORK.md && grep -c "FRMW-" docs/reviews/REVIEW-FRAMEWORK.md | grep -v "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - REVIEW-FRAMEWORK.md 的 teams/ 章节包含至少 4 个 FRMW-* issue
    - REVIEW-FRAMEWORK.md 的 tasks/ 章节包含至少 4 个 FRMW-* issue
    - teams/ 包含 MessageBus 非原子读写的 FRMW-LOGIC-* issue
    - teams/ 包含 inbox 文件可预测路径的 FRMW-SEC-* issue
    - tasks/ 包含 _apply_changes 变异逻辑的 FRMW-ARCH-* issue
    - tasks/ 包含 pending_writes 类型标注的 FRMW-LOGIC-* issue
    - tasks/ 包含每次操作全扫描的 FRMW-ARCH-* issue
    - 每个 issue 包含完整字段
  </acceptance_criteria>
  <done>
    teams/（4 文件）和 tasks/（4 文件）全部逐行审查完成。REVIEW-FRAMEWORK.md 对应章节包含四维度 issue。teams/ 重点覆盖 MessageBus 竞态条件和文件安全，tasks/ 重点覆盖 _apply_changes 复杂变异和性能问题。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 审查者 → 源码 | 只读操作 |
| bus.py → filesystem | JSONL inbox 无完整性保护 |
| tasks → filesystem | Task JSON 文件无访问控制 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Tampering | teams/bus.py | accept | 文件权限依赖，记录到审查报告 |
| T-12-08 | Denial of Service | teams/bus.py | mitigate | 评估非原子读写消息丢失风险 |
| T-12-09 | Denial of Service | tasks/runner.py | mitigate | 评估 try-except-pass 影响 |
| T-12-SC | Tampering | 无新包安装 | accept | 本 plan 不安装任何包 |
</threat_model>

<verification>
- `grep "## memory/" docs/reviews/REVIEW-FRAMEWORK.md` — memory/ 章节存在
- `grep "## safety/" docs/reviews/REVIEW-FRAMEWORK.md` — safety/ 章节存在
- `grep "## teams/" docs/reviews/REVIEW-FRAMEWORK.md` — teams/ 章节存在
- `grep "## tasks/" docs/reviews/REVIEW-FRAMEWORK.md` — tasks/ 章节存在
- `cd framework && pytest tests/ -x -q` — 确认源码未被修改
</verification>

<success_criteria>
- memory/ 章节包含同步 I/O 阻塞和 sha1 使用的 issue
- safety/ 章节包含 CommandPolicy 占位、_CRITICAL_TOOLS 空集合、verification stub、HITL 弃用 API 的 issue
- teams/ 章节包含 MessageBus 非原子读写和可预测路径的 issue
- tasks/ 章节包含 _apply_changes 变异逻辑和类型标注错误的 issue
- 所有 issue 按 D-07 分级标准正确分级
- 审查过程中未修改任何源码
</success_criteria>

<output>
Create `.planning/phases/12-framework/12-03-SUMMARY.md` when done
</output>
