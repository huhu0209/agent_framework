---
phase: 16-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - framework/pyproject.toml
  - framework/agent_framework/memory/log_manager.py
  - framework/agent_framework/memory/index_manager.py
  - framework/agent_framework/memory/retriever.py
  - framework/agent_framework/memory/semantic_writer.py
  - framework/agent_framework/memory/store.py
  - framework/agent_framework/memory/flush.py
  - framework/agent_framework/memory/search.py
  - framework/agent_framework/tools/context/result_truncator.py
  - framework/agent_framework/tools/executor.py
autonomous: true
requirements:
  - FW-SEC-02
  - FW-SEC-07

must_haves:
  truths:
    - "memory/ 模块所有文件 I/O 使用 aiofiles 异步操作，不阻塞事件循环"
    - "result_truncator.py 文件写入使用 aiofiles，调用方 ToolExecutor 等待异步完成"
    - "所有 memory/ 公共方法签名改为 async def"
    - "全部 964+ 测试通过"
  artifacts:
    - path: "framework/pyproject.toml"
      provides: "aiofiles 依赖声明"
      contains: "aiofiles"
    - path: "framework/agent_framework/memory/log_manager.py"
      provides: "EpisodicLogManager 异步 append/read_log/write_raw"
      min_lines: 40
    - path: "framework/agent_framework/tools/context/result_truncator.py"
      provides: "异步 truncate_if_needed"
  key_links:
    - from: "framework/agent_framework/memory/store.py"
      to: "framework/agent_framework/memory/log_manager.py"
      via: "await self._log_manager.read_log()"
      pattern: "await.*\\.read_log\\("
    - from: "framework/agent_framework/tools/executor.py"
      to: "framework/agent_framework/tools/context/result_truncator.py"
      via: "await truncate_if_needed()"
      pattern: "await truncate_if_needed"
---

<objective>
将 memory/ 模块和 result_truncator.py 的所有同步文件 I/O 替换为 aiofiles 异步操作，消除事件循环阻塞。

Purpose: 同步文件 I/O 在 async Agent 框架中阻塞事件循环，导致并发 agent 场景下一个 agent 的记忆搜索阻塞其他 agent 执行（per D-01, D-02, D-03）。
Output: memory/ 全模块 + result_truncator.py 改为 async def + aiofiles 调用。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-framework/16-CONTEXT.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From framework/agent_framework/memory/types.py:
- EventType enum (used by EpisodicLogManager.append)
- SemanticMemoryDraft (used by SemanticWriter.write)

From framework/agent_framework/tools/types.py:
- ToolResult class with fields: content, is_error, metadata

From framework/agent_framework/memory/log_manager.py current sync signatures:
- EpisodicLogManager.append(timestamp, event_type, content) -> None
- EpisodicLogManager.read_log(date) -> str | None
- EpisodicLogManager.write_raw(date_str, content) -> None
- EpisodicLogManager.list_dates() -> list[str] (no I/O, no change needed)

From framework/agent_framework/memory/index_manager.py current sync signatures:
- MemoryIndexManager._atomic_write(text) -> None
- MemoryIndexManager.update(file_name, name, description) -> None
- MemoryIndexManager.remove(file_name) -> None

From framework/agent_framework/tools/context/result_truncator.py current sync signature:
- truncate_if_needed(result, tool_call_id, workdir) -> ToolResult

From framework/agent_framework/tools/executor.py line 53:
- return truncate_if_needed(result, spec.name, ctx.working_dir)
  (must become: return await truncate_if_needed(...))
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: memory/ 全模块同步 I/O 改 aiofiles async</name>
  <files>
    framework/pyproject.toml,
    framework/agent_framework/memory/log_manager.py,
    framework/agent_framework/memory/index_manager.py,
    framework/agent_framework/memory/retriever.py,
    framework/agent_framework/memory/semantic_writer.py,
    framework/agent_framework/memory/store.py,
    framework/agent_framework/memory/flush.py,
    framework/agent_framework/memory/search.py
  </files>
  <read_first>
    framework/pyproject.toml
    framework/agent_framework/memory/log_manager.py
    framework/agent_framework/memory/index_manager.py
    framework/agent_framework/memory/retriever.py
    framework/agent_framework/memory/semantic_writer.py
    framework/agent_framework/memory/store.py
    framework/agent_framework/memory/flush.py
    framework/agent_framework/memory/search.py
    framework/tests/test_log_manager.py
    framework/tests/test_index_manager.py
    framework/tests/test_memory_retriever.py
    framework/tests/test_semantic_writer.py
    framework/tests/test_memory_store.py
    framework/tests/test_memory_flush.py
    framework/tests/test_memory_search.py
  </read_first>
  <behavior>
    - EpisodicLogManager.append: async, uses aiofiles.open for append write
    - EpisodicLogManager.read_log: async def, returns str | None
    - EpisodicLogManager.write_raw: async def, uses aiofiles.open for append write
    - MemoryIndexManager._atomic_write: async def, uses aiofiles.open
    - MemoryIndexManager.update: async def (calls await self._atomic_write and await aiofiles.open for read)
    - MemoryIndexManager.remove: async def
    - LLMScoringRetriever._scan_candidates: async def, uses aiofiles.open for read
    - LLMScoringRetriever.retrieve: already async, must await _scan_candidates and aiofiles reads
    - SemanticWriter._create: async def, uses aiofiles.open
    - SemanticWriter._merge: async def, uses aiofiles.open for read and append
    - SemanticWriter.write: async def (calls await self._create or await self._merge, await self._index.update)
    - SemanticWriter.write_batch: async def
    - MemoryStore.search: already async, must await self._log_manager.read_log and self._search_episodic
    - MemoryStore._search_episodic: async def
    - MemoryStore.write: async def (calls await self._writer.write)
    - FlushExtractor.flush: already async, must await log_manager.write_raw
    - handle_memory_search: already async, must await log_manager.read_log
  </behavior>
  <action>
Per D-01, D-02, D-03: Replace all sync file I/O in memory/ module with aiofiles.

Step 1 — Add aiofiles dependency:
In framework/pyproject.toml, add "aiofiles>=24.1.0" to the dependencies list (after websockets line).

Step 2 — Convert EpisodicLogManager (log_manager.py):
- Add `import aiofiles` at top.
- `append()`: change to `async def append()`, replace `with open(log_path, "a", ...)` with `async with aiofiles.open(log_path, "a", encoding="utf-8") as f: await f.write(entry)`.
- `read_log()`: change to `async def read_log()`, replace `log_path.read_text(encoding="utf-8")` with `async with aiofiles.open(log_path, "r", encoding="utf-8") as f: return await f.read()`.
- `write_raw()`: change to `async def write_raw()`, same aiofiles.open pattern with "a" mode.
- Remove the sync limitation note from the module docstring (lines 3-4).

Step 3 — Convert MemoryIndexManager (index_manager.py):
- Add `import aiofiles` at top.
- `_atomic_write()`: change to `async def _atomic_write()`, replace `with os.fdopen(fd, ...)` with aiofiles pattern. Note: tempfile.mkstemp returns fd, so keep os.fdopen for the temp file write (os.replace is already sync and fast), OR switch to aiofiles.open for the temp file. Simplest approach: keep tempfile.mkstemp for temp file creation, use `async with aiofiles.open(tmp_path, "w", encoding="utf-8") as f: await f.write(text)` instead of os.fdopen. Must close the fd first with os.close(fd).
- `update()`: change to `async def update()`, replace `self._path.read_text()` with aiofiles read, call `await self._atomic_write(...)`.
- `remove()`: change to `async def remove()`, same pattern.
- Remove the sync limitation note from docstring (lines 3-4).

Step 4 — Convert LLMScoringRetriever (retriever.py):
- Add `import aiofiles` at top.
- `_scan_candidates()`: change to `async def _scan_candidates()`, replace `f.read_text()` with `async with aiofiles.open(f, "r", encoding="utf-8") as af: content = await af.read()`.
- `retrieve()`: already async, change `self._scan_candidates()` call to `await self._scan_candidates()`.
- For the selected files reading (line 107): replace `fpath.read_text()` with aiofiles read.

Step 5 — Convert SemanticWriter (semantic_writer.py):
- Add `import aiofiles` at top.
- `_create()`: change to `async def _create()`, replace `path.write_text()` with aiofiles write.
- `_merge()`: change to `async def _merge()`, replace `path.read_text()` and `open(path, "a")` with aiofiles equivalents.
- `write()`: change to `async def write()`, add `await` to self._create, self._merge, self._index.update calls.
- `write_batch()`: change to `async def write_batch()`, add `await` to self.write calls.
- Remove the sync limitation note from docstring (lines 3-4).

Step 6 — Convert MemoryStore (store.py):
- `_search_episodic()`: change to `async def _search_episodic()`, add `await` to self._log_manager.read_log calls.
- `search()`: already async, add `await` to self._search_episodic call (line 53).
- `write()`: change to `async def write()`, add `await` to self._writer.write call.

Step 7 — Convert FlushExtractor (flush.py):
- `flush()`: already async, add `await` to log_manager.write_raw call (line 99).

Step 8 — Convert handle_memory_search (search.py):
- Already async, add `await` to log_manager.read_log call (line 30).

Step 9 — Update all tests:
- Any test that calls the now-async methods must use `await`. Since pytest-asyncio is in auto mode and all test files should already be async, add `await` before each call to the converted methods. Read each test file, find calls to the converted functions, prefix with `await`.
  </action>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && pytest tests/test_log_manager.py tests/test_index_manager.py tests/test_memory_retriever.py tests/test_semantic_writer.py tests/test_memory_store.py tests/test_memory_flush.py tests/test_memory_search.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    - memory/ 模块所有文件 I/O 使用 aiofiles
    - 所有涉及文件读写的公共方法签名改为 async def
    - EpisodicLogManager.append/read_log/write_raw 都是 async
    - MemoryIndexManager.update/remove 都是 async
    - LLMScoringRetriever._scan_candidates 是 async
    - SemanticWriter.write/write_batch/_create/_merge 都是 async
    - MemoryStore._search_episodic/write 都是 async
    - FlushExtractor.flush 中 write_raw 调用有 await
    - handle_memory_search 中 read_log 调用有 await
    - 相关测试全部通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: result_truncator.py 改 async + ToolExecutor 调用更新</name>
  <files>
    framework/agent_framework/tools/context/result_truncator.py,
    framework/agent_framework/tools/executor.py
  </files>
  <read_first>
    framework/agent_framework/tools/context/result_truncator.py
    framework/agent_framework/tools/executor.py
    framework/tests/test_result_truncator.py
    framework/tests/test_tool_executor.py
  </read_first>
  <behavior>
    - truncate_if_needed: async def, uses aiofiles for file write
    - ToolExecutor.execute line 53: awaits truncate_if_needed result
    - Tests calling truncate_if_needed use await
  </behavior>
  <action>
Per D-01, D-07: Convert result_truncator.py to async.

In result_truncator.py:
- Add `import aiofiles` at top.
- Change `def truncate_if_needed` to `async def truncate_if_needed`.
- Replace `os.makedirs(dump_dir, exist_ok=True)` — keep this (mkdir is sync but fast for a single directory creation; alternatively use `await asyncio.to_thread(os.makedirs, dump_dir, exist_ok=True)` if strictly needed, but aiofiles does not have makedirs).
- Replace `with open(dump_path, "w", encoding="utf-8") as f: f.write(result.content)` with `async with aiofiles.open(dump_path, "w", encoding="utf-8") as f: await f.write(result.content)`.

In executor.py:
- Change line 53 from `return truncate_if_needed(result, spec.name, ctx.working_dir)` to `return await truncate_if_needed(result, spec.name, ctx.working_dir)`.

In tests:
- Update test_result_truncator.py: add `await` before all `truncate_if_needed()` calls.
- Update test_tool_executor.py if it has assertions involving truncation.
  </action>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && pytest tests/test_result_truncator.py tests/test_tool_executor.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>
    - truncate_if_needed 签名为 async def，使用 aiofiles 写入
    - ToolExecutor.execute 中 truncate_if_needed 调用有 await
    - test_result_truncator.py 和 test_tool_executor.py 全部通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| async framework -> file system | aiofiles wraps file I/O; no new trust boundary created |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Denial of Service | memory/ async I/O | accept | aiofiles mitigates event loop blocking; concurrent reads bounded by asyncio scheduler |
| T-16-SC | Tampering | pip install aiofiles | mitigate | aiofiles is well-established (1.3k+ GitHub stars, 100M+ downloads); verify via pypi.org |
</threat_model>

<verification>
1. `cd framework && pytest tests/ -v` — 全部 964+ 测试通过
2. `grep -r "\.read_text\|\.write_text\|open(" framework/agent_framework/memory/ --include="*.py" | grep -v __pycache__` — 确认 memory/ 中无遗留同步 I/O（frontmatter.py 纯计算除外）
3. `grep -r "open(" framework/agent_framework/tools/context/result_truncator.py` — 确认无遗留同步 open()
</verification>

<success_criteria>
- memory/ 模块所有文件读写使用 aiofiles
- result_truncator.py 文件写入使用 aiofiles
- 所有方法签名改为 async def
- `cd framework && pytest tests/ -v` 通过 964+ 测试
- framework/pyproject.toml 包含 aiofiles 依赖
</success_criteria>

<output>
Create `.planning/phases/16-framework/16-01-SUMMARY.md` when done
</output>
