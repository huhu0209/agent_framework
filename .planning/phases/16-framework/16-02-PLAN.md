---
phase: 16-framework
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - framework/agent_framework/tools/mcp/transport.py
  - framework/agent_framework/tools/mcp/config.py
autonomous: true
requirements:
  - FW-SEC-03
  - FW-SEC-08

must_haves:
  truths:
    - "MCP 子进程只继承白名单环境变量（PATH, HOME, TEMP, TMPDIR, USER, LANG, SYSTEMROOT），不再继承全部 os.environ"
    - "config 中显式声明的 env 经过敏感 key 过滤后才传入子进程"
    - "_BLOCKED_ENV_PATTERNS 包含 auth, session, cookie, bearer, refresh, jwt 模式"
    - "全部 964+ 测试通过"
  artifacts:
    - path: "framework/agent_framework/tools/mcp/transport.py"
      provides: "白名单 env 构建逻辑"
      contains: "_ALLOWED_ENV_KEYS"
    - path: "framework/agent_framework/tools/mcp/config.py"
      provides: "扩展的敏感 key 过滤"
      contains: "auth"
  key_links:
    - from: "framework/agent_framework/tools/mcp/transport.py"
      to: "framework/agent_framework/tools/mcp/config.py"
      via: "_reject_sensitive_env_keys validator"
      pattern: "_reject_sensitive_env"
---

<objective>
MCP 子进程环境变量从全量继承改为白名单机制，并扩展敏感 key 过滤模式。

Purpose: 当前 MCP server 子进程继承全部 os.environ（含 API key 等敏感信息），第三方 MCP server 可读取宿主进程凭据（per D-04, D-05, D-06）。
Output: transport.py 白名单 env 构建 + config.py 扩展过滤模式。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-framework/16-CONTEXT.md

<interfaces>
From framework/agent_framework/tools/mcp/transport.py:
- StdioTransport.__init__(command, args, env) — self._env stores config env
- StdioTransport.connect() line 57: env = {**os.environ, **(self._env or {})} — THIS IS THE TARGET

From framework/agent_framework/tools/mcp/config.py:
- _BLOCKED_ENV_PATTERNS tuple (7 patterns) — needs expansion
- McpServerConfig.env field_validator _reject_sensitive_env_keys
- McpManager._create_transport(cfg) — passes cfg.env or None to StdioTransport
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: transport.py 白名单 env 构建</name>
  <files>
    framework/agent_framework/tools/mcp/transport.py
  </files>
  <read_first>
    framework/agent_framework/tools/mcp/transport.py
    framework/tests/test_mcp_transport.py
  </read_first>
  <behavior>
    - StdioTransport.connect builds env from whitelist only, not os.environ
    - Whitelist includes: PATH, HOME, TEMP, TMP, TMPDIR, USER, LANG, SYSTEMROOT
    - Config env (self._env) values are merged on top of whitelist base
    - Existing test_mcp_transport.py tests still pass
  </behavior>
  <action>
Per D-04, D-06: Replace full os.environ inheritance with whitelist mechanism.

In transport.py:
1. Add a module-level constant `_ALLOWED_ENV_KEYS: frozenset[str]` containing the minimum required environment variables: PATH, HOME, TEMP, TMP, TMPDIR, USER, LANG, SYSTEMROOT. These cover Windows (TEMP, SYSTEMROOT) and Unix (TMPDIR, HOME) basics.

2. In `StdioTransport.connect()` (line 57), replace:
   `env = {**os.environ, **(self._env or {})}`
   with:
   ```
   base_env = {k: v for k, v in os.environ.items() if k in _ALLOWED_ENV_KEYS}
   env = {**base_env, **(self._env or {})}
   ```

3. Update tests in test_mcp_transport.py if any test asserts on the subprocess receiving full os.environ. The tests should be updated to verify that only whitelisted keys plus config env are passed.

Note: self._env has already been validated by McpServerConfig._reject_sensitive_env_keys validator, so it is safe to merge on top of the whitelist base.
  </action>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && pytest tests/test_mcp_transport.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>
    - _ALLOWED_ENV_KEYS 常量定义在 transport.py 模块级
    - StdioTransport.connect 中 env 构建从白名单开始
    - 不再使用 {**os.environ, ...} 全量继承
    - test_mcp_transport.py 全部通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: config.py 扩展敏感 key 过滤 + shutdown logger</name>
  <files>
    framework/agent_framework/tools/mcp/config.py
  </files>
  <read_first>
    framework/agent_framework/tools/mcp/config.py
    framework/tests/test_mcp_manager.py
  </read_first>
  <behavior>
    - _BLOCKED_ENV_PATTERNS includes: auth, session, cookie, bearer, refresh, jwt (6 new patterns)
    - Total patterns: 13 (7 original + 6 new)
    - McpManager.shutdown logs debug message on client close failure instead of pass
    - test_mcp_manager.py passes
  </behavior>
  <action>
Per D-05, D-12: Expand blocked patterns and fix try-except-pass in shutdown.

In config.py:

1. Expand `_BLOCKED_ENV_PATTERNS` tuple — add 6 new patterns after the existing 7:
   "auth", "session", "cookie", "bearer", "refresh", "jwt"
   Total: ("api_key", "token", "secret", "password", "credential", "private_key", "access_key", "auth", "session", "cookie", "bearer", "refresh", "jwt")

2. In `McpManager.shutdown()` (lines 97-101), replace:
   ```
   except Exception:
       pass
   ```
   with:
   ```
   except Exception:
       logger.debug("MCP client '%s' 关闭失败", name)
   ```
   Note: Need to get the server name. Currently the loop is `for client in self._clients.values()` which loses the name. Change to `for name, client in self._clients.items()` to get the name for logging.

3. Update test_mcp_manager.py if needed to account for the loop variable change (for name, client in ...items()).
  </action>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && pytest tests/test_mcp_manager.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>
    - _BLOCKED_ENV_PATTERNS 包含 13 个模式（原 7 + 新 6）
    - McpManager.shutdown 中 except 分支有 logger.debug 记录
    - shutdown 循环改为 items() 遍历以获取 name
    - test_mcp_manager.py 全部通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host process -> MCP subprocess | Environment variable inheritance boundary (attack surface being reduced) |
| MCP config -> validator | User-provided config env values validated before passing to subprocess |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | Information Disclosure | MCP subprocess env | mitigate | Whitelist only essential env vars; block sensitive patterns in config env |
| T-16-03 | Elevation of Privilege | MCP config env injection | mitigate | Expanded _BLOCKED_ENV_PATTERNS catches auth/session/jwt/etc. |
</threat_model>

<verification>
1. `cd framework && pytest tests/ -v` — 全部 964+ 测试通过
2. `grep "_ALLOWED_ENV_KEYS" framework/agent_framework/tools/mcp/transport.py` — 白名单常量存在
3. `grep -c "os.environ" framework/agent_framework/tools/mcp/transport.py` — 确认 os.environ 仅用于白名单过滤（不直接传给子进程）
4. `grep "auth\|session\|cookie\|bearer\|refresh\|jwt" framework/agent_framework/tools/mcp/config.py` — 6 个新模式存在
</verification>

<success_criteria>
- MCP 子进程只继承白名单环境变量
- _BLOCKED_ENV_PATTERNS 从 7 扩展到 13 个模式
- shutdown 异常有 logger.debug 记录
- 全部测试通过
</success_criteria>

<output>
Create `.planning/phases/16-framework/16-02-SUMMARY.md` when done
</output>
