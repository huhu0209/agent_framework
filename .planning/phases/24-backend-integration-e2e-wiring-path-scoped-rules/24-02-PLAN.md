---
phase: 24-backend-integration-e2e-wiring-path-scoped-rules
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - framework/agent_framework/prompts/assembler.py
  - framework/tests/test_prompt_assembler.py
autonomous: true
requirements:
  - INS-06
  - INT-05

must_haves:
  truths:
    - "PromptAssembler.assemble() accepts (loader, profile, context_path=None) signature per D-09"
    - "assemble() builds blocks in order: USER_PROVIDED -> RULES -> SOUL -> AGENTS_RULES -> IDENTITY -> SKILLS -> TOOL_GUIDANCE per D-10"
    - "USER_PROVIDED block content comes from loader.load_agents_md() per D-11"
    - "RULES block content comes from RuleLoader.load_rules(loader, context_path) per D-11"
    - "render() delegates to assemble() with the new signature"
    - "All 1121+ existing tests pass (zero regression) per INT-05"
  artifacts:
    - path: "framework/agent_framework/prompts/assembler.py"
      provides: "Modified assemble() and render() with new signatures"
      exports: ["PromptAssembler"]
    - path: "framework/tests/test_prompt_assembler.py"
      provides: "Updated tests for new assemble() signature"
      min_lines: 100
  key_links:
    - from: "framework/agent_framework/prompts/assembler.py"
      to: "framework/agent_framework/config/loader.py"
      via: "ConfigLoader parameter in assemble()"
      pattern: "ConfigLoader"
    - from: "framework/agent_framework/prompts/assembler.py"
      to: "framework/agent_framework/rules/loader.py"
      via: "RuleLoader.load_rules call in assemble()"
      pattern: "RuleLoader"
---

<objective>
Modify PromptAssembler to integrate ConfigLoader and RuleLoader, update all 20 test methods.

Purpose: Per D-09, change assemble(profile) to assemble(loader, profile, context_path=None). Per D-10, reorder blocks to: USER_PROVIDED -> RULES -> SOUL -> AGENTS_RULES -> IDENTITY -> SKILLS -> TOOL_GUIDANCE. Per D-11, call loader.load_agents_md() and RuleLoader.load_rules(). This is the central integration point that connects all Phase 20-23 work.

Output: Modified assembler.py with new signatures and reordered blocks, updated test_prompt_assembler.py with all tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-backend-integration-e2e-wiring-path-scoped-rules/24-CONTEXT.md
@.planning/phases/24-backend-integration-e2e-wiring-path-scoped-rules/24-RESEARCH.md
@.planning/phases/24-backend-integration-e2e-wiring-path-scoped-rules/24-01-SUMMARY.md

<interfaces>
<!-- Executor needs these contracts -->

From framework/agent_framework/config/loader.py:
```python
class ConfigLoader:
    def load_agents_md(self) -> str: ...
    # Returns concatenated AGENTS.md chain string, empty string if nothing found
    def discover(self, module_name: str) -> list[Path]: ...
```

From framework/agent_framework/rules/loader.py (created in Plan 01):
```python
class RuleLoader:
    @staticmethod
    def load_rules(loader: ConfigLoader, context_path: str | None = None) -> str: ...
    # Returns joined rules content string, empty string if no matching rules
```

From framework/agent_framework/prompts/profiles.py:
```python
class PromptBlock(BaseModel):
    name: str
    content: str
    source: Literal["file", "auto_generated", "injected"]
    stability: Literal["static", "semi_static", "dynamic"]
    cache_breakpoint: bool

class AgentProfile(BaseModel):
    name: str
    description: str
    soul: str = ""
    agents_rules: str = ""
    identity: str = ""
    user_context: str | None = None
    tool_guidance: str | None = None
```

Current _BLOCK_TAGS in assembler.py:
```python
_BLOCK_TAGS: dict[str, str] = {
    "SOUL": "soul",
    "AGENTS_RULES": "instructions",
    "IDENTITY": "identity",
    "USER": "user-provided",
    "SKILLS": "skills",
    "TOOL_GUIDANCE": "tool-guidance",
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modify PromptAssembler with new signature and block order</name>
  <files>
    framework/agent_framework/prompts/assembler.py
  </files>
  <read_first>
    framework/agent_framework/prompts/assembler.py,
    framework/agent_framework/prompts/profiles.py,
    framework/agent_framework/rules/loader.py,
    framework/agent_framework/config/loader.py,
    framework/tests/test_prompt_assembler.py
  </read_first>
  <action>
    Modify framework/agent_framework/prompts/assembler.py per D-09, D-10, D-11, D-12.

    **Add imports**: Add `from agent_framework.config.loader import ConfigLoader` and `from agent_framework.rules.loader import RuleLoader` at top level (not behind TYPE_CHECKING — these are used at runtime). Remove `from typing import TYPE_CHECKING` if no longer needed (check if SkillRegistry TYPE_CHECKING import is still used). Keep the `from agent_framework.prompts.profiles import AgentProfile, PromptBlock` import.

    **Update _BLOCK_TAGS**: Add two new entries: `"USER_PROVIDED": "user-provided"` and `"RULES": "rules"`. Remove the old `"USER": "user-provided"` entry since the new USER_PROVIDED replaces it.

    **Modify assemble() signature**: Change from `def assemble(self, profile: AgentProfile) -> list[PromptBlock]` to `def assemble(self, loader: ConfigLoader, profile: AgentProfile, context_path: str | None = None) -> list[PromptBlock]` per D-09.

    **Reorder block construction per D-10**:
    1. USER_PROVIDED block: call `loader.load_agents_md()`. If result is non-empty, create PromptBlock with name="USER_PROVIDED", source="auto_generated", stability="semi_static", cache_breakpoint=True per D-11.
    2. RULES block: call `RuleLoader.load_rules(loader, context_path)`. If result is non-empty, create PromptBlock with name="RULES", source="auto_generated", stability="semi_static", cache_breakpoint=True per D-11.
    3. SOUL block: from `profile.soul`, name="SOUL", source="injected", stability="static", cache_breakpoint=True (unchanged).
    4. AGENTS_RULES block: from `profile.agents_rules`, name="AGENTS_RULES", source="injected", stability="static", cache_breakpoint=True (unchanged).
    5. IDENTITY block: from `profile.identity`, name="IDENTITY", source="injected", stability="semi_static", cache_breakpoint=True (unchanged).
    6. SKILLS block: from `self._skill_registry.describe_available()` if skill_registry is not None, name="SKILLS", source="auto_generated", stability="static", cache_breakpoint=False (unchanged).
    7. TOOL_GUIDANCE block: from `profile.tool_guidance`, name="TOOL_GUIDANCE", source="injected", stability="static", cache_breakpoint=False (unchanged).

    Remove the old USER block that read from `profile.user_context`. The user_context field on AgentProfile is no longer used as a separate block — its content is now part of the AGENTS.md chain loaded via ConfigLoader.

    **Modify render() signature**: Change from `def render(self, profile: AgentProfile) -> str` to `def render(self, loader: ConfigLoader, profile: AgentProfile, context_path: str | None = None) -> str`. Update internal call from `self.assemble(profile)` to `self.assemble(loader, profile, context_path)`.

    **Keep __init__ unchanged**: `def __init__(self, skill_registry: SkillRegistry | None = None) -> None:` stays the same per A4 in research.
  </action>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && python -c "from agent_framework.prompts.assembler import PromptAssembler; print('import OK')"</automated>
  </verify>
  <done>
    PromptAssembler.assemble() has signature (loader, profile, context_path=None), builds blocks in D-10 order, calls loader.load_agents_md() and RuleLoader.load_rules(). render() signature also updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update all 20 test methods for new signatures</name>
  <files>
    framework/tests/test_prompt_assembler.py
  </files>
  <read_first>
    framework/tests/test_prompt_assembler.py,
    framework/agent_framework/prompts/assembler.py (after Task 1 changes)
  </read_first>
  <action>
    Update all test methods in framework/tests/test_prompt_assembler.py to work with the new assemble(loader, profile, context_path=None) and render(loader, profile, context_path=None) signatures.

    **Add imports**: Add `from agent_framework.config.loader import ConfigLoader` at module top level.

    **Create a module-level test helper**: `_make_loader(tmp_path)` that creates a ConfigLoader with tmp_path subdirectories (global + project dirs), following the pattern from test_loader.py TestLoadAgentsMd._make_loader. Returns ConfigLoader instance with no config files written (so load_agents_md returns "", load_rules returns "" for clean tests).

    **Update TestPromptAssembler** (9 test methods):
    - Every call to `assembler.assemble(profile=profile)` becomes `assembler.assemble(loader=_make_loader(tmp_path) if tmp_path available, else _no_op_loader(), profile=profile)`.
    - Since TestPromptAssembler tests do not use tmp_path fixture for most tests, add a module-level `_no_op_loader` function that creates a ConfigLoader with a temp directory that has no .agent-framework files. Alternatively, use a `@pytest.fixture` that provides a no-op ConfigLoader.
    - Every call to `assembler.render(profile=profile)` becomes `assembler.render(loader=loader, profile=profile)`.
    - Update assertions: The old USER block test (`test_assemble_with_user_context`) now tests USER_PROVIDED instead. Since load_agents_md() returns "" for the no-op loader, the USER_PROVIDED block will not appear. Update this test to verify that when user_context is set on profile, no separate USER block appears (because user_context is no longer a block).
    - Update `test_cache_breakpoints_set`: The block order has changed. SOUL is now at index 1+ (after USER_PROVIDED and RULES which are empty with no-op loader), so assertions should use `next(b for b in blocks if b.name == "SOUL")` pattern which is already the case.
    - Update `test_assemble_renders_to_string`: render() now takes loader param.
    - Update `test_render_empty_profile`: render() with no-op loader and empty profile returns "".

    **Update TestPromptAssemblerWithSkills** (4 test methods):
    - Add `loader` parameter using the same no-op pattern.
    - `test_skills_block_position`: The block order is now USER_PROVIDED -> RULES -> SOUL -> AGENTS_RULES -> IDENTITY -> SKILLS -> TOOL_GUIDANCE. With no-op loader, USER_PROVIDED and RULES are absent, so SKILLS is still before TOOL_GUIDANCE. Update the name index assertion accordingly.

    **Update TestRenderXmlTags** (7 test methods):
    - All render() calls need loader parameter.
    - `test_user_context_wrapped_in_user_provided`: This tested the old USER block from profile.user_context. With the new design, user_context on profile is no longer rendered as a block. Update this test to verify USER_PROVIDED tag appears when loader.load_agents_md() returns content. Create a ConfigLoader with a tmp_path that has an AGENTS.md file.
    - `test_full_profile_all_tags_present`: Update to verify all expected tags: soul, instructions, identity, tool-guidance, plus user-provided (if loader provides content) and rules (if loader provides rules). With no-op loader, only profile-based tags appear.

    **Add new tests**:
    - `test_user_provided_block_from_loader`: Create a ConfigLoader with a tmp_path that has `.agent-framework/AGENTS.md` with test content. Call assemble() and verify USER_PROVIDED block contains that content.
    - `test_rules_block_from_loader`: Create a ConfigLoader with a tmp_path that has `.agent-framework/rules/test.md` with test content. Call assemble() and verify RULES block contains that content.
    - `test_context_path_passed_to_rule_loader`: Call assemble() with context_path="src/main.py" and verify it is forwarded correctly (rule with matching paths loaded, rule with non-matching paths not loaded).

    Keep the file's existing style: Chinese docstrings, class-based grouping, same import patterns.
  </action>
  <verify>
    <automated>cd /Users/huhu/project/agent_framework/framework && pytest tests/test_prompt_assembler.py -v</automated>
  </verify>
  <done>
    All test_prompt_assembler.py tests pass with new assemble(loader, profile, context_path) and render(loader, profile, context_path) signatures. New tests verify USER_PROVIDED and RULES blocks from ConfigLoader. Total test count >= 23 (20 existing + 3 new minimum).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ConfigLoader -> PromptAssembler | loader provides instruction chain and rule content as strings |
| RuleLoader -> PromptAssembler | rules filtered by context_path, returned as string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03 | Tampering | assemble() block order manipulation | accept | block order is deterministic in code, not configurable |
| T-24-04 | Information Disclosure | AGENTS.md content in system prompt | accept | intentional design — instructions are meant for the LLM |
| T-24-SC | Tampering | pip installs | mitigate | zero new packages in this plan |
</threat_model>

<verification>
- PromptAssembler tests pass: `cd framework && pytest tests/test_prompt_assembler.py -v`
- Full regression suite passes: `cd framework && pytest tests/ -q`
</verification>

<success_criteria>
- PromptAssembler.assemble() signature is (loader, profile, context_path=None) per D-09
- Block order follows D-10: USER_PROVIDED -> RULES -> SOUL -> AGENTS_RULES -> IDENTITY -> SKILLS -> TOOL_GUIDANCE
- USER_PROVIDED from loader.load_agents_md() per D-11
- RULES from RuleLoader.load_rules() per D-11
- All 1121+ existing tests pass (INT-05)
</success_criteria>

<output>
Create `.planning/phases/24-backend-integration-e2e-wiring-path-scoped-rules/24-02-SUMMARY.md` when done
</output>
