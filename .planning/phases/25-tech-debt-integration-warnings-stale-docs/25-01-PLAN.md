---
phase: 25-tech-debt-integration-warnings-stale-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
autonomous: true
requirements: [D-01, D-02, D-15, D-16, D-17]

must_haves:
  truths:
    - "pytest -W all produces zero asyncio warnings in 1146 tests"
    - "pytest with DeprecationWarning-as-error mode passes all 1146 tests"
    - "STATE.md Deferred Items table no longer contains WR-01/02/03 row"
    - "STATE.md Current Position reflects Phase 25 execution"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Accurate project state with WR-01/02/03 resolved"
      contains: "Phase 25"
  key_links:
    - from: "STATE.md Deferred Items"
      to: "WR-01/02/03 resolution"
      via: "row removal after verification passes"
      pattern: "WR-01.*asyncio"
---

<objective>
Verify that all asyncio timing/cleanup warnings (WR-01/02/03) have been resolved by environment/dependency updates, then update STATE.md to reflect current project position.

Purpose: Confirm the pre-existing tech debt from v0.0.1 is self-healed and update project tracking docs.
Output: Verified test output (zero asyncio warnings) and updated STATE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run dual-layer asyncio warning verification</name>
  <files>(no file modifications — test runs only)</files>
  <read_first>
    - .planning/phases/25-tech-debt-integration-warnings-stale-docs/25-CONTEXT.md
  </read_first>
  <action>Run the two verification commands per D-01:

1. Standard warning check: `cd framework && pytest tests/ -v -W all 2>&1 | grep -iE "(warning|asyncio.*cleanup|timing)"` — confirm zero asyncio-related lines in output.
2. Strict deprecation check: `cd framework && pytest tests/ -W error::DeprecationWarning -W error::PendingDeprecationWarning --tb=short` — confirm all 1146 tests pass (exit code 0) with no deprecation warnings promoted to errors.

If both pass, WR-01/02/03 are confirmed self-healed (resolved-by-environment-update). If either fails, document the specific warnings/errors found.</action>
  <verify>
    <automated>cd framework && pytest tests/ -W error::DeprecationWarning -W error::PendingDeprecationWarning --tb=short -q 2>&1 | tail -3</automated>
  </verify>
  <done>Both verification commands pass: (1) grep output is empty for asyncio warnings, (2) all 1146 tests pass with deprecation-as-error mode.</done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md — remove WR-01/02/03 deferred item and update current position</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md
  </read_first>
  <action>Apply three updates to STATE.md per D-15, D-16, D-17:

1. **D-15**: In the Deferred Items table, remove the row: `| test_warning | WR-01/02/03 asyncio timing/cleanup | pre-existing | v0.0.1 |`. This row is no longer applicable.

2. **D-16**: Update the "Current Position" section:
   - Change `Phase: 24 (backend-integration-e2e-wiring-path-scoped-rules) — EXECUTING` to `Phase: 25 (tech-debt-integration-warnings-stale-docs) — EXECUTING`
   - Change `Plan: 1 of 3` to `Plan: 1 of 4`
   - Change `Status: Executing Phase 24` to `Status: Executing Phase 25`
   - Change `Last activity: 2026-06-12 -- Phase 24 execution started` to `Last activity: 2026-06-12 -- Phase 25 execution`

3. **D-17**: Update the progress percentage line:
   - Change `Progress: [████████░░] 80%` to `Progress: [████████░░] 83%`
   - The `percent: 83` in frontmatter is already correct.

Do NOT modify `last_updated` frontmatter timestamp or other sections.</action>
  <verify>
    <automated>grep -c "WR-01" .planning/STATE.md && grep -c "Phase 25" .planning/STATE.md</automated>
  </verify>
  <done>STATE.md has zero mentions of WR-01/02/03 in Deferred Items, Current Position shows Phase 25, progress shows 83%.</done>
</task>

</tasks>

<verification>
1. `cd framework && pytest tests/ -W error::DeprecationWarning -W error::PendingDeprecationWarning --tb=short -q` exits 0
2. `grep -c "WR-01" .planning/STATE.md` returns 0
3. `grep "Phase 25" .planning/STATE.md` shows current position
</verification>

<success_criteria>
- All 1146 tests pass with zero asyncio warnings and zero deprecation warnings
- STATE.md Deferred Items table has no WR-01/02/03 row
- STATE.md Current Position reflects Phase 25 execution
</success_criteria>

<output>
Create `.planning/phases/25-tech-debt-integration-warnings-stale-docs/25-01-SUMMARY.md` when done
</output>
