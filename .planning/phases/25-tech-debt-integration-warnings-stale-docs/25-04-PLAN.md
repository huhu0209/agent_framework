---
phase: 25-tech-debt-integration-warnings-stale-docs
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements: [D-09, D-10, D-11, D-12, D-13, D-14]

must_haves:
  truths:
    - "PROJECT.md Current State reflects Phase 24 completed and Phase 25 in progress"
    - "PROJECT.md includes v0.0.6 progress narrative after v0.0.5 shipped section"
    - "CONCERNS.md has 4 issues marked as Resolved with version and explanation"
    - "CONCERNS.md CommandPolicy entry remains unresolved per D-13"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Current project state documentation"
      contains: "v0.0.6"
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Accurate known-issues tracker"
      contains: "RESOLVED"
  key_links:
    - from: "CONCERNS.md resolved items"
      to: "milestone history"
      via: "version numbers in resolution markers"
      pattern: "RESOLVED \\(v0\\.0\\.[23]\\)"
---

<objective>
Update PROJECT.md to reflect v0.0.6 progress (Phases 20-24 complete, 1146 tests), and mark 4 resolved issues in CONCERNS.md while preserving historical records.

Purpose: PROJECT.md is the project context document used across sessions. CONCERNS.md tracks known issues — several have been resolved in v0.0.2~v0.0.6 but never marked.
Output: Updated PROJECT.md with v0.0.6 narrative and updated CONCERNS.md with 4 resolved markers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PROJECT.md — v0.0.6 progress narrative and current state</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (full file, focus on lines 49-56 Active section, lines 163-175 Current State and Evolution sections)
  </read_first>
  <action>Apply three updates per D-09, D-10, D-11:

**D-09 — Current State section (around line 163-167):**
Replace the Current State block:
```
**Shipped v0.0.5** — 1002 测试通过，全部 HIGH + 关键 MEDIUM issue 已修复。

**Current: v0.0.6** — 路径文件的统一。参照 Claude Code 路径机制，实现统一配置层级体系。
```
With:
```
**Shipped v0.0.5** — 1002 测试通过，全部 HIGH + 关键 MEDIUM issue 已修复。

**Shipped v0.0.6** — 路径文件的统一（2026-06-12）。1146 测试通过（+144 新增）。
参照 Claude Code 路径机制，实现统一配置层级体系：
- ConfigLoader 统一入口（settings.json 四级覆盖链）
- discover_paths() 8 种模块类型自动发现
- from_loader() 工厂方法（Skills/Hooks/Commands/Agents/Profiles/MCP/Tasks/Permissions）
- AGENTS.md 指令链 + RuleLoader path-scoped 过滤
- Backend 集成 + 端到端验证

**Current:** Phase 25 — 技术债务清理（asyncio 警告验证 + 过时文档更新）
```

**D-10 — Active requirements section (around line 50-56):**
The Active section already describes v0.0.6. Update it to show completion:
Change `**v0.0.6 — 路径文件的统一**` section. Replace the bullet list with a completion note:
```
**v0.0.6 — 路径文件的统一** ✅ Shipped (2026-06-12, 1146 tests)
- ConfigLoader + Settings + Merge + Discovery + Loader — complete
- 8 module adapters (from_loader factory methods) — complete
- AGENTS.md instruction chain + RuleLoader — complete
- Backend integration + E2E wiring — complete
```

**D-11 — Last updated timestamp (line 174):**
Change `*Last updated: 2026-06-11 after v0.0.6 milestone initiated*` to `*Last updated: 2026-06-12 — v0.0.6 shipped (1146 tests)*`

Also update the Tech Stack line for test count:
- Change `pytest, pytest-asyncio (964 tests passing)` to `pytest, pytest-asyncio (1146 tests passing)`</action>
  <verify>
    <automated>grep "1146" .planning/PROJECT.md; grep "v0.0.6.*Shipped\|Shipped v0.0.6" .planning/PROJECT.md; grep "2026-06-12" .planning/PROJECT.md; grep "Phase 25" .planning/PROJECT.md</automated>
  </verify>
  <done>PROJECT.md shows v0.0.6 as shipped with 1146 tests, includes progress narrative, shows Phase 25 as current, and has updated timestamp.</done>
</task>

<task type="auto">
  <name>Task 2: Mark 4 resolved issues in CONCERNS.md per D-12/D-13/D-14</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <read_first>
    - .planning/codebase/CONCERNS.md (full file, focus on Tech Debt section lines 7-47)
  </read_first>
  <action>Per D-12, add `RESOLVED` markers to 4 issues in the Tech Debt section. Do NOT delete any content — append resolution markers preserving original records.

Per D-13, mark exactly these 4 issues as resolved. Per D-14, do NOT audit other issues.

**1. "Backend is entirely scaffold" (first issue, around lines 7-11):**
After the "Fix approach" line, add:
```
**✅ RESOLVED (v0.0.3~v0.0.6):** Backend progressively implemented — EventBus (v0.0.3), code review fixes (v0.0.4~v0.0.5), ConfigLoader integration (v0.0.6). Files now contain real implementation.
```

**2. "Orchestrator engine and router are empty" (around lines 13-18):**
After the "Fix approach" line, add:
```
**✅ RESOLVED (v0.0.2):** Orchestrator engine implemented in Phase 7. Planner, plan parsing, drift detection all functional. Engine and router are no longer empty.
```

**3. "Agent tool dispatch is a stub" (around lines 20-23):**
After the "Fix approach" line, add:
```
**✅ RESOLVED (v0.0.2):** Sub-Agent dispatch implemented via `run_subagent` tool in `agents/sub_agent.py`. Agent-to-agent tool calling works through `agent__` prefix routing.
```

**4. "web_search is a mock implementation" (around lines 32-35):**
After the "Fix approach" line, add:
```
**✅ RESOLVED (v0.0.2):** Replaced with Tavily API integration (`AsyncTavilyClient`). Returns real search results when `TAVILY_API_KEY` is configured.
```

Do NOT modify "CommandPolicy is a placeholder interface" (per D-13 — it is still a placeholder).
Do NOT modify any other sections (Known Bugs, Security Considerations, etc.).</action>
  <verify>
    <automated>grep -c "RESOLVED" .planning/codebase/CONCERNS.md; grep -c "CommandPolicy" .planning/codebase/CONCERNS.md</automated>
  </verify>
  <done>CONCERNS.md has exactly 4 RESOLVED markers (Backend scaffold, Orchestrator, Agent dispatch, web_search). CommandPolicy entry is present and unmodified (still unresolved).</done>
</task>

</tasks>

<verification>
1. `grep -c "Shipped v0.0.6\|v0.0.6.*Shipped" .planning/PROJECT.md` returns at least 1
2. `grep "1146" .planning/PROJECT.md` returns matches in both Current State and Tech Stack
3. `grep "Phase 25" .planning/PROJECT.md` returns a match
4. `grep -c "RESOLVED" .planning/codebase/CONCERNS.md` returns 4
5. `grep "CommandPolicy is a placeholder" .planning/codebase/CONCERNS.md` returns the unchanged entry
</verification>

<success_criteria>
- PROJECT.md reflects v0.0.6 as shipped with 1146 tests and progress narrative
- PROJECT.md shows Phase 25 as current work
- CONCERNS.md has 4 RESOLVED markers with version numbers
- CommandPolicy remains marked as unresolved
- No other CONCERNS.md entries were modified
</success_criteria>

<output>
Create `.planning/phases/25-tech-debt-integration-warnings-stale-docs/25-04-SUMMARY.md` when done
</output>
