---
phase: 25-tech-debt-integration-warnings-stale-docs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [D-03, D-04]

must_haves:
  truths:
    - "README.md contains correct class name CommandDispatcher (not CommandRouter)"
    - "README.md project structure includes config/ and rules/ modules"
    - "README.md module overview table includes ConfigLoader and RuleLoader entries"
    - "README.md Roadmap reflects v0.0.6 completion state"
    - "README.md test count is 1146"
  artifacts:
    - path: "README.md"
      provides: "Accurate project documentation reflecting v0.0.6 state"
      contains: "CommandDispatcher"
  key_links:
    - from: "README.md project structure"
      to: "actual framework directory layout"
      via: "directory listing verification"
      pattern: "config/"
---

<objective>
Update README.md to reflect current v0.0.6 state — fixing incorrect class names, adding missing modules, updating roadmap, and correcting test count.

Purpose: README.md is the first document visitors see. It currently references wrong class names, omits entire modules (config, rules), shows a roadmap that stops at Phase 13, and lists an outdated test count.
Output: Accurate README.md reflecting the real project state after v0.0.6.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix class names and add missing modules to README.md project structure + module overview</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file, especially lines 56-90 for structure and table)
    - framework/agent_framework/config/__init__.py (to get exact exports)
    - framework/agent_framework/rules/__init__.py (to get exact exports)
  </read_first>
  <action>Apply the following changes per D-04:

**1. Fix class name (D-04a):**
- In the module overview table (line 84): change `| \`commands/\` | \`CommandRouter\` | Slash 命令 |` to `| \`commands/\` | \`CommandDispatcher\` | Slash 命令 |`
- The actual class is `CommandDispatcher` in `commands/dispatcher.py` (verified).

**2. Add config/ and rules/ to project structure (D-04b):**
In the project structure tree (lines 56-71), add these two entries after `hooks/`:
```
├── config/         # 配置层级（ConfigLoader / Settings / Merge）
├── rules/          # 规则加载（RuleLoader / path-scoped filtering）
```
Insert them in alphabetical order: `commands/`, then `config/`, then `hooks/`, then `rules/` is already after `prompts/` in the tree — actually `rules/` should go after `prompts/`. Place `config/` after `commands/` and `rules/` after `prompts/`.

**3. Add ConfigLoader and RuleLoader to module overview table (D-04d):**
Add two new rows to the module overview table:
- `| \`config/\` | \`ConfigLoader\`, \`Settings\` | 配置层级加载与合并 |`
- `| \`rules/\` | \`RuleLoader\` | 路径过滤规则加载 |`

Insert in alphabetical order within the table.

**4. Update test count (D-04e):**
- Change the mention of test count from `964` (in Tech Stack or Quick Start) to `1146`. Search for any occurrence of `964` or `1002` in the file and update to `1146`.</action>
  <verify>
    <automated>grep -c "CommandRouter" README.md; grep -c "CommandDispatcher" README.md; grep -c "config/" README.md; grep -c "rules/" README.md; grep -c "ConfigLoader" README.md; grep -c "RuleLoader" README.md</automated>
  </verify>
  <done>README.md has zero occurrences of `CommandRouter`, contains `CommandDispatcher`, `config/`, `rules/`, `ConfigLoader`, and `RuleLoader` in both the project structure tree and the module overview table.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md Roadmap to reflect v0.0.6 completion</name>
  <files>README.md</files>
  <read_first>
    - README.md (Roadmap section, lines 92-103)
    - .planning/ROADMAP.md (for accurate phase history)
  </read_first>
  <action>Replace the Roadmap section (currently lines 92-103) per D-04c. The current roadmap stops at "Phase 13: Plugin — 计划中" which is severely outdated.

Replace with a comprehensive roadmap reflecting all 6 milestones:

```markdown
## Roadmap

| Milestone | Phases | Status |
|-----------|--------|--------|
| v0.0.1 彻底 Code Review | 1-5 | ✅ Shipped (2026-05-29, 687 tests) |
| v0.0.2 Agent 扩展与编排 | 6-8 | ✅ Shipped (2026-05-29, 812 tests) |
| v0.0.3 Agent 可视化平台 MVP | 9-11 | ✅ Shipped (2026-05-31, 964 tests) |
| v0.0.4 全面代码审查 | 12-14 | ✅ Shipped (2026-06-09, 964 tests) |
| v0.0.5 Review 问题修复 | 15-19 | ✅ Shipped (2026-06-10, 1002 tests) |
| v0.0.6 路径文件的统一 | 20-25 | ✅ Shipped (2026-06-12, 1146 tests) |
```

This replaces the old phase-by-phase table with a milestone-level view that accurately reflects project history.</action>
  <verify>
    <automated>grep -c "v0.0.6" README.md; grep -c "1146" README.md; grep -c "Plugin" README.md</automated>
  </verify>
  <done>README.md Roadmap shows all 6 milestones with correct dates and test counts. No reference to "Phase 13: Plugin" remains.</done>
</task>

</tasks>

<verification>
1. `grep "CommandRouter" README.md` returns nothing (exit code 1)
2. `grep "CommandDispatcher" README.md` returns a match
3. `grep "config/" README.md` returns at least 2 matches (structure tree + table)
4. `grep "rules/" README.md` returns at least 2 matches
5. `grep "ConfigLoader" README.md` returns a match
6. `grep "RuleLoader" README.md` returns a match
7. `grep "v0.0.6" README.md` returns a match
8. `grep "1146" README.md` returns a match
</verification>

<success_criteria>
- Zero occurrences of `CommandRouter` in README.md
- Project structure includes `config/` and `rules/` entries
- Module overview table includes ConfigLoader and RuleLoader rows
- Roadmap covers all 6 milestones through v0.0.6
- Test count shows 1146
</success_criteria>

<output>
Create `.planning/phases/25-tech-debt-integration-warnings-stale-docs/25-03-SUMMARY.md` when done
</output>
